# Срез 2 «Рабочее место: лента» — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Цель:** бот получает второй вход — страницу в браузере. Туда перетаскивают
фотографию чека или пишут трату текстом, там же видно всё, что бот сделал, —
включая записи, пришедшие из телеграма. Телеграм перестаёт быть обязательным.

**Устройство:** один процесс, три потока. Главный поток — Flask на
`127.0.0.1`. Поток «телеграм» опрашивает Bot API. Поток «разбор» берёт задания
из очереди по одному и зовёт `intake.accept()` — единственную функцию разбора
на оба канала. Она возвращает события; `bot.py` подаёт их словами в чат,
`web.py` — словарями в браузер. Одно событие, две подачи.

**Почему потоков три, а в спеке два.** Спека рисует «Flask» и «опрос
телеграма». Третий поток — разбор — появляется потому, что вызов движка идёт
15–50 секунд: держать на нём HTTP-запрос браузера нельзя (браузер оборвёт его
раньше), а держать телеграмный опрос — значит не отвечать никому, пока читается
один чек. Схему спеки это не нарушает: приём по-прежнему один, просто он живёт
не в потоке того, кто прислал.

**Технологии:** Python 3.13, `requests`, `python-dotenv`, `flask`. На клиенте —
один файл `static/app.js` на обычном JS, без сборщиков и библиотек. Claude Code
CLI в headless-режиме, Google Apps Script — без изменений.

**Спека:** [2026-09-02-рабочее-место-design.md](../specs/2026-09-02-рабочее-место-design.md),
разделы «Лента и как в неё попадают», «Устройство», «Дизайн».
Роадмап: [2026-09-01-чеки-бот-roadmap.md](../specs/2026-09-01-чеки-бот-roadmap.md).
Предыдущий срез: [план среза 1](2026-09-01-срез-1-бот-который-работает.md).
Эталон вёрстки: `design/workspace.html`.

## Кто пишет клиентскую часть

Сессия, которая вела `templates/` и `static/`, закончилась, не дописав. На диске
лежит переделанный под новую спеку макет: разметка и стили готовы, скрипт
демонстрационный — переключает панели и ничего никуда не отправляет.

**Решение владельца 2 сентября: срез 2 берёт клиентскую часть целиком.** Иначе
«приём фото перетаскиванием» и «опрос `/api/events`» из роадмапа делать некому,
и срез не сдаётся. Разводить HTML и JS по разным авторам не будем: два писателя
на одну DOM — та же беда в профиль.

Практически это значит: задача 1 правит `templates/workspace.html`, задачи 6–9
пишут `static/app.js`. `static/style.css` не трогаем вовсе — там всё, что нужно
ленте, уже есть.

## Границы среза

**Делаем:** страницу на Flask, ленту, приём фотографии и текста из браузера,
показ телеграмных записей в той же ленте, опрос `/api/events`, дедупликацию по
хешу содержимого, «Записи сегодня» в сайдбаре, старт без телеграма.

**Не делаем:** правку последней записи (срез 3 — на «не 450, а 480» отвечаем
честным «правки пока не умею»), управление белым списком, замену бота и
переключатель движка (срез 4 — три пункта сайдбара до тех пор скрыты), мастер
установки (срез 5), адаптер Codex (срез 6), тёмную тему.

**Взято сверх трёх разделов спеки, и почему:**

1. **`BOT_TOKEN` становится необязательным.** Роадмап обещает срезом 2
   «телеграм больше не нужен», а `config.load_env()` сейчас выходит без токена.
   Без этой правки обещание не выполняется. Задача 10.
2. **Страница вместо выхода при `ANTHROPIC_API_KEY`.** Панель `#p-blocked`
   («Бот не запущен») в разметке уже есть, а бот при ключе делает `sys.exit` —
   страницу никто не увидит. Пункт 11 приёмки требует именно страницу.
   Задача 10.
3. **«Записи сегодня» в сайдбаре.** Формально это раздел «Сайдбар» (срез 4),
   но список собирается из событий, которые лента и так получила: три десятка
   строк на клиенте и ноль на сервере. Показывать вместо него выдуманные
   «Пятёрочка · 1 004,70» в работающем окне нельзя. Задача 9.

## Global Constraints

Требования, действующие во всех задачах без исключения.

- **Python 3.13**, венв в `.venv`. Запуск всегда `.venv/bin/python`, не `python`.
- **Зависимости ровно три:** `requests`, `python-dotenv`, `flask`. Ни gunicorn,
  ни waitress, ни flask-cors, ни pytest.
- **На клиенте ни одной библиотеки.** Один файл `static/app.js`, обычный JS,
  без сборщиков, npm, TypeScript, React и fetch-обёрток. Ученик открывает файл
  и читает его целиком.
- **Только `127.0.0.1`.** Ни `0.0.0.0`, ни внешнего адреса: страница показывает
  токен бота и секрет таблицы, а пароля нет — доступ ограничен тем, что адрес
  локальный.
- **Порт 8765, занят — следующий, до 8775.** Выбранный адрес печатается в
  терминал.
- **Никаких вебсокетов и SSE.** Страница опрашивает `/api/events` раз в три
  секунды. На локальном адресе опрос дешевле и понятнее на уроке.
- **У разбора один вход.** Телеграм и браузер зовут `intake.accept()`. Ни одна
  проверка, ни запись в таблицу, ни раскладка файлов не дублируется в канале.
- **Проверки без модели не спрашивают агента, уверен ли он.** `checks.py` в
  этом срезе не меняется ни строкой.
- **Канала в таблице нет.** Одиннадцать колонок листа «Расходы» остаются как
  есть, колонка «кто прислал» — чистое имя, без «· телеграм».
- **Своё не подписывается, чужое подписывается «имя · канал».** Человек впереди
  способа. Событие из браузера полей `author` и `channel` не несёт вовсе.
- **`sheet.gs` не трогаем.** Apps Script в этом срезе не меняется: обновление
  строки со сверкой продавца и суммы — срез 3.
- **Эталон вёрстки — `design/workspace.html`.** Цвета, размеры, отступы,
  скругления и иконки берутся оттуда. `static/style.css` уже им следует и в
  этом срезе не правится.
- **Терракота `#C4714F` значит «взгляни», и только это.** Точка «телеграм
  подключён» зелёная `#4E9A5A`.
- **Звёздочка Anthropic не используется.** Знак свой — кассовая лента,
  `<symbol id="mark">` в шаблоне.
- **Тёмной темы нет.** Первая версия светлая.
- **Код и имена файлов латиницей**, тексты, докстринги и комментарии русские —
  и в питоне, и в JS.
- **Автотестов нет.** Проверка живьём плюс `tools/selfcheck.py` на голых
  `assert` для чистых функций. Ни pytest, ни папки `tests/`.
- **Код — учебный материал.** Простые функции, русские докстринги, никаких
  дата-классов, аннотаций типов и абстракций сверх необходимого. Читателем
  будет финансист, впервые открывший питон.
- **Наследие среза 1, которое нельзя нарушить:** `--agent` не используется
  никогда; `--json-schema` принимает текст схемы, а не путь; фото — `sonnet`,
  текст — `haiku`; пустой `structured_output` — отказ; таймаут 180 секунд и одна
  повторная попытка; `subprocess` только списком аргументов; `allow_redirects`
  в запросах к Apps Script не отключать; статьи агенту придумывать нельзя.

## Раскладка файлов

```
чеки-бот/
  feed.py         задача 2 — лента: журнал событий в памяти процесса
  intake.py       задача 3 — единый приём: разбор, проверки, таблица, события
  web.py          задачи 1, 4, 5 — Flask: страница и три маршрута
  static/app.js   задачи 1, 6, 7, 8, 9 — клиент: опрос, отрисовка, отправка

  bot.py          задачи 1, 3, 10 — телеграм становится потоком; поток разбора
  config.py       задачи 3, 10 — owner, SHEET_LINK, необязательный BOT_TOKEN
  state.py        задача 3 — seen становится словарём, save под замком
  sheet.py        задача 3 — deliver отдаёт номер строки, а не «да/нет»
  agent.py        задача 9 — TITLES: название движка для сайдбара
  tools/selfcheck.py  задачи 2, 3 — проверки чистых функций

  templates/workspace.html  задача 1 — снять демо, подключить статику, id-крючки
  static/style.css          не трогаем
  design/workspace.html     эталон, не трогаем
  checks.py                 не трогаем
  engines/                  не трогаем
  sheet.gs                  не трогаем
```

**Почему `feed.py`, `intake.py` и `web.py` — три файла, а не один.** `feed.py`
не знает ни про телеграм, ни про Flask: это список словарей с номерами, его
целиком видно на экране. `intake.py` — то самое место, где живут проверки,
и его на уроке разбирают отдельно; смешать его с маршрутами Flask значит
спрятать суть за обвязкой. `web.py` — маршруты и ничего больше. Та же логика,
по которой в срезе 1 разъехались `agent.py`, `checks.py` и `sheet.py`.

**Где лежит лента.** В памяти процесса. Спека (раздел «Лента переживает
перезапуск») разрешает хранить её в `state.json` — последние пятьдесят
событий, не больше, — но **в срез 2 это не входит прямым текстом спеки**, и
мы не тащим. Значит перезапустили бота — лента пуста, а записи никуда не
делись: они в таблице.

Одно требование из этого следует и здесь обязательно: пока лента не
переживает перезапуск, пустой сайдбар обязан говорить «с момента запуска
ничего не записано», а не «ничего не записано». Задача 9, шаг 7.

---

### Задача 1: Flask поднимает рабочее место

Страница открывается по адресу, стили и скрипт грузятся, телеграм по-прежнему
принимает чеки. Ленты ещё нет — есть пустое начало.

**Файлы:**
- Создать: `web.py`, `static/app.js`
- Изменить: `templates/workspace.html` (пути к статике, снять демо-навигацию и
  демо-скрипт, очистить выдуманные данные, добавить id-крючки, скрыть три
  пункта сайдбара)
- Изменить: `bot.py:222-249` — `main()` поднимает Flask, телеграм уезжает в поток
- Проверка: `.venv/bin/python bot.py`, затем браузер

**Стыки:**
- Берёт: `config.load_env()`, `config.load_settings()` — как есть.
- Отдаёт: `web.create(env, st, jobs, blocked="")` → приложение Flask;
  `web.choose_port()` → число; `bot.telegram_loop(env, st, jobs)` → бесконечный
  цикл опроса. В этой задаче `jobs` ещё не используется — передаётся `None`,
  очередь появится в задаче 3.

- [ ] **Шаг 1: Поставить Flask**

```bash
.venv/bin/pip install --quiet flask
.venv/bin/python -c "import flask; print('flask', flask.__version__)"
```

Ожидание: `flask 3.x.x`.

- [ ] **Шаг 2: Написать `web.py`**

```python
"""Рабочее место в браузере — второй клиент бота, наравне с телеграмом.

Здесь только маршруты. Разбор живёт в intake.py, лента — в feed.py: страница
ничего не решает сама, она показывает то же, что бот отвечает в телеграм.

Слушаем строго 127.0.0.1. Пароля у страницы нет, а показывает она токен бота
и секрет таблицы — доступ ограничен ровно тем, что адрес локальный.
"""

import logging
import socket
import sys

from flask import Flask, jsonify, render_template

FIRST_PORT = 8765
LAST_PORT = 8775


def create(env, st, jobs, blocked=""):
    """Приложение Flask. env и st — те же словари, с которыми живёт бот."""
    app = Flask(__name__)

    @app.get("/")
    def page():
        return render_template("workspace.html")

    return app


def choose_port():
    """Первый свободный порт из 8765…8775.

    Занятый порт — обычное дело: бот уже запущен в соседнем окне терминала.
    Падать из-за этого незачем, но и молча уезжать на другой адрес нельзя —
    поэтому выбранный адрес печатается в терминал."""
    for port in range(FIRST_PORT, LAST_PORT + 1):
        probe = socket.socket()
        try:
            probe.bind(("127.0.0.1", port))
            return port
        except OSError:
            continue
        finally:
            probe.close()
    sys.exit(f"Порты {FIRST_PORT}…{LAST_PORT} заняты — закройте лишнее "
             "и запустите бота заново.")


def quiet():
    """Flask печатает строку на каждый запрос, а страница опрашивает ленту
    раз в три секунды. В терминале должно быть видно бота, а не журнал
    веб-сервера."""
    logging.getLogger("werkzeug").setLevel(logging.ERROR)
```

- [ ] **Шаг 3: Починить пути к статике в `templates/workspace.html`**

В макете стили подключались относительным путём — под Flask он не работает.
Заодно подключаем скрипт и убираем строчку про «данные выдуманные».

Было (строка 7):

```html
<link rel="stylesheet" href="../static/style.css">
```

Стало:

```html
<link rel="stylesheet" href="/static/style.css">
```

Перед `</body>` (вместо всего блока `<script> … </script>` с демо-логикой):

```html
<script src="/static/app.js"></script>
```

- [ ] **Шаг 4: Убрать демо-навигацию из `templates/workspace.html`**

Целиком удалить блок:

```html
<nav class="demo" aria-label="Навигация по макету">
  …
</nav>
```

Это переключалка состояний макета: она показывала панели, которых в срезе 2
ещё нет, и вела на `setup.html` из среза 5.

- [ ] **Шаг 5: Очистить выдуманные данные в `templates/workspace.html`**

Лента и «Записи сегодня» наполняются скриптом. В разметке остаются пустые
ёмкости.

`#p-feed`: всё содержимое `<div class="col">` удалить, оставить

```html
<div class="flow">
  <div class="col"></div>
</div>
```

`#today`: все пять кнопок удалить, оставить

```html
<div id="today"></div>
```

- [ ] **Шаг 6: Добавить id-крючки в `templates/workspace.html`**

Скрипту нужны точки, куда писать. Позиционные селекторы вроде
`.nv:nth-child(3)` в учебном коде запрещены: одна вставка в разметку — и
скрипт пишет не туда.

Приветствие в `#p-empty`:

```html
<h1 id="greeting">Что записываем?</h1>
```

Пункт «Таблица» (он единственный `<a>` среди пунктов):

```html
<a class="nv" id="nav-table" target="_blank" rel="noopener">
  <svg class="ic"><use href="#i-table"/></svg>Таблица<span class="r"></span>
</a>
```

Три пункта настроек получают id и `hidden` — они оживут в срезе 4:

```html
<button class="nv" type="button" id="nav-people" data-pane="people" hidden>
  <svg class="ic"><use href="#i-users"/></svg>Кто может писать<span class="r"></span>
</button>

<button class="nv" type="button" id="nav-telegram" data-pane="telegram" hidden>
  <svg class="ic"><use href="#i-send"/></svg>Телеграм<span class="dot off"></span>
</button>

<button class="nv" type="button" id="nav-engine" data-pane="engine" hidden>
  <svg class="ic"><use href="#i-cpu"/></svg>Движок<span class="r"></span>
</button>
```

Кнопка движка в обоих полях ввода (в `#p-feed` и в `#p-empty`) — подпись, а не
переключатель: переключение движка это срез 4.

```html
<button class="rt engine-badge" type="button" disabled></button>
```

Скрытое поле выбора файла — по одному в каждом поле ввода не нужно, хватит
одного на страницу. Перед `</main>`:

```html
<input id="pick" type="file" accept="image/jpeg,image/png" hidden>
```

- [ ] **Шаг 7: Написать `static/app.js` — пока только переключение панелей**

Логика демо-скрипта, переехавшая в свой файл. Всё остальное — задачи 6–9.

```javascript
/* Рабочее место: клиент.

   Никаких библиотек и сборщиков: один файл, обычный JS. Разметка приезжает
   пустым скелетом, всё содержимое — из /api/events. Первый экран сервер не
   рисует намеренно: иначе каждое сообщение собиралось бы в двух местах, и эти
   два места разъехались бы через месяц. */

(function () {
  var app = document.getElementById("app");
  var panes = document.querySelectorAll(".pane");
  var navs = document.querySelectorAll(".nv[data-pane]");

  /* Раздел сайдбара подменяет ленту, а не открывается окном поверх — так же
     устроено приложение Claude, с которого списан вид. */
  function show(name) {
    panes.forEach(function (pane) { pane.hidden = pane.id !== "p-" + name; });
    var lit = (name === "empty") ? "feed" : name;
    navs.forEach(function (nav) {
      nav.setAttribute("aria-current", String(nav.dataset.pane === lit));
    });
    app.classList.remove("menu-open");
  }

  document.querySelectorAll("[data-pane]").forEach(function (node) {
    node.addEventListener("click", function () { show(node.dataset.pane); });
  });

  var burger = document.getElementById("burger");
  burger.addEventListener("click", function () {
    var open = app.classList.toggle("menu-open");
    burger.setAttribute("aria-expanded", String(open));
  });

  /* Пока лента пуста, показываем приглашение, а не пустое поле. */
  show("empty");
})();
```

- [ ] **Шаг 8: Переписать `main()` и вынести опрос телеграма в `bot.py`**

Главный поток отдан Flask: он и живёт, пока живёт бот. Телеграм уезжает в
поток-демон — так его можно будет не запускать вовсе, когда токена нет
(задача 10).

Заменить весь `def main():` в `bot.py` на:

```python
def telegram_loop(env, st, jobs):
    """Опрос телеграма. Своим потоком, потому что главный занят страницей."""
    print("Телеграм: жду сообщений.")
    while True:
        try:
            updates = get_updates(env["bot_token"], st["offset"])
        except (requests.RequestException, RuntimeError, ValueError) as error:
            # Телеграм не отвечает — ждём и повторяем, offset не двигаем.
            print(f"телеграм не отвечает ({error}), жду пять секунд")
            time.sleep(5)
            continue
        for update in updates:
            st["offset"] = update["update_id"] + 1
            message = update.get("message")
            if message:
                try:
                    handle(env, st, message, "телеграм", who(message))
                except Exception as error:
                    # Одно сломанное сообщение не должно останавливать бота:
                    # offset уже сдвинут, следующее разберётся.
                    print(f"сообщение не обработалось: {error}")
            memory.save(st)


def main():
    env = config.load_env()
    config.refuse_if_api_key()
    st = memory.load()
    jobs = None  # очередь заданий появится в задаче 3

    threading.Thread(target=telegram_loop, args=(env, st, jobs),
                     daemon=True).start()

    web.quiet()
    port = web.choose_port()
    print(f"Рабочее место: http://127.0.0.1:{port}")
    print("Ctrl+C — выход.")
    try:
        web.create(env, st, jobs).run(host="127.0.0.1", port=port,
                                      threaded=True, use_reloader=False)
    except KeyboardInterrupt:
        print("\nОстанавливаюсь.")
```

В шапку `bot.py` добавить импорты:

```python
import threading

import web
```

- [ ] **Шаг 9: Проверить руками**

```bash
.venv/bin/python bot.py
```

Ожидание в терминале: `Рабочее место: http://127.0.0.1:8765`, следом
`Телеграм: жду сообщений.` Строк веб-сервера на каждый запрос быть не должно.

Во втором окне терминала:

```bash
curl -s -o /dev/null -w "страница %{http_code}\n" http://127.0.0.1:8765/
curl -s -o /dev/null -w "стили %{http_code}\n" http://127.0.0.1:8765/static/style.css
curl -s -o /dev/null -w "скрипт %{http_code}\n" http://127.0.0.1:8765/static/app.js
```

Ожидание: `страница 200`, `стили 200`, `скрипт 200`.

Открыть `http://127.0.0.1:8765` в браузере. Ожидание: тёплый айвори, сайдбар
с логотипом-лентой, приветствие «Что записываем?», поле ввода, подсказка
«Фотографию чека можно просто перетащить в окно». В сайдбаре видны только
«Новый расход» и «Таблица»; «Записи сегодня» пусты. Сравнить с
`design/workspace.html`, экран «Пустое начало», — отличаться должны только
данные.

Прислать боту фотографию чека в телеграм. Ожидание: как в срезе 1 — «Смотрю
чек…», потом «Записал», строка в таблице.

- [ ] **Шаг 10: Проверить занятый порт**

Не закрывая первого бота, запустить второго в другом окне:

```bash
.venv/bin/python bot.py
```

Ожидание: `Рабочее место: http://127.0.0.1:8766`. Закрыть второго.

- [ ] **Шаг 11: Коммит**

`templates/` и `static/` до сих пор не в гите — их положила сессия, которая
закончилась, не закоммитив (эталон `design/workspace.html` уже в гите,
коммит `bb52676`). Первый коммит среза забирает их вместе со своим кодом:
`templates/setup.html` — макет мастера для среза 5, он понадобится там.

```bash
git add web.py static/app.js static/style.css bot.py
git add templates/workspace.html templates/setup.html
git commit -m "feat: рабочее место открывается в браузере, телеграм уехал в поток"
```

---

### Задача 2: Лента событий в памяти

Журнал, из которого страница узнаёт, что произошло. Пока в него никто не
пишет — маршрут отдаёт пустой список, и это уже проверяемо.

**Файлы:**
- Создать: `feed.py`
- Изменить: `web.py` — маршрут `/api/events`
- Изменить: `tools/selfcheck.py` — проверки ленты
- Проверка: `curl`, `.venv/bin/python tools/selfcheck.py`

**Стыки:**
- Берёт: ничего, `feed.py` ни от чего не зависит.
- Отдаёт: `feed.add(event)` → то же событие со `id` и `at`;
  `feed.since(after)` → `(список событий, номер последнего)`;
  `feed.LIMIT` — сколько событий помним.
  Маршрут `GET /api/events?after=N` → `{"events": [...], "last": N, "state": {...}}`.

- [ ] **Шаг 1: Написать `feed.py`**

```python
"""Лента рабочего места — журнал событий в памяти процесса.

В state.json ленты нет намеренно: спека перечисляет содержимое того файла
поимённо, и событий там не значится. Перезапустили бота — лента пуста, а
записи никуда не делись: они в таблице, за тем таблица и нужна.

Пишут сюда из двух потоков — телеграмного и веб-запросов, — поэтому номер
события выдаётся под замком. Без него два события получили бы один номер, и
страница одно из них не показала бы.
"""

import threading
import time
from datetime import date, datetime

# Лента не растёт бесконечно: она нужна, чтобы видеть сегодняшнюю работу,
# а не чтобы помнить всё за год.
LIMIT = 300

_LOCK = threading.Lock()
_EVENTS = []
_NEXT = 1


def add(event):
    """Кладёт событие в ленту и возвращает его же — уже с номером и временем.

    Номер нужен странице: по нему она спрашивает «что нового после N-го»."""
    global _NEXT
    with _LOCK:
        stamped = dict(event, id=_NEXT, at=time.time())
        _NEXT += 1
        _EVENTS.append(stamped)
        del _EVENTS[:-LIMIT]
        return stamped


def since(after):
    """Всё, что появилось после события с номером after.

    Возвращает (события, номер последнего). Если after больше последнего
    номера, значит бота перезапустили и нумерация пошла заново — отдаём
    журнал целиком, иначе страница будет ждать событий, которых уже не
    будет."""
    with _LOCK:
        newest = _EVENTS[-1]["id"] if _EVENTS else 0
        if after > newest:
            after = 0
        fresh = [event for event in _EVENTS if event["id"] > after]
        return fresh, newest


def forget():
    """Забыть всё. Нужно только самопроверке — бот этого не делает."""
    global _NEXT
    with _LOCK:
        _EVENTS.clear()
        _NEXT = 1
```

- [ ] **Шаг 2: Добавить маршрут `/api/events` в `web.py`**

Состояние сайдбара прикладывается к каждому ответу опроса, а не живёт своим
маршрутом: один запрос вместо двух, и сайдбар не может разъехаться с лентой.
Наполнится `snapshot()` в задаче 9 — пока отдаёт пустое.

В `web.py` добавить импорт:

```python
from flask import Flask, jsonify, render_template, request

import feed
```

И внутрь `create()`, после маршрута `page`:

```python
    @app.get("/api/events")
    def events():
        """Что нового в ленте после события с номером after.

        Страница зовёт этот маршрут раз в три секунды. Вебсокетов нет
        намеренно: на локальном адресе опрос дешевле и понятнее на уроке."""
        after = request.args.get("after", default=0, type=int)
        fresh, last = feed.since(after)
        return jsonify(events=fresh, last=last, state=snapshot(env, st, blocked))
```

И рядом с `choose_port()`, на уровне модуля:

```python
def snapshot(env, st, blocked):
    """Состояние для сайдбара. Наполнится в задаче 9."""
    return {}
```

- [ ] **Шаг 3: Дописать проверки ленты в `tools/selfcheck.py`**

В блок импортов наверху файла добавить `import feed`. Перед блоком `print("проверки без модели")` добавить:

```python
print()
print("лента")

feed.forget()
first = feed.add({"kind": "слово", "text": "раз"})
second = feed.add({"kind": "слово", "text": "два"})
check("номера растут", first["id"] == 1 and second["id"] == 2)
check("время проставляется", first["at"] > 0)

fresh, last = feed.since(0)
check("с нуля отдаётся всё", len(fresh) == 2 and last == 2)

fresh, last = feed.since(1)
check("после первого — только второе",
      len(fresh) == 1 and fresh[0]["text"] == "два")

fresh, last = feed.since(2)
check("после последнего — пусто, номер прежний", fresh == [] and last == 2)

fresh, last = feed.since(99)
check("номер из прошлой жизни бота — отдаём журнал целиком", len(fresh) == 2)

feed.forget()
for number in range(feed.LIMIT + 50):
    feed.add({"kind": "слово", "text": str(number)})
fresh, last = feed.since(0)
check("лента не растёт бесконечно", len(fresh) == feed.LIMIT)
check("выкидывается старое, а не новое", fresh[-1]["text"] == str(feed.LIMIT + 49))

feed.forget()
```

- [ ] **Шаг 4: Прогнать самопроверку**

```bash
.venv/bin/python tools/selfcheck.py
```

Ожидание: раздел «лента», семь галочек, в конце `проверок пройдено: 34`.

- [ ] **Шаг 5: Проверить маршрут**

```bash
.venv/bin/python bot.py
```

Во втором окне:

```bash
curl -s "http://127.0.0.1:8765/api/events?after=0"
```

Ожидание: `{"events":[],"last":0,"state":{}}`.

- [ ] **Шаг 6: Коммит**

```bash
git add feed.py web.py tools/selfcheck.py
git commit -m "feat: лента событий в памяти и маршрут опроса"
```

---

### Задача 3: Единый приём и поток разбора

Ядро среза. Разбор уезжает из телеграмного потока в общий, и оба канала зовут
одну функцию. Телеграмные записи начинают появляться в ленте браузера.

Задача крупная потому, что разъять её нечем: вынести `handle()` в `intake.py`
и не завести очередь — значит написать телеграмную ветку дважды, второй раз
выбросив первую.

**Файлы:**
- Создать: `intake.py`
- Изменить: `bot.py` — `handle()` распадается на `admit()` и поток `worker()`;
  `download_photo()`, `move()`, `safe()`, папки чеков переезжают в `intake.py`;
  появляется `as_text()`
- Изменить: `state.py` — `seen` становится словарём, `save()` под замком
- Изменить: `sheet.py:deliver` — отдаёт номер строки, а не «да/нет»
- Изменить: `config.py` — `owner` в настройках по умолчанию
- Изменить: `settings.example.json` — строка `owner`
- Изменить: `tools/selfcheck.py` — проверки словаря разобранных файлов
- Проверка: фотография в телеграм, `curl /api/events`

**Стыки:**
- Берёт: `feed.add()` из задачи 2; `agent.parse()`, `checks.review()`,
  `sheet.categories()`, `sheet.flush()` — как есть.
- Отдаёт:
  - `intake.accept(env, st, job)` → список событий (уже подписанных),
  - `intake.duplicate(st, mark)` → `{"row": 47, "at": 1756800000.0}` или `None`,
  - `intake.fingerprint(data)` → строка sha256,
  - `intake.save_photo(data, author, suffix=".jpg")` → `Path`,
  - `intake.sign(event, job)` → событие с `author`/`channel`/`task`,
  - `intake.when(old)` → «такой же файл приходил сегодня в 11:20»,
  - `intake.INBOX`, `intake.DONE`, `intake.DOUBTFUL`, `intake.move()`, `intake.safe()`,
  - `bot.as_text(event)` → строка для телеграма,
  - `bot.worker(env, st, jobs)` → бесконечный цикл разбора,
  - `bot.admit(env, st, jobs, message)` → кладёт задание в очередь,
  - `memory.seen(st, mark)` → словарь или `None`,
  - `memory.remember(st, mark, row)` → `None`,
  - `sheet.deliver(...)` → номер строки или `None`.
- **Задание** — словарь, который кладут в очередь оба канала:
  `{"kind": "фото"|"текст", "payload": путь или текст, "channel": "браузер"|"телеграм",
    "author": "Мария", "chat_id": 123 или None, "file": "чек.jpg" или "",
    "mark": отпечаток или "", "task": "a1b2c3d4" или ""}`.
- **Событие** — словарь ленты, четыре вида:
  - `{"kind": "мой", "text": ...}` или `{"kind": "мой", "file": ...}` — реплика
    человека; рождается только в браузере (задачи 4 и 5),
  - `{"kind": "работа", "task": ...}` — плашка «Смотрю чек…»; только в браузере,
  - `{"kind": "запись", "merchant", "amount", "currency", "category", "payment",
    "date", "row", "status", "reasons", "warnings", "reply", "file"}` — расход,
  - `{"kind": "слово", "text", "note", "row"}` — фраза бота: отказ или пояснение.
  У чужих событий сверх этого есть `author` и `channel`; у своих их нет вовсе.

- [ ] **Шаг 1: Переделать `seen` в словарь в `state.py`**

Ленте нужно не «этот файл уже был», а «уже записан, строка 47». Значит рядом с
отпечатком надо хранить номер строки и время.

В `state.py` заменить константу и три функции:

```python
import threading
import time

EMPTY = {
    "offset": 0,
    "seen": {},
    "categories": [],
    "categories_at": 0,
    "queue": [],
    "refused": [],
}

# Разобранные файлы не должны копиться бесконечно: они нужны, чтобы поймать
# чек, присланный дважды подряд, а не чтобы помнить всё за год.
SEEN_LIMIT = 500

# Пишут в файл два потока — телеграмный и поток разбора. Без замка они
# затрут запись друг друга на середине.
_LOCK = threading.Lock()
```

```python
def load():
    """Память бота. Файла нет или он испорчен — начинаем с чистой заготовки."""
    state = json.loads(json.dumps(EMPTY))
    try:
        data = json.loads(STATE_PATH.read_text(encoding="utf-8"))
        state.update(data)
    except (FileNotFoundError, json.JSONDecodeError, TypeError, ValueError):
        return json.loads(json.dumps(EMPTY))
    if not isinstance(state["seen"], dict):
        # Файл от прежней версии бота: там был список отпечатков без номеров
        # строк. Разбирать его незачем — потеря невелика, а формат чистый.
        state["seen"] = {}
    return state


def save(state):
    """Пишем через временный файл: Ctrl+C посреди записи не оставит огрызок,
    из которого бот потом не поднимется."""
    with _LOCK:
        tmp = STATE_PATH.with_name("state.json.tmp")
        tmp.write_text(json.dumps(state, ensure_ascii=False, indent=2),
                       encoding="utf-8")
        tmp.replace(STATE_PATH)


def seen(state, mark):
    """Этот файл уже разбирали? Отдаёт запомненное — номер строки и время —
    или None. Отпечаток у телеграма file_unique_id, у браузера хеш файла."""
    return state["seen"].get(mark)


def remember(state, mark, row):
    """Запомнить разобранный файл. row может быть None: строка ушла в очередь
    и номера у неё пока нет — но разбирать этот чек второй раз всё равно не надо."""
    state["seen"][mark] = {"row": row, "at": time.time()}
    extra = len(state["seen"]) - SEEN_LIMIT
    for old in list(state["seen"])[:extra]:
        del state["seen"][old]
```

- [ ] **Шаг 2: Научить `sheet.deliver` отдавать номер строки**

Лента показывает «строка 47» — значит номер нужен наверху, а не только внутри.

В `sheet.py` заменить `deliver()`:

```python
def deliver(state, url, secret, row):
    """Пишет строку и возвращает её номер. Не вышло — кладёт в очередь и
    возвращает None: разобрали, но в таблицу пока не попало."""
    try:
        return append_row(url, secret, row)
    except (SheetError, requests.RequestException) as error:
        print(f"таблица не приняла строку ({error}) — отложил в очередь")
        state["queue"].append(row)
        return None
```

- [ ] **Шаг 3: Добавить `owner` в настройки**

Имя владельца нужно приветствию («Что записываем, Леонид?») и колонке «кто
прислал» для записей из браузера.

В `config.py`:

```python
DEFAULTS = {"engine": "claude_code", "allowed": [], "knocked": [], "owner": ""}
```

В `settings.example.json`:

```json
{
  "engine": "claude_code",
  "owner": "Ваше имя",
  "allowed": [
    {"id": 123456789, "username": "ваш-ник", "name": "Ваше имя"}
  ],
  "knocked": []
}
```

То же самое дописать в свой `settings.json` (он не под гитом).

- [ ] **Шаг 4: Написать `intake.py`**

```python
"""Единый вход разбора. Телеграм и браузер зовут отсюда одно и то же.

Это и есть то место, ради которого затевалось рабочее место: разбор, проверки
без модели, запись в таблицу и раскладка файлов живут здесь, и обойти их
нельзя ни одним из двух путей. Каналы отличаются только тем, как человек
прислал сообщение и как ему ответить, — а не тем, что с сообщением сделают.

Ответа отсюда никто не печатает: функция возвращает события. Словами их
подаёт bot.py — в чат, словарями web.py — в браузер. Одно событие, две подачи.
"""

import hashlib
from datetime import date, datetime
from pathlib import Path

import agent
import checks
import config
import sheet
import state as memory

BASE_DIR = Path(__file__).resolve().parent
INBOX = BASE_DIR / "чеки" / "входящие"
DONE = BASE_DIR / "чеки" / "готово"
DOUBTFUL = BASE_DIR / "чеки" / "спорные"


def accept(env, st, job):
    """Путь сообщения от входа до строки в таблице. Возвращает события ленты."""
    settings = config.load_settings()
    events = []

    # Отложенные строки уезжают при первой возможности — раньше, чем новая.
    delivered = sheet.flush(st, env["sheet_url"], env["sheet_secret"])
    if delivered:
        events.append({"kind": "слово",
                       "text": f"Отложенные строки доехали до таблицы: {delivered}."})

    categories, source = sheet.categories(st, env["sheet_url"], env["sheet_secret"])
    if not categories:
        # Придумывать статьи агенту нельзя: в отчёте заведутся «Продукты»,
        # «Продукты питания» и «Еда» вместо одной строки.
        events.append({
            "kind": "слово",
            "text": "Не вижу справочник статей в таблице — разбирать не буду.",
            "note": "Иначе в отчёте заведутся выдуманные статьи. "
                    "Проверьте лист «Статьи».",
        })
        return [sign(event, job) for event in events]
    if source == "запас":
        events.append({
            "kind": "слово",
            "text": "Таблица не отдала справочник, работаю по последнему "
                    "известному списку статей.",
        })

    today = date.today()
    try:
        answer = agent.parse(job["kind"], job["payload"], categories, today,
                             settings["engine"])
    except agent.AgentError as error:
        # Файл остаётся во входящих: сам он не пересмотрится, второй раз
        # чек присылает человек.
        print(f"агент не справился: {error}")
        events.append({"kind": "слово", "text": "Не смог разобрать.",
                       "note": "Попробуйте ещё раз или напишите текстом."})
        return [sign(event, job) for event in events]

    path = Path(job["payload"]) if job["kind"] == "фото" else None

    if answer["intent"] == "правка":
        # Правка последней записи появится в срезе 3. Пока честно говорим,
        # что не умеем, и ничего не пишем: молча проглотить исправление
        # хуже, чем отказать.
        if path:
            move(path, DOUBTFUL)
        events.append({"kind": "слово", "text": "Правки пока не умею.",
                       "note": "Пришлите трату заново, целиком."})
        return [sign(event, job) for event in events]

    if answer["intent"] == "не расход":
        # В таблицу не пишем вообще. Файл не помечаем разобранным: если это
        # было меню, человек может прислать настоящий чек тем же файлом.
        if path:
            move(path, DOUBTFUL)
        events.append({"kind": "слово", "text": answer["reply"]})
        return [sign(event, job) for event in events]

    verdict = checks.review(answer, categories, today)
    if path:
        path = move(path, DONE if verdict["status"] == "готово" else DOUBTFUL)

    amount = answer["amount"] if checks.is_number(answer["amount"]) else None
    row = {
        "date": answer["date"],
        "amount": amount if amount is not None else "",
        "currency": answer["currency"],
        "merchant": answer["merchant"],
        "category": verdict["category"],
        "payment": answer["payment"],
        "source": "фото чека" if job["kind"] == "фото" else "текст",
        # Канала в таблице нет намеренно: иначе сводная по людям разложит
        # одного человека на две строки.
        "who": job["author"],
        "status": verdict["status"],
        "file": path.name if path else "",
    }
    number = sheet.deliver(st, env["sheet_url"], env["sheet_secret"], row)
    if job.get("mark"):
        memory.remember(st, job["mark"], number)

    events.append({
        "kind": "запись",
        "merchant": answer["merchant"],
        "amount": amount,
        "currency": answer["currency"],
        "category": verdict["category"],
        "payment": answer["payment"],
        "date": answer["date"],
        "row": number,
        "status": verdict["status"],
        "reasons": verdict["reasons"],
        "warnings": verdict["warnings"],
        # Фраза агента нужна телеграму: там ответ это текст. Страница её не
        # берёт — она собирает строку из полей сама, иначе суммы в сайдбаре
        # не встанут колонкой.
        "reply": answer["reply"],
        "file": path.name if path else "",
    })
    return [sign(event, job) for event in events]


def sign(event, job):
    """Подпись под событием.

    Чужое подписывается «Мария · телеграм» — человек впереди способа: значимо,
    кто потратил, а не с какого устройства прислал. Своё, сделанное здесь же,
    не подписывается вовсе: подпись означает «это не вы»."""
    if job["channel"] != "браузер":
        event = dict(event, author=job["author"], channel=job["channel"])
    if job.get("task"):
        # По номеру задачи страница уберёт плашку «Смотрю чек…».
        event = dict(event, task=job["task"])
    return event


def fingerprint(data):
    """Отпечаток файла. В телеграме такой даёт сам Bot API — file_unique_id,
    в браузере его нет, поэтому считаем хеш содержимого."""
    return hashlib.sha256(data).hexdigest()


def duplicate(st, mark):
    """Этот чек уже разбирали? Отдаёт запомненное или None."""
    return memory.seen(st, mark)


def when(old):
    """«такой же файл приходил сегодня в 11:20» — чтобы человек понял, о чём
    речь, а не гадал, когда это было."""
    moment = datetime.fromtimestamp(old.get("at") or 0)
    when_day = "сегодня" if moment.date() == date.today() else moment.strftime("%d.%m")
    return f"такой же файл приходил {when_day} в {moment:%H:%M}"


def safe(name):
    """Имя в имя файла: буквы и цифры оставляем, остальное — подчёркивание."""
    cleaned = "".join(ch if ch.isalnum() else "_" for ch in name).strip("_")
    return cleaned or "кто-то"


def save_photo(data, author, suffix=".jpg"):
    """Кладёт фотографию в чеки/входящие/ и отдаёт путь.

    Имя одно на оба канала: дата, время и кто прислал."""
    INBOX.mkdir(parents=True, exist_ok=True)
    stamp = datetime.now().strftime("%Y-%m-%d_%H%M%S")
    path = INBOX / f"{stamp}_{safe(author)}{suffix}"
    path.write_bytes(data)
    return path


def move(path, folder):
    """Файл едет в готово/ только при статусе «готово», иначе в спорные/."""
    folder.mkdir(parents=True, exist_ok=True)
    target = folder / path.name
    path.replace(target)
    return target
```

- [ ] **Шаг 5: Переписать телеграмную часть `bot.py`**

Из `bot.py` **удалить** целиком: константы `INBOX`, `DONE`, `DOUBTFUL`,
функции `safe()`, `move()`, `reply_text()`, `handle()`. Они переехали в
`intake.py` или заменены.

Импорты в шапке:

```python
import json
import queue
import threading
import time

import requests

import config
import feed
import intake
import state as memory
import web
```

`agent`, `checks`, `sheet` и `datetime` из `bot.py` уходят: телеграм больше не разбирает
сам, он только принимает.

`download_photo()` переписать на общую раскладку файлов:

```python
def download_photo(token, photo, sender):
    """Качаем самый крупный размер в чеки/входящие/ и возвращаем путь."""
    info = call(token, "getFile", file_id=photo["file_id"])
    url = f"https://api.telegram.org/file/bot{token}/{info['file_path']}"
    response = requests.get(url, timeout=60)
    response.raise_for_status()
    return intake.save_photo(response.content, sender)
```

`extract()` — добавить голосовые. Bot API расшифровку не отдаёт, и это
окончательно; молчать об этом хуже, чем отказать:

```python
def extract(message):
    """Любой вход сводится к паре «что разбирать» и «откуда пришло».

    Веток три: фотография, текст и голосовое. Голосовые в проекте не
    разбираются — Claude не принимает звук, а Bot API расшифровки не даёт."""
    if message.get("photo"):
        # Подпись под фотографией в первой версии не читается: чек говорит
        # сам за себя, а спорить с ним подписью — отдельная история.
        largest = max(message["photo"],
                      key=lambda size: size.get("file_size")
                      or size.get("width", 0) * size.get("height", 0))
        return {"kind": "фото", "photo": largest}
    if message.get("voice") or message.get("audio"):
        return {"kind": "голос"}
    text = (message.get("text") or "").strip()
    if text:
        return {"kind": "текст", "text": text}
    return None
```

Вместо `handle()` — `admit()`: от сообщения до задания в очереди.

```python
def admit(env, st, jobs, message):
    """От телеграмного сообщения до задания на разбор.

    Здесь только приём: белый список, скачивание, дубликат. Разбор — общий,
    он в intake.accept(), и зовёт его поток разбора."""
    settings = config.load_settings()
    chat_id = message["chat"]["id"]
    author = who(message)
    if message.get("from", {}).get("id") not in config.allowed_ids(settings):
        knock(env, st, message)
        return

    incoming = extract(message)
    if incoming is None:
        say(env["bot_token"], chat_id,
            "Пришлите фотографию чека или напишите тратой: что, сколько и чем "
            "платили. Например: обед 850 картой")
        return

    if incoming["kind"] == "голос":
        tell(env, chat_id, {"kind": "слово", "text": "Голосовые не умею.",
                            "note": "напишите текстом или пришлите фото чека",
                            "author": author, "channel": "телеграм"})
        return

    job = {"kind": incoming["kind"], "payload": "", "channel": "телеграм",
           "author": author, "chat_id": chat_id, "file": "", "mark": "", "task": ""}

    if incoming["kind"] == "фото":
        mark = incoming["photo"]["file_unique_id"]
        old = intake.duplicate(st, mark)
        if old:
            tell(env, chat_id, {"kind": "слово", "text": "Этот чек уже записан.",
                                "row": old.get("row"), "note": intake.when(old),
                                "author": author, "channel": "телеграм"})
            return
        try:
            path = download_photo(env["bot_token"], incoming["photo"], author)
        except (requests.RequestException, RuntimeError) as error:
            print(f"не скачал фотографию: {error}")
            say(env["bot_token"], chat_id,
                "Не смог скачать фотографию, пришлите ещё раз.")
            return
        # Ответа ждать 15–50 секунд, человеку надо сказать, что мы живы.
        say(env["bot_token"], chat_id, "Смотрю чек…")
        job.update(payload=str(path), file=path.name, mark=mark)
    else:
        job.update(payload=incoming["text"])

    jobs.put(job)


def tell(env, chat_id, event):
    """Событие в ленту и то же самое словами в чат."""
    feed.add(event)
    say(env["bot_token"], chat_id, as_text(event))
```

- [ ] **Шаг 6: Написать телеграмную подачу `as_text()` в `bot.py`**

Событие одно, подачи две. Здесь — словами.

```python
def as_text(event):
    """Событие ленты словами, для телеграма.

    Страница из тех же полей собирает свою строку — с разрядами в сумме и
    колонкой цифр. В чате разрядов не будет, зато будет фраза агента."""
    if event["kind"] != "запись":
        line = event.get("text", "")
        if event.get("row"):
            line += f" — строка {event['row']}"
        note = event.get("note")
        return f"{line}\n{note}" if note else line

    if not event["row"]:
        head = "Разобрал, но в таблицу не попало — попробую позже"
    elif event["status"] == "готово":
        head = "Записал"
    else:
        head = "Записал с пометкой «проверить»"
    lines = [f"{head}: {event['reply']}"]
    if event["row"]:
        lines[0] += f" — строка {event['row']}"
    lines += ["— " + reason for reason in event["reasons"]]
    lines += ["— " + warning for warning in event["warnings"]]
    return "\n".join(lines)
```

- [ ] **Шаг 7: Завести поток разбора в `bot.py`**

Один разбор за раз, и оба канала стоят в одной очереди. Так проще, чем замки
вокруг общей памяти, и честнее по деньгам: два вызова движка разом стоили бы
вдвое и шли бы вдвое дольше.

```python
def worker(env, st, jobs):
    """Поток разбора: берёт задания по одному и зовёт общий приём.

    Разбор идёт 15–50 секунд. Держать на нём телеграмный опрос или
    HTTP-запрос браузера нельзя — поэтому он живёт здесь, а ответ приезжает
    событием в ленту."""
    while True:
        job = jobs.get()
        try:
            events = intake.accept(env, st, job)
        except Exception as error:
            # Одно сорвавшееся задание не должно останавливать поток: иначе
            # бот замолчит навсегда, ничего об этом не сказав.
            print(f"разбор сорвался: {error}")
            events = [intake.sign({"kind": "слово",
                                   "text": "Что-то пошло не так — попробуйте ещё раз."},
                                  job)]
        for event in events:
            feed.add(event)
            if job["channel"] == "телеграм" and job.get("chat_id"):
                say(env["bot_token"], job["chat_id"], as_text(event))
        memory.save(st)
```

- [ ] **Шаг 8: Соединить в `main()`**

В `telegram_loop()` заменить вызов `handle(...)` на `admit(env, st, jobs, message)`.

В `main()` завести очередь и поток разбора:

```python
def main():
    env = config.load_env()
    config.refuse_if_api_key()
    st = memory.load()
    jobs = queue.Queue()

    threading.Thread(target=worker, args=(env, st, jobs), daemon=True).start()
    threading.Thread(target=telegram_loop, args=(env, st, jobs),
                     daemon=True).start()

    web.quiet()
    port = web.choose_port()
    print(f"Рабочее место: http://127.0.0.1:{port}")
    print("Ctrl+C — выход.")
    try:
        web.create(env, st, jobs).run(host="127.0.0.1", port=port,
                                      threaded=True, use_reloader=False)
    except KeyboardInterrupt:
        print("\nОстанавливаюсь.")
```

- [ ] **Шаг 9: Дописать проверки словаря разобранных файлов в `tools/selfcheck.py`**

В блоке «служебная память» заменить три строки про `remember`/`seen` на:

```python
    memory = state.load()
    state.remember(memory, "AgAD123", 47)
    check("файл запоминается вместе с номером строки",
          state.seen(memory, "AgAD123")["row"] == 47)
    check("чужой файл не считается разобранным",
          state.seen(memory, "AgAD999") is None)
    state.remember(memory, "AgAD777", None)
    check("строка без номера тоже запоминается",
          state.seen(memory, "AgAD777")["row"] is None)

    for number in range(600):
        state.remember(memory, f"file{number}", number)
    check("список разобранных не растёт бесконечно",
          len(memory["seen"]) == state.SEEN_LIMIT)
    check("выкидывается старое, а не новое",
          state.seen(memory, "file599") is not None)

    state.STATE_PATH.write_text('{"seen": ["старый", "формат"]}', encoding="utf-8")
    check("список отпечатков от прежней версии не роняет бота",
          state.load()["seen"] == {})
```

- [ ] **Шаг 10: Прогнать самопроверку**

```bash
.venv/bin/python tools/selfcheck.py
```

Ожидание: все галочки, `проверок пройдено: 37`.

- [ ] **Шаг 11: Проверить живьём**

```bash
.venv/bin/python bot.py
```

Прислать боту фотографию чека в телеграм. Ожидание в чате: «Смотрю чек…»,
затем «Записал: …  — строка N». В таблице строка. Во втором окне:

```bash
curl -s "http://127.0.0.1:8765/api/events?after=0" | .venv/bin/python -m json.tool
```

Ожидание: событие `"kind": "запись"` с полями `merchant`, `amount`, `row`,
`status`, и — обязательно — `"author"` с именем и `"channel": "телеграм"`.
Поля `task` быть не должно: задание пришло не из браузера.

Прислать ту же фотографию второй раз. Ожидание в чате: «Этот чек уже записан
— строка N», следом «такой же файл приходил сегодня в HH:MM».

Прислать голосовое. Ожидание: «Голосовые не умею.» и подсказка; в
`/api/events` — событие `"kind": "слово"` с подписью.

Написать боту «привет». Ожидание: отказ от агента, в таблице ничего.

- [ ] **Шаг 12: Коммит**

```bash
git add intake.py bot.py state.py sheet.py config.py settings.example.json tools/selfcheck.py
git commit -m "feat: единый приём для обоих каналов и поток разбора"
```

---

### Задача 4: Приём из браузера текстом

Второй вход открывается. Страницы ещё нет — проверяем `curl`'ом, и это
правильный порядок: сначала работает приём, потом появляется то, чем его
удобно звать.

**Файлы:**
- Изменить: `web.py` — маршрут `POST /api/say`, функции `start()`, `published()`
- Проверка: `curl -F "text=…"`

**Стыки:**
- Берёт: `feed.add()`, `intake.sign()` — из задач 2 и 3; очередь `jobs` из
  задачи 3.
- Отдаёт: `POST /api/say` с полем формы `text` →
  `{"events": [...], "last": N}`. Отправка возвращается сразу, с номером
  задачи внутри события «работа»; ответ приедет опросом через 15–50 секунд.
- Отдаёт: `web.start(jobs, job, mine)` → список из двух событий;
  `web.published(events)` → словарь ответа.

- [ ] **Шаг 1: Дописать `web.py`**

Импорты в шапку:

```python
from uuid import uuid4

import config
import feed
import intake
```

Рядом с `snapshot()`, на уровне модуля:

```python
def published(events):
    """Ответ страницы: что появилось в ленте прямо сейчас.

    Номер последнего события отдаётся вместе с ними, чтобы страница не
    показала эти же события второй раз следующим опросом."""
    return {"events": events, "last": events[-1]["id"] if events else 0}


def start(jobs, job, mine):
    """Кладёт задание в очередь и рождает два события: реплику человека и
    плашку «Смотрю чек…».

    Ответ придёт отдельным событием через 15–50 секунд — держать на нём
    HTTP-запрос нельзя, браузер оборвёт его раньше."""
    job["task"] = uuid4().hex[:8]
    born = [feed.add(mine), feed.add({"kind": "работа", "task": job["task"]})]
    jobs.put(job)
    return born


def owner_name(settings):
    """Как подписывать записи из браузера в колонке «кто прислал».

    Имени нет — «хозяин»: пустая клетка в таблице хуже, чем общее слово."""
    return (settings.get("owner") or "").strip() or "хозяин"
```

И внутрь `create()`:

```python
    @app.post("/api/say")
    def say():
        """Приём из браузера: фотография или текст.

        Отказы возвращаются не кодом ошибки, а событием ленты: у человека
        один список того, что произошло, и отказ — такая же его строка,
        как запись."""
        settings = config.load_settings()
        author = owner_name(settings)

        text = (request.form.get("text") or "").strip()
        if not text:
            return jsonify(published([feed.add({
                "kind": "слово",
                "text": "Пустое сообщение — нечего разбирать.",
                "note": "Напишите тратой или перетащите чек.",
            })]))

        job = {"kind": "текст", "payload": text, "channel": "браузер",
               "author": author, "chat_id": None, "file": "", "mark": ""}
        return jsonify(published(start(jobs, job, {"kind": "мой", "text": text})))
```

- [ ] **Шаг 2: Проверить отправку текстом**

```bash
.venv/bin/python bot.py
```

Во втором окне:

```bash
curl -s -X POST -F "text=такси 450 наличными" \
  http://127.0.0.1:8765/api/say | .venv/bin/python -m json.tool
```

Ожидание: два события — `{"kind": "мой", "text": "такси 450 наличными"}` и
`{"kind": "работа", "task": "…"}`. Ответ приходит мгновенно, не через минуту.

Подождать до минуты и спросить ленту:

```bash
curl -s "http://127.0.0.1:8765/api/events?after=0" | .venv/bin/python -m json.tool
```

Ожидание: третьим событием `"kind": "запись"` с `merchant`, `amount: 450`,
`row` и тем же `task`, что у плашки. Полей `author` и `channel` у него быть
**не должно** — своё не подписывается. В таблице строка, в колонке «кто
прислал» — имя из `settings.json`.

- [ ] **Шаг 3: Проверить отказ на «привет»**

```bash
curl -s -X POST -F "text=привет" http://127.0.0.1:8765/api/say > /dev/null
sleep 40
curl -s "http://127.0.0.1:8765/api/events?after=0" | .venv/bin/python -m json.tool
```

Ожидание: последнее событие `"kind": "слово"` с фразой агента. В таблице
новой строки нет.

- [ ] **Шаг 4: Проверить пустую отправку**

```bash
curl -s -X POST -F "text=   " http://127.0.0.1:8765/api/say | .venv/bin/python -m json.tool
```

Ожидание: одно событие «Пустое сообщение — нечего разбирать.» Движок при этом
не звался: в терминале бота тихо.

- [ ] **Шаг 5: Коммит**

```bash
git add web.py
git commit -m "feat: приём расхода текстом из браузера"
```

---

### Задача 5: Приём из браузера фотографией

Главная проверка всего замысла: чек попадает в таблицу, не открыв телеграма.
Плюс два отказа до вызова агента и дедупликация по хешу содержимого.

**Файлы:**
- Изменить: `web.py` — ветка файла в `/api/say`, функция `photo()`
- Проверка: `curl -F "file=@…"`

**Стыки:**
- Берёт: `intake.fingerprint()`, `intake.duplicate()`, `intake.save_photo()`,
  `intake.when()` — из задачи 3.
- Отдаёт: `POST /api/say` с файлом в поле `file` → тот же вид ответа, что и у
  текста.

- [ ] **Шаг 1: Дописать `web.py`**

Константы рядом с `FIRST_PORT`:

```python
# Больше десяти мегабайт фотография чека не весит. Отказ до вызова агента:
# держать движок 50 секунд ради заведомо чужого файла незачем.
MAX_FILE = 10 * 1024 * 1024
PICTURES = (".jpg", ".jpeg", ".png")
```

Импорт в шапку:

```python
from pathlib import Path
```

Функция рядом с `start()`:

```python
def photo(st, jobs, upload, author):
    """Фотография из браузера: проверить, поймать повтор, положить в очередь.

    Дедупликация в браузере устроена не так, как в телеграме: там файл
    опознаёт сам Bot API по file_unique_id, здесь такого нет — считаем хеш
    содержимого. Считаем до того, как файл лёг на диск: повтор не должен
    плодить копии в чеки/входящие/."""
    name = upload.filename
    mine = {"kind": "мой", "file": name}

    if Path(name).suffix.lower() not in PICTURES:
        return [feed.add(mine), feed.add({
            "kind": "слово", "text": "Это не фотография чека.",
            "note": "Перетащите файл JPG или PNG.",
        })]

    data = upload.read(MAX_FILE + 1)
    if len(data) > MAX_FILE:
        return [feed.add(mine), feed.add({
            "kind": "слово", "text": "Файл больше 10 МБ.",
            "note": "Уменьшите фотографию и пришлите ещё раз.",
        })]

    mark = intake.fingerprint(data)
    old = intake.duplicate(st, mark)
    if old:
        return [feed.add(mine), feed.add({
            "kind": "слово", "text": "Этот чек уже записан.",
            "row": old.get("row"), "note": intake.when(old),
        })]

    path = intake.save_photo(data, author, Path(name).suffix.lower())
    job = {"kind": "фото", "payload": str(path), "channel": "браузер",
           "author": author, "chat_id": None, "file": path.name, "mark": mark}
    return start(jobs, job, mine)
```

- [ ] **Шаг 2: Добавить ветку файла в маршрут `/api/say`**

В начало тела `say()`, сразу после `author = owner_name(settings)`:

```python
        upload = request.files.get("file")
        if upload and upload.filename:
            return jsonify(published(photo(st, jobs, upload, author)))
```

- [ ] **Шаг 3: Проверить главную дорогу замысла**

```bash
.venv/bin/python bot.py
```

Во втором окне:

```bash
curl -s -X POST -F "file=@тестовые-чеки/01-чёткий.jpg" \
  http://127.0.0.1:8765/api/say | .venv/bin/python -m json.tool
sleep 60
curl -s "http://127.0.0.1:8765/api/events?after=0" | .venv/bin/python -m json.tool
```

Ожидание: сразу — «мой» с именем файла и «работа»; через минуту — «запись» с
тем же `task`. **В таблице строка.** В `чеки/готово/` лежит файл с именем
вида `2026-09-02_143012_Леонид.jpg`.

- [ ] **Шаг 4: Проверить повтор**

```bash
curl -s -X POST -F "file=@тестовые-чеки/01-чёткий.jpg" \
  http://127.0.0.1:8765/api/say | .venv/bin/python -m json.tool
```

Ожидание: «Этот чек уже записан.» с номером строки и «такой же файл приходил
сегодня в HH:MM». Движок не звался — в терминале бота тихо. В
`чеки/входящие/` новых файлов нет.

- [ ] **Шаг 5: Проверить отказ по виду файла**

```bash
curl -s -X POST -F "file=@README.md" \
  http://127.0.0.1:8765/api/say | .venv/bin/python -m json.tool
```

Ожидание: «Это не фотография чека.» Файл на диск не лёг.

- [ ] **Шаг 6: Проверить отказ по размеру**

```bash
.venv/bin/python -c "open('/tmp/большой.jpg','wb').write(b'0'*11*1024*1024)"
curl -s -X POST -F "file=@/tmp/большой.jpg" \
  http://127.0.0.1:8765/api/say | .venv/bin/python -m json.tool
rm /tmp/большой.jpg
```

Ожидание: «Файл больше 10 МБ.»

- [ ] **Шаг 7: Коммит**

```bash
git add web.py
git commit -m "feat: приём фотографии чека из браузера, повтор ловится по хешу"
```

---

### Задача 6: Клиент — опрос ленты и отрисовка

Страница оживает: показывает то, что уже произошло, и дорисовывает новое раз
в три секунды. Отправлять пока нечем — события делает `curl`.

**Файлы:**
- Изменить: `static/app.js`
- Проверка: браузер плюс `curl`

**Стыки:**
- Берёт: `GET /api/events?after=N` → `{events, last, state}` — из задачи 2.
- Отдаёт (внутри файла, нужны задачам 7–9): `place(event)` рисует событие;
  `records` — накопленные события «запись» для сайдбара; `show(name)`
  переключает панели; `poll()` — один заход опроса.

- [ ] **Шаг 1: Дописать в `static/app.js` разбор чисел, денег и дат**

Сразу после `var app = document.getElementById("app");` добавить ёмкости и
словари:

```javascript
  var col = document.querySelector("#p-feed .col");
  var flow = document.querySelector("#p-feed .flow");
  var last = 0;          /* номер последнего показанного события */
  var shown = {};        /* какие события уже нарисованы */
  var records = [];      /* события «запись» — из них собирается сайдбар */

  var MONTHS = ["января", "февраля", "марта", "апреля", "мая", "июня",
                "июля", "августа", "сентября", "октября", "ноября", "декабря"];
  var PAYMENTS = {"карта": "картой", "наличные": "наличными",
                  "перевод": "переводом", "неизвестно": ""};
  var SIGNS = {"RUB": " ₽", "USD": " $", "EUR": " €", "другая": ""};

  /* 1004.7 → «1 004,70». Разряды отбиваем неразрывным пробелом, иначе строка
     переносится посреди числа. Дробную часть при этом не трогаем: правило
     «по три цифры» применимо только к целой. */
  function money(sum) {
    var parts = sum.toFixed(2).split(".");
    return parts[0].replace(/\B(?=(\d{3})+$)/g, " ") + "," + parts[1];
  }

  /* «2026-08-20» → «20 августа». Пустая дата — пустая строка: агент честно
     сказал, что даты не видит, и выдумывать её здесь тем более незачем. */
  function day(iso) {
    if (!iso) return "";
    var parts = String(iso).split("-");
    if (parts.length !== 3) return "";
    return Number(parts[2]) + " " + MONTHS[Number(parts[1]) - 1];
  }
```

- [ ] **Шаг 2: Дописать сборку узлов**

```javascript
  function el(tag, cls, text) {
    var node = document.createElement(tag);
    if (cls) node.className = cls;
    if (text !== undefined) node.textContent = text;
    return node;
  }

  /* Иконку рисуем ссылкой на <symbol> в шапке страницы — те же значки, что
     в эталоне. className у SVG только для чтения, поэтому setAttribute. */
  function icon(name) {
    var svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
    svg.setAttribute("class", name === "mark" ? "mark" : "ic");
    var use = document.createElementNS("http://www.w3.org/2000/svg", "use");
    use.setAttribute("href", "#" + name);
    svg.appendChild(use);
    return svg;
  }

  /* Названия магазинов приезжают из фотографии, прочитанной моделью. Всё,
     что от неё пришло, ставим только через textContent: innerHTML на таких
     данных — дыра. */
  function head(text) {
    var hdr = el("div", "hdr");
    hdr.appendChild(icon("mark"));
    hdr.appendChild(el("b", "", text));
    return hdr;
  }

  /* Подпись только у чужого. Человек впереди способа: значимо, кто потратил,
     а не с какого устройства прислал. */
  function source(event, box) {
    if (event.author) box.appendChild(el("div", "src", event.author + " · " + event.channel));
  }
```

- [ ] **Шаг 3: Дописать четыре вида событий**

```javascript
  /* Реплика человека — то, что он прислал. */
  function mine(event) {
    var box = el("div", "u");
    if (event.file) {
      var att = el("span", "att");
      att.appendChild(icon("i-clip"));
      att.appendChild(document.createTextNode(event.file));
      box.appendChild(att);
    } else {
      box.textContent = event.text;
    }
    return box;
  }

  /* «Смотрю чек…» — плашка на 15–50 секунд. Её уберёт событие с тем же
     номером задачи. */
  function running(event) {
    var box = el("div", "b");
    box.dataset.task = event.task;
    var hdr = el("div", "hdr");
    hdr.appendChild(icon("mark"));
    var run = el("span", "run", "Смотрю чек… ");
    run.appendChild(el("small", "", "15–50 сек"));
    hdr.appendChild(run);
    box.appendChild(hdr);
    return box;
  }

  /* Фраза бота: отказ или пояснение. Её сочиняет агент или сам бот — здесь
     переписывать нечего, только показать. */
  function said(event) {
    var box = el("div", "b");
    source(event, box);
    var hdr = el("div", "hdr");
    hdr.appendChild(icon("mark"));
    var title = el("b", "", event.text);
    if (event.row) {
      title.appendChild(document.createTextNode(" — "));
      title.appendChild(el("em", "", "строка " + event.row));
    }
    hdr.appendChild(title);
    box.appendChild(hdr);
    if (event.note) box.appendChild(el("div", "sub", event.note));
    return box;
  }

  /* Запись расхода. Фразу собираем здесь, из полей: сервер отдаёт числа и
     слова, а разряды, знак валюты и порядок — дело вёрстки. */
  function written(event) {
    var box = el("div", "b");
    source(event, box);
    var title = event.merchant;
    if (event.amount !== null && event.amount !== undefined) {
      title += " · " + money(event.amount) + (SIGNS[event.currency] || "");
    } else if (event.date) {
      title += " · " + day(event.date);
    }
    box.appendChild(head(title));
    box.appendChild(details(event));
    return box;
  }

  /* Вторая строка записи. У чистой — «продукты · картой · 20 августа —
     строка 47». У пойманной проверками — сама причина плашкой и «на
     проверку»: терракота значит «взгляни», и только это. */
  function details(event) {
    var sub = el("div", "sub");
    var trouble = (event.reasons || []).concat(event.warnings || []);
    if (event.status === "проверить" && trouble.length) {
      sub.appendChild(el("span", "warn", trouble[0]));
      sub.appendChild(document.createTextNode(" — "));
    } else {
      var facts = [event.category, PAYMENTS[event.payment], day(event.date)]
        .filter(function (part) { return part; });
      sub.appendChild(document.createTextNode(facts.join(" · ") + " — "));
    }
    if (event.row) {
      sub.appendChild(el("em", "", "строка " + event.row +
        (event.status === "проверить" ? ", на проверку" : "")));
    } else {
      sub.appendChild(el("em", "", "в таблицу пока не попало"));
    }
    return sub;
  }
```

- [ ] **Шаг 4: Дописать укладку события в ленту**

```javascript
  function place(event) {
    /* Отправка и опрос идут навстречу друг другу и возвращают одни и те же
       события: отправка — сразу, опрос — потому что успел начаться раньше.
       Без этой проверки трата нарисовалась бы дважды. Номер 0 у событий,
       которые страница придумала сама, — их не помним. */
    if (event.id > 0) {
      if (shown[event.id]) return;
      shown[event.id] = true;
    }

    /* Ответ пришёл — плашка «Смотрю чек…» своё отслужила. */
    if (event.task && event.kind !== "работа") {
      var pending = col.querySelector('[data-task="' + event.task + '"]');
      if (pending) pending.remove();
    }

    var node = event.kind === "мой" ? mine(event)
             : event.kind === "работа" ? running(event)
             : event.kind === "запись" ? written(event)
             : said(event);
    node.dataset.id = event.id;
    col.appendChild(node);

    if (event.kind === "запись") records.push(event);
    flow.scrollTop = flow.scrollHeight;
  }

  function receive(answer) {
    (answer.events || []).forEach(place);
    /* Назад номер не двигаем: ответ отправки и ответ опроса приходят в любом
       порядке, и меньший из них заставил бы спросить уже показанное. */
    last = Math.max(last, answer.last);
    /* Пустое начало и лента — одно место в двух состояниях. */
    show(col.children.length ? "feed" : "empty");
  }
```

- [ ] **Шаг 5: Дописать опрос**

```javascript
  /* Раз в три секунды. Вебсокетов нет намеренно: на локальном адресе опрос
     дешевле и понятнее на уроке. Сорвался запрос — молчим и пробуем снова:
     бот мог перезапускаться, и красная строка на весь экран здесь ни к чему. */
  function poll() {
    fetch("/api/events?after=" + last)
      .then(function (response) { return response.json(); })
      .then(receive)
      .catch(function () {});
  }

  poll();
  setInterval(poll, 3000);
```

Строку `show("empty");` из задачи 1 убрать: состояние теперь выбирает
`receive()`.

- [ ] **Шаг 6: Проверить отрисовку**

```bash
.venv/bin/python bot.py
```

Открыть `http://127.0.0.1:8765`. Ожидание: пустое начало.

Во втором окне отправить текст и, не закрывая страницы, смотреть на неё:

```bash
curl -s -X POST -F "text=обед с клиентом 2400 картой" \
  http://127.0.0.1:8765/api/say > /dev/null
```

Ожидание: в течение трёх секунд экран сменился на ленту, справа появился
пузырь «обед с клиентом 2400 картой», под ним «Смотрю чек… 15–50 сек».
Через полминуты плашка **исчезла**, на её месте — «Обед с клиентом · 2 400,00 ₽»
и второй строкой «еда вне дома · картой · 2 сентября — строка N».

Отправить чек, который проверки пометят:

```bash
curl -s -X POST -F "file=@тестовые-чеки/03-обрезанный.jpg" \
  http://127.0.0.1:8765/api/say > /dev/null
```

Ожидание: строка с терракотовой плашкой причины и «строка N, на проверку».

Прислать чек из телеграма. Ожидание: в ленте браузера над записью серая
подпись «Имя · телеграм». У записей из браузера подписи нет.

Сравнить с `design/workspace.html`, экран «То же окно с лентой»: отступы,
размеры, цвета, положение знака должны совпадать.

- [ ] **Шаг 7: Коммит**

```bash
git add static/app.js
git commit -m "feat: страница опрашивает ленту и рисует события"
```

---

### Задача 7: Клиент — отправка текстом

Поле ввода начинает работать. Их два — в ленте и на пустом начале, — и ведут
они себя одинаково.

**Файлы:**
- Изменить: `static/app.js`
- Проверка: браузер

**Стыки:**
- Берёт: `POST /api/say` с полем `text` — из задачи 4; `receive()` и `place()`
  — из задачи 6.
- Отдаёт: `send(form)` — отправляет `FormData` и показывает вернувшиеся
  события, не дожидаясь опроса.

- [ ] **Шаг 1: Дописать отправку в `static/app.js`**

```javascript
  /* Отправка возвращается сразу, с плашкой «Смотрю чек…». Ответ приедет
     опросом. Вернувшиеся события показываем немедленно, чтобы поле не
     выглядело мёртвым три секунды. */
  function send(form) {
    fetch("/api/say", {method: "POST", body: form})
      .then(function (response) { return response.json(); })
      .then(receive)
      .catch(function () {
        place({id: 0, kind: "слово", text: "Бот не отвечает.",
               note: "Проверьте окно терминала, в котором он запущен."});
      });
  }

  function sendText(area) {
    var text = area.value.trim();
    if (!text) return;
    area.value = "";
    area.style.height = "";
    var form = new FormData();
    form.append("text", text);
    send(form);
  }
```

- [ ] **Шаг 2: Привязать оба поля ввода**

```javascript
  /* Enter отправляет, Shift+Enter переносит строку — как в чате, а не как
     в форме. Поле растёт под текст: трата в три строки не должна прятаться
     под скроллом. */
  document.querySelectorAll(".box .in").forEach(function (area) {
    area.addEventListener("keydown", function (event) {
      if (event.key === "Enter" && !event.shiftKey) {
        event.preventDefault();
        sendText(area);
      }
    });
    area.addEventListener("input", function () {
      area.style.height = "";
      area.style.height = area.scrollHeight + "px";
    });
  });
```

- [ ] **Шаг 3: Проверить**

Открыть страницу. Напечатать в поле `такси 450 наличными`, нажать Enter.

Ожидание: поле очистилось, экран сменился на ленту, справа пузырь с текстом,
под ним «Смотрю чек…». Через полминуты — «Такси · 450,00 ₽» и «транспорт и
такси · наличными · 2 сентября — строка N». В таблице строка.

Нажать Shift+Enter — курсор переходит на вторую строку, поле подрастает,
ничего не отправляется.

Нажать Enter на пустом поле — ничего не происходит, запроса нет.

Написать `привет`. Ожидание: отказ фразой агента, в таблице пусто.

Написать `не 450, а 480`. Ожидание: «Правки пока не умею.» и «Пришлите трату
заново, целиком.» В таблице ничего не изменилось — правка это срез 3.

- [ ] **Шаг 4: Коммит**

```bash
git add static/app.js
git commit -m "feat: трата отправляется текстом из окна браузера"
```

---

### Задача 8: Клиент — перетаскивание и прикрепление чека

То, ради чего затевалось рабочее место: фотографию чека можно просто бросить
в окно.

**Файлы:**
- Изменить: `static/app.js`
- Проверка: браузер

**Стыки:**
- Берёт: `POST /api/say` с полем `file` — из задачи 5; `send()` — из задачи 7.
- Отдаёт: `sendFiles(list)` — отправляет файлы по одному.

- [ ] **Шаг 1: Дописать отправку файлов в `static/app.js`**

```javascript
  /* Файлы уходят по одному: в очереди разбора всё равно один за другим, а
     отдельным заданием каждый получает свою плашку и свою строку в ленте. */
  function sendFiles(list) {
    Array.prototype.forEach.call(list, function (file) {
      var form = new FormData();
      form.append("file", file, file.name);
      send(form);
    });
  }
```

- [ ] **Шаг 2: Дописать перетаскивание**

```javascript
  /* Браузер шлёт dragleave и на переходе между вложенными элементами, поэтому
     считаем входы и выходы, а не гасим подсветку на первом же dragleave —
     иначе рамка мигает, пока ведёшь файл через окно. */
  var dragDepth = 0;

  function dragging(on) {
    app.classList.toggle("dragging", on);
  }

  document.addEventListener("dragenter", function (event) {
    event.preventDefault();
    dragDepth += 1;
    dragging(true);
  });

  document.addEventListener("dragover", function (event) {
    event.preventDefault();
  });

  document.addEventListener("dragleave", function (event) {
    event.preventDefault();
    dragDepth -= 1;
    if (dragDepth <= 0) { dragDepth = 0; dragging(false); }
  });

  /* Без этого браузер откроет фотографию во всю вкладку вместо того, чтобы
     отдать её боту. */
  document.addEventListener("drop", function (event) {
    event.preventDefault();
    dragDepth = 0;
    dragging(false);
    if (event.dataTransfer && event.dataTransfer.files.length) {
      sendFiles(event.dataTransfer.files);
    }
  });
```

- [ ] **Шаг 3: Дописать кнопку «+»**

```javascript
  /* Перетащить можно не всегда: чек бывает в телефоне, а окно на другом
     экране. Кнопка «+» открывает обычный выбор файла — оба поля ввода
     зовут одно и то же скрытое поле. */
  var pick = document.getElementById("pick");

  document.querySelectorAll(".box .add").forEach(function (button) {
    button.addEventListener("click", function () { pick.click(); });
  });

  pick.addEventListener("change", function () {
    if (pick.files.length) sendFiles(pick.files);
    pick.value = "";   /* иначе тот же файл второй раз не выберется */
  });
```

- [ ] **Шаг 4: Проверить**

Открыть страницу. Перетащить `тестовые-чеки/01-чёткий.jpg` из Finder в окно.

Ожидание: пока файл над окном, рамка поля ввода становится терракотовой; на
отпускании подсветка гаснет, в ленте появляется строка с именем файла и
скрепкой, следом «Смотрю чек…». Через 15–50 секунд — запись и строка в
таблице. Фотография во всю вкладку **не** открывается.

Провести файл над окном и увести его, не отпуская. Ожидание: подсветка
гаснет, ничего не отправляется, рамка по дороге не мигает.

Перетащить ту же фотографию второй раз. Ожидание: «Этот чек уже записан —
строка N», ниже «такой же файл приходил сегодня в HH:MM».

Перетащить `README.md`. Ожидание: «Это не фотография чека.»

Нажать «+» в поле ввода, выбрать `тестовые-чеки/02-мятый.jpg`. Ожидание: то
же, что при перетаскивании. Нажать «+» и выбрать тот же файл ещё раз —
выбор срабатывает (поле очищается после каждой отправки).

- [ ] **Шаг 5: Коммит**

```bash
git add static/app.js
git commit -m "feat: чек принимается перетаскиванием и кнопкой прикрепления"
```

---

### Задача 9: Сайдбар живьём

В сайдбаре перестают висеть выдуманные «16 статей» и «Пятёрочка · 1 004,70».
Настройки остаются на срез 4 — их пункты скрыты с задачи 1.

**Файлы:**
- Изменить: `agent.py` — `TITLES`
- Изменить: `config.py` — `SHEET_LINK` в `.env`
- Изменить: `.env.example`
- Изменить: `web.py` — `snapshot()`
- Изменить: `static/app.js` — `paint()`, `drawToday()`
- Проверка: браузер

**Стыки:**
- Берёт: `state.get("state")` из ответа `/api/events` — из задачи 2;
  `records` — из задачи 6.
- Отдаёт: `web.snapshot()` →
  `{"owner": "Леонид", "categories": 16, "sheet_link": "https://…",
    "engine": "Claude Code", "telegram": true, "blocked": ""}`.

- [ ] **Шаг 1: Дать движкам человеческие названия в `agent.py`**

Рядом с `ENGINES`:

```python
# Как движок называется в сайдбаре. Ключ — то, что лежит в settings.json.
TITLES = {"claude_code": "Claude Code", "codex": "Codex"}
```

- [ ] **Шаг 2: Добавить адрес таблицы в `config.py`**

`SHEET_URL` — это адрес веб-приложения Apps Script, по нему человеку смотреть
нечего. Пункту «Таблица» нужен адрес самой таблицы, и это отдельная строка.
Необязательная: не заполнена — пункт просто ничего не откроет.

В `load_env()`, в возвращаемый словарь:

```python
        "sheet_link": os.getenv("SHEET_LINK", ""),
```

В `.env.example`:

```
# Адрес самой таблицы — для кнопки «Таблица» в рабочем месте. Необязательно.
SHEET_LINK=
```

Свой `.env` заполнить настоящим адресом таблицы.

- [ ] **Шаг 3: Наполнить `snapshot()` в `web.py`**

```python
def snapshot(env, st, blocked):
    """Состояние для сайдбара. Едет с каждым ответом опроса — так сайдбар не
    может разъехаться с лентой.

    Число статей берётся из кэша в state.json, а не из таблицы: опрос идёт
    раз в три секунды, и дёргать Apps Script на каждый заход незачем."""
    settings = config.load_settings()
    return {
        "owner": (settings.get("owner") or "").strip(),
        "categories": len(st["categories"]),
        "sheet_link": env["sheet_link"],
        "engine": agent.TITLES.get(settings["engine"], settings["engine"]),
        "telegram": bool(env["bot_token"]),
        "blocked": blocked,
    }
```

Импорт в шапку `web.py`:

```python
import agent
```

- [ ] **Шаг 4: Дописать `paint()` в `static/app.js`**

Ёмкости — рядом с остальными, в начале файла:

```javascript
  var today = document.getElementById("today");
  var todayNone = document.getElementById("today-none");
  var greeting = document.getElementById("greeting");
  var navTable = document.getElementById("nav-table");
```

```javascript
  /* Сайдбар. Здесь только то, что уже посчитано где-то ещё: имя из настроек,
     статьи из кэша справочника, движок из settings.json. Управление ими —
     срез 4, до тех пор их пункты в разметке скрыты. */
  function paint(state) {
    greeting.textContent = state.owner
      ? "Что записываем, " + state.owner + "?"
      : "Что записываем?";

    navTable.querySelector(".r").textContent =
      state.categories ? state.categories + " статей" : "";
    if (state.sheet_link) {
      navTable.href = state.sheet_link;
    } else {
      /* Адреса таблицы нет — пункт не должен притворяться ссылкой. */
      navTable.removeAttribute("href");
    }

    document.querySelectorAll(".engine-badge").forEach(function (badge) {
      badge.textContent = state.engine;
    });
  }
```

- [ ] **Шаг 5: Дописать «Записи сегодня»**

```javascript
  /* Список того, что записано сегодня. Собирается из тех же событий, что и
     лента, — второго источника у него нет и быть не должно.

     Кружок закрашен терракотой у строк со статусом «проверить»: терракота
     значит «взгляни». */
  function drawToday() {
    var midnight = new Date();
    midnight.setHours(0, 0, 0, 0);

    var mineToday = records.filter(function (record) {
      return record.at * 1000 >= midnight.getTime();
    });

    today.textContent = "";
    mineToday.forEach(function (record) {
      var item = el("button", "li");
      item.type = "button";
      item.appendChild(el("span", "bl" + (record.status === "проверить" ? " chk" : "")));
      item.appendChild(el("span", "nm",
        record.merchant + (record.author ? " · " + record.author : "")));
      item.appendChild(el("span", "am",
        record.amount === null || record.amount === undefined ? "—" : money(record.amount)));
      item.addEventListener("click", function () {
        show("feed");
        var node = col.querySelector('[data-id="' + record.id + '"]');
        if (node) node.scrollIntoView({block: "center"});
      });
      today.appendChild(item);
    });

    today.hidden = mineToday.length === 0;
    todayNone.hidden = !today.hidden;
  }
```

- [ ] **Шаг 6: Позвать обе функции из `receive()`**

`receive()` становится таким:

```javascript
  function receive(answer) {
    (answer.events || []).forEach(place);
    last = Math.max(last, answer.last);
    if (answer.state) paint(answer.state);
    drawToday();
    show(col.children.length ? "feed" : "empty");
  }
```

`send()` отвечает без `state` — там `paint()` просто не зовётся, а сайдбар
обновит ближайший опрос.

- [ ] **Шаг 7: Поправить текст пустого сайдбара в `templates/workspace.html`**

Лента живёт в памяти процесса, и «Записи сегодня» собираются из неё. Значит
после перезапуска бота сайдбар пуст, хотя в таблице за сегодня уже пять
строк. «Пока пусто» в этом месте — обещание, которого код не держит.

Это не вкусовщина, а требование спеки: раздел «Лента переживает перезапуск»
предписывает такой оборот на всё время, пока лента не сохраняется.

Было:

```html
<p class="li-none" id="today-none" hidden>
  Пока пусто. Перетащите чек в окно справа — он появится здесь и в таблице.
</p>
```

Стало:

```html
<p class="li-none" id="today-none" hidden>
  С момента запуска ничего не записано. Перетащите чек в окно справа —
  он появится здесь и в таблице.
</p>
```

Разница в одном обороте, а утверждение становится правдой в обоих случаях:
и при первом запуске, и после перезапуска среди рабочего дня.

- [ ] **Шаг 8: Проверить**

Открыть страницу на пустой ленте. Ожидание: приветствие «Что записываем,
Леонид?» с именем из `settings.json`; в сайдбаре «Таблица» с числом статей
(столько же, сколько строк на листе «Статьи»); «Записи сегодня» — приглашение
«С момента запуска ничего не записано»; под полем ввода подпись «Claude Code».

Записать три траты: чистый чек, обрезанный (проверки пометят) и трату из
телеграма.

Ожидание в сайдбаре: три строки; у обрезанного кружок закрашен терракотой, у
остальных пустой; сумма не прочиталась — вместо неё «—»; телеграмная строка
подписана именем; суммы стоят колонкой у правого края. Нажатие на строку
возвращает ленту и прокручивает её к этой записи.

Нажать «Таблица» — открывается таблица в новой вкладке.

Записать трату, перезапустить бота, открыть страницу заново. Ожидание:
«Записи сегодня» снова пусты — и говорят об этом честно, «с момента запуска».
Строка в таблице при этом на месте.

Сравнить сайдбар с `design/workspace.html`: отличаться должны только данные и
три скрытых пункта.

- [ ] **Шаг 9: Коммит**

```bash
git add agent.py config.py .env.example web.py static/app.js templates/workspace.html
git commit -m "feat: сайдбар показывает настоящие статьи, движок и записи за день"
```

---

### Задача 10: Старт без телеграма и при ключе в окружении

Два пути запуска, на которых бот раньше просто не поднимался. Первый — то
самое обещание роадмапа: телеграм больше не нужен. Второй — экран, который
уже нарисован в разметке, но которого никто не видел.

**Файлы:**
- Изменить: `config.py` — `BOT_TOKEN` необязателен, `refuse_if_api_key()`
  становится `api_key_in_env()`
- Изменить: `.env.example`
- Изменить: `bot.py` — `main()` решает, какие потоки поднимать
- Изменить: `web.py` — `/api/say` при заблокированном боте
- Изменить: `static/app.js` — панель `#p-blocked`
- Проверка: пустой `BOT_TOKEN`, затем `ANTHROPIC_API_KEY=x`

**Стыки:**
- Отдаёт: `config.api_key_in_env()` → `True`/`False`;
  `web.create(env, st, jobs, blocked)` — `blocked` это `""` или `"api-key"`;
  `snapshot()["blocked"]` доезжает до страницы.

- [ ] **Шаг 1: Сделать `BOT_TOKEN` необязательным в `config.py`**

Без телеграма бот полноценен: рабочее место принимает чеки само. Требовать
токен на старте значит гнать ученика в @BotFather раньше, чем он увидит хоть
что-то, — а на этом отваливается половина группы.

```python
# Телеграм необязателен: рабочее место в браузере работает без него. Без
# таблицы бот бессмыслен — писать будет некуда.
REQUIRED_ENV = ("SHEET_URL", "SHEET_SECRET")
```

В `load_env()`:

```python
    return {
        "bot_token": os.getenv("BOT_TOKEN", "").strip(),
        "sheet_url": os.environ["SHEET_URL"],
        "sheet_secret": os.environ["SHEET_SECRET"],
        "sheet_link": os.getenv("SHEET_LINK", ""),
    }
```

- [ ] **Шаг 2: Превратить отказ в наблюдение**

Ключ в окружении по-прежнему означает, что разбирать нельзя. Но выйти молча
теперь не годится: страницу, которая это объясняет, надо кому-то показать.

В `config.py` заменить `refuse_if_api_key()` на:

```python
def api_key_in_env():
    """Ключ API в окружении уводит Claude Code с подписки на поминутную
    оплату, и человек узнаёт об этом из счёта.

    Раньше бот на этом просто выходил. Теперь он поднимает рабочее место и
    ничего не разбирает: объяснить на странице надёжнее, чем строкой в
    терминале, которую человек закрыл вместе с окном."""
    return bool(os.getenv("ANTHROPIC_API_KEY"))


BLOCKED_TEXT = (
    "В окружении задан ANTHROPIC_API_KEY.\n"
    "С ним Claude Code пойдёт по счётчику мимо подписки, поэтому чеки я не разбираю.\n"
    "Уберите строку с ANTHROPIC_API_KEY из ~/.zshrc, закройте терминал,\n"
    "откройте заново и запустите бота ещё раз."
)
```

В блоке `if __name__ == "__main__":` того же файла заменить
`refuse_if_api_key()` на:

```python
    if api_key_in_env():
        print(BLOCKED_TEXT)
```

- [ ] **Шаг 3: Научить `main()` в `bot.py` трём путям запуска**

```python
def main():
    env = config.load_env()
    st = memory.load()
    jobs = queue.Queue()
    blocked = "api-key" if config.api_key_in_env() else ""

    if blocked:
        print(config.BLOCKED_TEXT)
        print()
    else:
        threading.Thread(target=worker, args=(env, st, jobs), daemon=True).start()
        if env["bot_token"]:
            threading.Thread(target=telegram_loop, args=(env, st, jobs),
                             daemon=True).start()
        else:
            # Это не поломка, а обычный способ работать: чеки принимает
            # браузер. Телеграм подключается в срезе 5, кнопкой.
            print("Телеграм не подключён — работаем через браузер.")

    web.quiet()
    port = web.choose_port()
    print(f"Рабочее место: http://127.0.0.1:{port}")
    print("Ctrl+C — выход.")
    try:
        web.create(env, st, jobs, blocked).run(host="127.0.0.1", port=port,
                                               threaded=True, use_reloader=False)
    except KeyboardInterrupt:
        print("\nОстанавливаюсь.")
```

- [ ] **Шаг 4: Не принимать чеки, пока бот заблокирован (`web.py`)**

В самое начало тела `say()`, до чтения формы:

```python
        if blocked:
            return jsonify(published([feed.add({
                "kind": "слово", "text": "Бот не запущен.",
                "note": "Уберите ANTHROPIC_API_KEY из окружения и запустите заново.",
            })]))
```

- [ ] **Шаг 5: Показать панель `#p-blocked` в `static/app.js`**

`receive()` дополняется одной веткой в начале:

```javascript
  function receive(answer) {
    /* Бот поднял страницу, но разбирать не будет. Показываем объяснение и
       ничего больше не рисуем: лента здесь ни при чём. */
    if (answer.state && answer.state.blocked) {
      show("blocked");
      return;
    }
    (answer.events || []).forEach(place);
    last = Math.max(last, answer.last);
    if (answer.state) paint(answer.state);
    drawToday();
    show(col.children.length ? "feed" : "empty");
  }
```

- [ ] **Шаг 6: Проверить запуск без телеграма**

```bash
cp .env .env.сохранил
```

В `.env` оставить `BOT_TOKEN=` пустым.

```bash
.venv/bin/python bot.py
```

Ожидание в терминале: `Телеграм не подключён — работаем через браузер.`,
следом адрес. Строки `Телеграм: жду сообщений.` быть не должно.

Открыть страницу, перетащить чек. Ожидание: разобрался, строка в таблице.
**Это главная проверка всего замысла: чек попал в таблицу, а телеграма нет.**

```bash
cp .env.сохранил .env && rm .env.сохранил
```

- [ ] **Шаг 7: Проверить ключ в окружении**

```bash
ANTHROPIC_API_KEY=ничего-страшного .venv/bin/python bot.py
```

Ожидание в терминале: объяснение про `~/.zshrc` и перезапуск терминала,
следом адрес рабочего места. Страниц открыть — видно «Бот не запущен» с
терракотовой плашкой и объяснением. Ленты нет.

```bash
curl -s -X POST -F "text=такси 450" http://127.0.0.1:8765/api/say
```

Ожидание: событие «Бот не запущен.» В таблице ничего, движок не звался.

- [ ] **Шаг 8: Проверить, что обычный запуск не сломался**

```bash
.venv/bin/python bot.py
```

Ожидание: телеграм поднялся, рабочее место открывается, чек из браузера и
чек из телеграма доезжают до таблицы.

- [ ] **Шаг 9: Коммит**

```bash
git add config.py .env.example bot.py web.py static/app.js
git commit -m "feat: бот стартует без телеграма и объясняет ключ в окружении страницей"
```

---

### Задача 11: README и сквозная приёмка

**Файлы:**
- Изменить: `README.md`
- Изменить: `docs/superpowers/specs/2026-09-01-чеки-бот-roadmap.md` — строка
  среза 2 и запись в Notes
- Проверка: прогон по разделу «Как проверяем» спеки

**Стыки:**
- Берёт: всё сделанное задачами 1–10.

- [ ] **Шаг 1: Переписать README**

Заменить первый абзац:

```markdown
Бот принимает фотографию чека или сообщение «такси 450 наличными» и дописывает
строку расхода в Google-таблицу. Входов два: рабочее место в браузере и
телеграм. Мозгом служит Claude Code по подписке — ключ API не нужен.
```

В разделе «Запуск» — зависимости и порядок:

```bash
.venv/bin/pip install requests python-dotenv flask
```

и после `.venv/bin/python bot.py`:

```markdown
В терминале появится адрес вида `http://127.0.0.1:8765` — откройте его.
Перетащите фотографию чека в окно или напишите трату текстом.

Телеграм необязателен: с пустым `BOT_TOKEN` бот работает через браузер.
Страница слушает только `127.0.0.1` — снаружи её не видно, пароля у неё нет.
```

В таблицу «Файлы» дописать четыре строки:

```markdown
| `web.py` | рабочее место: страница и три маршрута |
| `intake.py` | единый приём: один разбор на телеграм и браузер |
| `feed.py` | лента: журнал событий в памяти |
| `static/app.js` | клиент: опрос ленты, отрисовка, перетаскивание |
```

Строку про `bot.py` поправить: `телеграм и потоки: приём, очередь, разбор`.

Раздел «Чего бот не делает» — последний абзац про «телеграм здесь
единственный вход» заменить на:

```markdown
Пока не правит записанное («не 450, а 480» — следующий срез), не управляет
белым списком из браузера и не ставится мастером: `.env` и `settings.json`
заполняются руками.
```

- [ ] **Шаг 2: Прогнать приёмку**

Пункты из раздела «Как проверяем» спеки рабочего места, которые относятся к
этому срезу. Пункт 1 — мастер установки (срез 5), пункты 6, 8 и 9 — правка
(срез 3): их не проверяем.

- [ ] **2. Телеграм пропущен, чек перетащен в браузер — строка в таблице.**
  Пустой `BOT_TOKEN`, `python bot.py`, перетащить `тестовые-чеки/01-чёткий.jpg`.
  Ожидание: запись в ленте и строка в таблице. Главная проверка замысла.
- [ ] **3. «такси 450 наличными» текстом — записано.**
- [ ] **4. «привет» — отказ с подсказкой, в таблице ничего.**
- [ ] **5. Тот же чек второй раз — «уже записан» с номером строки.**
- [ ] **7. Телеграм подключён, чек с телефона — запись видна в ленте
  браузера с пометкой «Имя · телеграм».**
- [ ] **10. Лист «Статьи» переименован — в сайдбаре ноль статей, чеки не
  разбираются, в ленте честный отказ.** Вернуть имя листа обратно.
- [ ] **11. `ANTHROPIC_API_KEY=x python bot.py` — бот не разбирает, страница
  объясняет.**
- [ ] **12. `claude` переименован — отказ в ленте, бот не падает.**
  Проверять так: `mv $(which claude) /tmp/claude-спрятал`, перетащить чек,
  вернуть обратно.
- [ ] **13. `state.json` удалён на ходу — белый список и настройки на
  месте.** Лента при этом уцелеет: она в памяти процесса.
- [ ] **14. Интернет выключен на середине разбора — бот не падает, запись
  доезжает при возврате.** Ожидание в ленте: «в таблицу пока не попало»,
  потом «Отложенные строки доехали до таблицы: 1.»

- [ ] **Шаг 3: Прогнать самопроверку целиком**

```bash
.venv/bin/python tools/selfcheck.py
```

Ожидание: ни одного `✗`.

- [ ] **Шаг 4: Убедиться, что секреты не уехали**

```bash
git status --short
git ls-files | grep -E "^(\.env|settings\.json|state\.json)$" || echo "секретов в гите нет"
```

Ожидание: `секретов в гите нет`, и в `git status` нет ни `.env`, ни
`settings.json`, ни `state.json`, ни папки `чеки/`.

- [ ] **Шаг 5: Отметить срез в роадмапе**

В таблице Slices строку среза 2 привести к виду:

```
| 2 | Рабочее место: лента | … | … | **shipped** | [рабочее место](…) — «Лента», «Устройство», «Дизайн» | [план](../plans/2026-09-02-срез-2-рабочее-место-лента.md) | main, `<хеш>` |
```

В «Notes / changes along the way» дописать запись о сдаче: что проверено,
сколько проверок в самопроверке, что взято сверх трёх разделов спеки
(необязательный `BOT_TOKEN`, страница вместо выхода при ключе, «Записи
сегодня») и что клиентскую часть писал этот срез, потому что у неё не
оказалось владельца.

- [ ] **Шаг 6: Коммит**

```bash
git add README.md docs/superpowers/specs/2026-09-01-чеки-бот-roadmap.md
git commit -m "docs: README про два входа и роадмап со сданным срезом 2"
```
