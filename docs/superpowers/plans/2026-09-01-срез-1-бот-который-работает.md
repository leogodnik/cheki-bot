# Срез 1 «Бот, который работает» — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Цель:** телеграм-бот принимает фотографию чека или сообщение «такси 450
наличными», разбирает Claude Code по подписке, проверяет результат кодом и
дописывает строку в Google-таблицу.

**Устройство:** мозг и руки разделены. `agent.py` — шов: зовёт движок и
проверяет форму ответа. `engines/claude_code.py` — единственный движок первой
версии. `checks.py` — детерминированные проверки, которые срабатывают всегда и
не спрашивают у агента, уверен ли он. `bot.py` — руки: приём, скачивание,
запись, ответ, раскладка файлов. Таблица отвечает через Apps Script.

**Технологии:** Python 3.13, `requests`, `python-dotenv`, Claude Code CLI в
headless-режиме, Google Apps Script. Больше ничего — Flask появится в срезе 2.

**Спека:** [2026-08-23-чеки-в-таблицу-design.md](../specs/2026-08-23-чеки-в-таблицу-design.md)
с поправкой из [роадмапа](../specs/2026-09-01-чеки-бот-roadmap.md), раздел «Notes».
Смежная спека, которую этот срез не реализует, но не должен ломать:
[2026-09-01-панель-администратора-design.md](../specs/2026-09-01-панель-администратора-design.md).

## Global Constraints

Требования, действующие во всех задачах без исключения.

- **Python 3.13**, венв в `.venv`, создаётся базовым питоном
  `/Library/Frameworks/Python.framework/Versions/3.13/bin/python3` (в PATH
  сейчас лежит венв соседнего проекта faq-bot — им пользоваться нельзя).
- **Зависимости ровно две:** `requests`, `python-dotenv`. Никаких OCR-библиотек,
  tesseract, pytest, Flask.
- **Код и имена файлов латиницей.** Русский — в текстах, комментариях,
  докстрингах, промптах, названиях папок с чеками и колонках таблицы.
- **Ключи в `schema.json` латиницей, значения русские.** API отвечает
  `400 Property keys should match pattern '^[a-zA-Z0-9_.-]{1,64}$'` (находка 2).
- **`--agent` не используется никогда.** С ним схема молча отваливается:
  `structured_output` приходит пустым, модель придумывает свои имена полей.
  Мозг агента уходит в `--append-system-prompt` (находка 3).
- **`--json-schema` принимает текст схемы, а не путь к файлу.** Проверено
  1 сентября 2026: на путь CLI отвечает
  `Error: --json-schema is not valid JSON: JSON Parse error: Unrecognized token '/'`.
  В спеке нарисован путь — это устарело, передаём `SCHEMA_PATH.read_text()`.
- **Фото — `sonnet`, текст — `haiku`** (находка 5). На мятом чеке haiku в одном
  прогоне из четырёх прочитал 20.08 как 20.09 и поставил себе высокую уверенность.
- **Пустой `structured_output` — отказ.** Разбирать текст из блока кода запрещено.
- **Таймаут вызова движка 180 секунд, одна повторная попытка**, потом честный
  отказ в чат. Каждый вызов стоит около 33 тысяч токенов подписки (находка 7).
- **`subprocess` только списком аргументов.** Через строку в шелле нельзя:
  в промпте кавычки и обратные кавычки, шелл на них спотыкается.
- **Белый список живёт в `settings.json`**, не в `.env` — поправка роадмапа
  от 1 сентября. В срезе 1 файл правится руками, в срезе 3 его будет писать
  панель. Формат менять нельзя: `{"engine": ..., "allowed": [...], "knocked": []}`.
- **Бот `settings.json` не пишет** — только читает, перед каждым сообщением.
  Пишет его человек (в срезе 3 — панель).
- **`ANTHROPIC_API_KEY` в окружении — бот не запускается** и печатает, что делать.
  Иначе Claude Code уйдёт с подписки на поминутную оплату.
- **`allow_redirects` в запросах к Apps Script не отключать** — придёт пустой 302.
- **Статья не из справочника → «Прочее», статус при этом не меняется.** Это
  рабочий сигнал «в справочнике не хватает статьи», а не сомнение в строке.
- **Статьи агенту придумывать нельзя.** Нет справочника и нет запаса в
  `state.json` — бот отказывается разбирать и говорит об этом.
- **Тестовые чеки — JPEG.** На PNG те же файлы читаются иначе (находка 8).
- **Автотестов нет** — решение спеки. Проверка: живьём по разделу «Как проверяем»
  плюс `tools/selfcheck.py` на голых `assert` для чистых функций. Ни pytest,
  ни папки `tests/`.
- **Расхождения между прогонами модели — норма** (находка 6). Ненормально, когда
  проверки их пропускают. Ни одна задача не «чинится» повтором прогона.
- **Код — учебный материал.** Простые функции, русские докстринги, никаких
  дата-классов, аннотаций типов и абстракций сверх необходимого. Читателем будет
  финансист, впервые открывший питон.

## Раскладка файлов

```
чеки-бот/
  prompt.md                 есть — мозг агента, уходит в --append-system-prompt
  schema.json               есть — поля ответа, ими же ответ и валидируется
  categories-seed.md        есть — заготовка листа «Статьи», в работе бота не участвует
  тестовые-чеки/            есть — три JPEG и примеры текстом
  tools/make_receipts.py    есть — генератор тестовых чеков

  config.py                 задача 1 — .env и settings.json, отказ при ключе API
  state.py                  задача 2 — state.json: offset, разобранные файлы, кэш, очередь
  tools/selfcheck.py        задачи 2 и 4 — самопроверка чистых функций
  engines/__init__.py       задача 3
  engines/claude_code.py    задача 3 — сборка команды и subprocess
  agent.py                  задача 3 — шов parse() + проверка формы ответа + запуск из терминала
  checks.py                 задача 4 — проверки без модели
  sheet.gs                  задача 5 — код для Apps Script
  sheet.py                  задача 5 — мост к таблице: справочник, строка, очередь
  bot.py                    задачи 6 и 7 — телеграм: приём, проверки, запись, ответ
  README.md                 задача 8 — запуск и что проверять

  .env / .env.example              секреты
  settings.json / settings.example.json   белый список и движок
  state.json                       служебная память бота
  чеки/входящие|готово|спорные/    создаются ботом сами
```

**Почему файлов больше, чем в разделе «Файлы проекта» базовой спеки.** Спека
перечисляет `agent.py` и `bot.py`; здесь добавлены `config.py`, `state.py`,
`sheet.py`, `checks.py` и папка `engines/`. Причины три, и все проверяемые:

1. `engines/` — прямое требование спеки панели: шов `agent.parse()` →
   `engines/claude_code.py`, чтобы срез 4 добавлялся одним файлом.
2. `checks.py` отдельно от `bot.py` — потому что проверки и есть суть урока
   («именно это спасает от уверенных ошибок агента»), и потому что чистые
   функции без телеграма внутри можно прогнать `tools/selfcheck.py`.
3. `config.py`, `state.py`, `sheet.py` — иначе `bot.py` превращается в файл,
   который на уроке по блокам не разобрать. Это та же раскладка, что в faq-bot
   (`config.py`, `db.py`, `listener.py`), — ученик увидит знакомую структуру.

---

### Задача 1: Каркас, настройки, отказ при ключе API

**Файлы:**
- Создать: `.gitignore`, `.env.example`, `settings.example.json`, `config.py`
- Создать вне гита: `.env`, `settings.json`
- Проверка: `.venv/bin/python config.py`

**Стыки:**
- Берёт: ничего, это первая задача.
- Отдаёт: `config.BASE_DIR`, `config.load_env()` → словарь с ключами
  `bot_token`, `sheet_url`, `sheet_secret`; `config.refuse_if_api_key()` → None
  или выход; `config.load_settings()` → словарь с ключами `engine`, `allowed`,
  `knocked`; `config.allowed_ids(settings)` → множество чисел.

- [ ] **Шаг 1: Завести репозиторий**

Проект пока не под гитом, а дальше каждая задача заканчивается коммитом.

```bash
cd чеки-бот
git init
```

- [ ] **Шаг 2: Написать `.gitignore`**

Секреты, настройки хозяина и чеки живых людей на GitHub не уезжают.

```
# секреты и настройки хозяина
.env
settings.json
state.json
state.json.tmp

# чеки живых людей
чеки/

# служебное
.superpowers/
__pycache__/
*.pyc
.venv/
.DS_Store
```

- [ ] **Шаг 3: Первый коммит — то, что уже лежит в папке**

```bash
git add .gitignore prompt.md schema.json categories-seed.md docs tools тестовые-чеки
git commit -m "chore: репозиторий, промпт, схема и тестовые чеки"
```

- [ ] **Шаг 4: Венв и зависимости**

В PATH сейчас питон из венва соседнего проекта — берём базовый явно.

```bash
/Library/Frameworks/Python.framework/Versions/3.13/bin/python3 -m venv .venv
.venv/bin/pip install --quiet requests python-dotenv
.venv/bin/python -c "import requests, dotenv; print('зависимости на месте')"
```

Ожидание: `зависимости на месте`.

- [ ] **Шаг 5: Написать `.env.example`**

```
# Токен бота из @BotFather
BOT_TOKEN=

# Адрес веб-приложения Apps Script, заканчивается на /exec
SHEET_URL=

# Секрет: ровно та же строка, что в первой строке sheet.gs
SHEET_SECRET=
```

- [ ] **Шаг 6: Написать `settings.example.json`**

Формат придуман не здесь: его же будет писать панель в срезе 3, поэтому менять
имена ключей нельзя. `knocked` в срезе 1 не заполняется — постучавшиеся
печатаются в терминал (задача 6).

```json
{
  "engine": "claude_code",
  "allowed": [
    {"id": 123456789, "username": "ваш-ник", "name": "Ваше имя"}
  ],
  "knocked": []
}
```

- [ ] **Шаг 7: Завести бота в @BotFather и заполнить `.env`**

Токен нужен уже в задаче 6, а `.env` должен быть непустым, чтобы проверить
`config.py` прямо сейчас. Адрес таблицы появится в задаче 5 — пока заглушка.

```bash
cp .env.example .env
chmod 600 .env
cp settings.example.json settings.json
```

В `.env` вписать настоящий `BOT_TOKEN` от @BotFather, а в две другие строки —
`SHEET_URL=не-настроено` и `SHEET_SECRET=не-настроено`. В `settings.json`
оставить пример как есть: свой настоящий номер человек узнает в задаче 6.

- [ ] **Шаг 8: Написать `config.py`**

```python
"""Настройки бота: секреты из .env, белый список и движок из settings.json.

Файлы разделены по признаку «что случится, если удалить». Потерять .env —
настраивать заново. Потерять settings.json — заново впускать людей. Поэтому
белый список не лежит в state.json: тот файл иногда приходится удалять, когда
бот запутался, и вместе со служебным файлом человек выкинул бы себя из
собственного бота.

.env читается один раз при старте: токен и адреса на ходу не меняются.
settings.json перечитывается перед каждым сообщением — правку белого списка
руками бот подхватывает без перезапуска. В срезе 3 этот же файл будет писать
панель, и перезапуск не понадобится там тоже.
"""

import json
import os
import sys
from pathlib import Path

from dotenv import load_dotenv

BASE_DIR = Path(__file__).resolve().parent
SETTINGS_PATH = BASE_DIR / "settings.json"

REQUIRED_ENV = ("BOT_TOKEN", "SHEET_URL", "SHEET_SECRET")

# Пустой белый список — безопасное состояние: бот не отвечает никому.
DEFAULTS = {"engine": "claude_code", "allowed": [], "knocked": []}


def load_env():
    """Секреты из .env. Не хватает строки — выходим с внятным текстом."""
    load_dotenv(BASE_DIR / ".env")
    missing = [name for name in REQUIRED_ENV if not os.getenv(name)]
    if missing:
        sys.exit(
            "В .env не заполнено: " + ", ".join(missing) + ".\n"
            "Возьмите образец: cp .env.example .env"
        )
    return {
        "bot_token": os.environ["BOT_TOKEN"],
        "sheet_url": os.environ["SHEET_URL"],
        "sheet_secret": os.environ["SHEET_SECRET"],
    }


def refuse_if_api_key():
    """Ключ API в окружении уводит Claude Code с подписки на поминутную оплату,
    и человек узнаёт об этом из счёта. Лучше не запуститься."""
    if os.getenv("ANTHROPIC_API_KEY"):
        sys.exit(
            "В окружении задан ANTHROPIC_API_KEY.\n"
            "С ним Claude Code пойдёт по счётчику мимо подписки.\n"
            "Уберите строку с ANTHROPIC_API_KEY из ~/.zshrc, закройте терминал,\n"
            "откройте заново и запустите бота ещё раз."
        )


def load_settings():
    """Белый список и движок. Читается перед каждым сообщением.

    Испорченный файл — не повод падать: возвращаем пустой белый список, бот
    молчит для всех, и причина видна в терминале."""
    try:
        data = json.loads(SETTINGS_PATH.read_text(encoding="utf-8"))
    except FileNotFoundError:
        print("settings.json не найден — бот никого не впустит. "
              "cp settings.example.json settings.json")
        return dict(DEFAULTS)
    except json.JSONDecodeError as error:
        print(f"settings.json испорчен ({error}) — бот никого не впустит, "
              "пока файл не починят.")
        return dict(DEFAULTS)
    settings = dict(DEFAULTS)
    settings.update(data)
    return settings


def allowed_ids(settings):
    """Числовые идентификаторы впущенных. Ник в телеграме есть не у всех,
    а номер есть всегда — и не меняется при замене бота."""
    ids = set()
    for person in settings.get("allowed", []):
        try:
            ids.add(int(person["id"]))
        except (KeyError, TypeError, ValueError):
            print(f"в settings.json строка без числового id, пропускаю: {person}")
    return ids


if __name__ == "__main__":
    env = load_env()
    refuse_if_api_key()
    settings = load_settings()
    print("токен бота:", env["bot_token"][:8] + "…")
    print("адрес таблицы:", env["sheet_url"])
    print("движок:", settings["engine"])
    print("впущено человек:", len(allowed_ids(settings)))
```

- [ ] **Шаг 9: Проверить, что настройки читаются**

```bash
.venv/bin/python config.py
```

Ожидание: четыре строки — начало токена с многоточием, `не-настроено`,
`claude_code`, `впущено человек: 1`.

- [ ] **Шаг 10: Проверить отказ при ключе API**

```bash
ANTHROPIC_API_KEY=sk-проверка .venv/bin/python config.py; echo "код выхода: $?"
```

Ожидание: текст про `~/.zshrc` и `код выхода: 1`. Это та самая защита, из-за
которой человек не узнает о поминутной оплате из счёта.

- [ ] **Шаг 11: Проверить, что испорченный settings.json не роняет бота**

```bash
cp settings.json settings.json.цел && echo "{сломано" > settings.json
.venv/bin/python config.py; mv settings.json.цел settings.json
```

Ожидание: строка «settings.json испорчен …», затем `впущено человек: 0`, код
выхода 0. Пустой белый список означает, что бот молчит, — это безопасно.

- [ ] **Шаг 12: Коммит**

```bash
git add .gitignore .env.example settings.example.json config.py
git commit -m "feat: настройки из .env и settings.json, отказ при ключе API"
```

---

### Задача 2: Служебная память в `state.json`

**Файлы:**
- Создать: `state.py`, `tools/selfcheck.py`
- Проверка: `.venv/bin/python tools/selfcheck.py`

**Стыки:**
- Берёт: ничего.
- Отдаёт: `state.STATE_PATH`, `state.EMPTY`, `state.SEEN_LIMIT`,
  `state.load()` → словарь с ключами `offset`, `seen`, `categories`,
  `categories_at`, `queue`, `refused`; `state.save(state)` → None;
  `state.seen(state, unique_id)` → да/нет; `state.remember(state, unique_id)` → None.

- [ ] **Шаг 1: Написать `state.py`**

```python
"""state.json — служебная память бота.

Хранит четыре вещи: offset телеграма (чтобы после перезапуска не разбирать всё
заново), уже разобранные файлы, последний прочитанный справочник статей и
очередь строк, не доехавших до таблицы. Плюс тех, кому уже отказали, — чтобы
не отвечать чужому дважды.

Этот файл можно удалять: ничего невосполнимого в нём нет. Белого списка здесь
намеренно нет, он живёт в settings.json.
"""

import json
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent
STATE_PATH = BASE_DIR / "state.json"

EMPTY = {
    "offset": 0,
    "seen": [],
    "categories": [],
    "categories_at": 0,
    "queue": [],
    "refused": [],
}

# Список разобранных файлов не должен расти бесконечно: он нужен, чтобы
# поймать чек, присланный дважды подряд, а не чтобы помнить всё за год.
SEEN_LIMIT = 500


def load():
    """Память бота. Файла нет или он испорчен — начинаем с чистой заготовки."""
    try:
        data = json.loads(STATE_PATH.read_text(encoding="utf-8"))
    except (FileNotFoundError, json.JSONDecodeError):
        return json.loads(json.dumps(EMPTY))
    state = json.loads(json.dumps(EMPTY))
    state.update(data)
    return state


def save(state):
    """Пишем через временный файл: Ctrl+C посреди записи не оставит огрызок,
    из которого бот потом не поднимется."""
    tmp = STATE_PATH.with_name("state.json.tmp")
    tmp.write_text(json.dumps(state, ensure_ascii=False, indent=2), encoding="utf-8")
    tmp.replace(STATE_PATH)


def seen(state, unique_id):
    """Этот файл уже разбирали? Ловится по file_unique_id из телеграма."""
    return unique_id in state["seen"]


def remember(state, unique_id):
    state["seen"].append(unique_id)
    del state["seen"][:-SEEN_LIMIT]
```

Копия заготовки делается через `json.loads(json.dumps(...))`, а не `dict(...)`:
внутри лежат списки, и поверхностная копия отдала бы всем вызовам один и тот же
список — дописав в очередь одного, дописали бы всем.

- [ ] **Шаг 2: Написать `tools/selfcheck.py`**

```python
"""Самопроверка чистых функций — тех, где тихая опечатка даёт кривые строки
в таблице.

Запуск: .venv/bin/python tools/selfcheck.py

Это не тесты проекта. Проект проверяется живьём, чеками и ботом, как написано
в спеке. Здесь только то, что можно проверить без телеграма, таблицы и модели:
служебная память и проверки без модели.
"""

import sys
import tempfile
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

import state

passed = 0
failed = []


def check(name, ok):
    global passed
    if ok:
        passed += 1
        print("  ✓", name)
    else:
        failed.append(name)
        print("  ✗", name)


print("служебная память")
with tempfile.TemporaryDirectory() as folder:
    state.STATE_PATH = Path(folder) / "state.json"

    check("пустого файла нет — отдаём заготовку", state.load() == state.EMPTY)

    memory = state.load()
    memory["offset"] = 42
    state.save(memory)
    check("offset переживает перезапуск", state.load()["offset"] == 42)

    state.STATE_PATH.write_text("{это не json", encoding="utf-8")
    check("испорченный файл не роняет бота", state.load() == state.EMPTY)

    memory = state.load()
    state.remember(memory, "AgAD123")
    check("файл запоминается", state.seen(memory, "AgAD123"))
    check("чужой файл не считается разобранным", not state.seen(memory, "AgAD999"))

    for number in range(600):
        state.remember(memory, f"file{number}")
    check("список разобранных не растёт бесконечно",
          len(memory["seen"]) == state.SEEN_LIMIT)

    first = state.load()
    first["queue"].append({"merchant": "проверка"})
    check("заготовка не общая на всех", state.load()["queue"] == [])

print()
if failed:
    print(f"провалено {len(failed)}: " + ", ".join(failed))
    sys.exit(1)
print(f"{passed} проверок, все прошли")
```

- [ ] **Шаг 3: Прогнать самопроверку — она должна упасть**

Проверка, которая не умеет падать, — не проверка. Сломайте `SEEN_LIMIT` на
время и убедитесь, что самопроверка это заметит:

```bash
sed -i '' 's/^SEEN_LIMIT = 500/SEEN_LIMIT = 5000/' state.py
.venv/bin/python tools/selfcheck.py; echo "код выхода: $?"
```

Ожидание: строка `✗ список разобранных не растёт бесконечно`, `провалено 1`,
`код выхода: 1`.

- [ ] **Шаг 4: Вернуть как было и прогнать снова**

```bash
sed -i '' 's/^SEEN_LIMIT = 5000/SEEN_LIMIT = 500/' state.py
.venv/bin/python tools/selfcheck.py; echo "код выхода: $?"
```

Ожидание: семь галок, `7 проверок, все прошли`, `код выхода: 0`.

- [ ] **Шаг 5: Коммит**

```bash
git add state.py tools/selfcheck.py
git commit -m "feat: служебная память в state.json и самопроверка"
```

---

### Задача 3: Агент — шов, движок Claude Code, запуск из терминала

Самая содержательная задача среза: после неё чек разбирается из терминала,
без телеграма и без таблицы.

**Файлы:**
- Создать: `engines/__init__.py`, `engines/claude_code.py`, `agent.py`
- Проверка: `.venv/bin/python agent.py тестовые-чеки/01-чёткий.jpg`

**Стыки:**
- Берёт: `categories-seed.md` и `prompt.md` с `schema.json` — они уже в проекте.
- Отдаёт: `agent.parse(kind, payload, categories, today, engine="claude_code")`
  → словарь по `schema.json`; `agent.AgentError`; `agent.build_task(...)` → строка;
  `agent.validate(answer)` → словарь; `agent.seed_categories()` → список строк;
  `engines.claude_code.run(kind, task)` → словарь; `engines.claude_code.EngineError`.
  `kind` — строка `"фото"` или `"текст"`. `payload` — абсолютный путь к файлу
  или текст сообщения. `today` — `datetime.date`.

- [ ] **Шаг 1: Создать `engines/__init__.py`**

```python
"""Движки. За этим швом живёт вызов конкретной модели — и только он."""
```

- [ ] **Шаг 2: Написать `engines/claude_code.py`**

```python
"""Claude Code в headless-режиме — единственный движок первой версии.

Три вещи здесь неочевидны и добыты прогонами, а не документацией.

1. `--agent` вместе со схемой не работает: схема молча игнорируется,
   structured_output приходит пустым, а модель пишет JSON в блоке кода и
   придумывает свои имена полей. Ошибки при этом не возникает. Поэтому мозг
   агента уходит в `--append-system-prompt`, а не в агента.
2. `--json-schema` принимает текст схемы, а не путь к файлу: на путь CLI
   отвечает «--json-schema is not valid JSON».
3. Фотографии разбираем на sonnet. На мятом чеке haiku в одном прогоне из
   четырёх прочитал 20.08 как 20.09 — и поставил себе высокую уверенность.
"""

import json
import subprocess
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent.parent
PROMPT_PATH = BASE_DIR / "prompt.md"
SCHEMA_PATH = BASE_DIR / "schema.json"

MODELS = {"фото": "sonnet", "текст": "haiku"}
TIMEOUT = 180


class EngineError(Exception):
    """Движок не ответил, ответил ошибкой или ответил не тем."""


def run(kind, task):
    """Зовёт claude и возвращает structured_output словарём.

    Команда собирается списком аргументов, а не строкой: в промпте есть кавычки
    и обратные кавычки, и шелл на них спотыкается — проверено."""
    command = [
        "claude",
        "-p", task,
        "--append-system-prompt", PROMPT_PATH.read_text(encoding="utf-8"),
        "--json-schema", SCHEMA_PATH.read_text(encoding="utf-8"),
        "--output-format", "json",
        "--model", MODELS[kind],
        "--allowedTools", "Read",
        "--permission-mode", "acceptEdits",
    ]
    try:
        done = subprocess.run(
            command, capture_output=True, text=True, timeout=TIMEOUT, cwd=BASE_DIR
        )
    except subprocess.TimeoutExpired:
        raise EngineError(f"движок молчал {TIMEOUT} секунд")

    if done.returncode != 0:
        raise EngineError(
            f"claude вышел с кодом {done.returncode}: {done.stderr.strip()[:300]}"
        )

    try:
        answer = json.loads(done.stdout)
    except json.JSONDecodeError:
        raise EngineError("ответ движка не разобрался как JSON: " + done.stdout[:300])

    if answer.get("is_error"):
        raise EngineError("движок ответил ошибкой: " + str(answer.get("result"))[:300])

    structured = answer.get("structured_output") or {}
    if not structured:
        # Схема отвалилась. Полный ответ — в терминал: без него не понять,
        # что именно сломалось, а поломка тихая и легко проходит незамеченной.
        print("пустой structured_output, полный ответ движка:")
        print(json.dumps(answer, ensure_ascii=False, indent=2))
    return structured
```

- [ ] **Шаг 3: Написать `agent.py`**

```python
"""Шов между ботом и движком.

bot.py зовёт parse() и получает словарь по schema.json. Какой движок стоит за
швом, бот не знает: в срезе 4 рядом ляжет engines/codex.py, и bot.py не
изменится ни строкой.

Проверка формы ответа живёт здесь, а не в движке. Флаг схемы может отвалиться
молча у любого вендора — у Claude Code это находка 3 базовой спеки, у Codex
тот же класс поломки описан багом openai/codex#15451. Страховать от этого
должен шов, а не отдельный движок.
"""

import json
import sys
from datetime import date
from pathlib import Path

from engines import claude_code
from engines.claude_code import EngineError

BASE_DIR = Path(__file__).resolve().parent

FIELDS = ("is_expense", "date", "amount", "currency", "merchant",
          "category", "payment", "confidence", "doubts", "reply")

ENGINES = {"claude_code": claude_code}


class AgentError(Exception):
    """Разобрать не вышло. Человеку — честный отказ, в терминал — причина."""


def parse(kind, payload, categories, today, engine="claude_code"):
    """kind — «фото» или «текст», payload — путь к файлу или текст сообщения.

    Одна повторная попытка: вызов иногда отваливается по таймауту, а второй
    заход обычно проходит. Больше двух не пробуем — каждый стоит около
    33 тысяч токенов подписки."""
    if engine not in ENGINES:
        raise AgentError(f"движок «{engine}» пока не поддержан")
    task = build_task(kind, payload, categories, today)
    for attempt in (1, 2):
        try:
            return validate(ENGINES[engine].run(kind, task))
        except (EngineError, AgentError) as error:
            print(f"попытка {attempt} не удалась: {error}")
            if attempt == 2:
                raise AgentError(str(error))


def build_task(kind, payload, categories, today):
    """Задание из трёх кусков: сегодняшняя дата, вход и справочник статей.

    Дата подставляется всегда — без неё «вчера» и «в пятницу» не во что
    превратить."""
    listing = "\n".join(f"- {category}" for category in categories)
    if kind == "фото":
        what = f"Разбери чек на фотографии. Файл: {payload}"
    else:
        what = f"Разбери сообщение о расходе: {payload}"
    return (
        f"Сегодня {today.isoformat()}.\n\n"
        f"{what}\n\n"
        f"Статья расхода — строго одна строка из этого справочника:\n{listing}\n"
    )


def validate(answer):
    """Форма ответа.

    Пустой structured_output означает, что схема отвалилась. Разбирать вместо
    этого текст из блока кода нельзя: модель придумает свои имена полей
    (payment_method вместо payment, currency потеряется), и в таблицу уедет
    мусор — молча."""
    if not isinstance(answer, dict) or not answer:
        raise AgentError("пустой structured_output — схема отвалилась")
    missing = [field for field in FIELDS if field not in answer]
    if missing:
        raise AgentError("в ответе нет полей: " + ", ".join(missing))
    if not isinstance(answer["is_expense"], bool):
        raise AgentError("is_expense не «да» и не «нет»")
    amount = answer["amount"]
    if amount is not None and (isinstance(amount, bool)
                               or not isinstance(amount, (int, float))):
        raise AgentError("amount не число и не null")
    return answer


def seed_categories():
    """Справочник для запуска из терминала.

    Бот берёт статьи из таблицы; здесь, пока таблицы нет, читаем заготовку
    урока из categories-seed.md."""
    lines = (BASE_DIR / "categories-seed.md").read_text(encoding="utf-8").splitlines()
    return [line[2:].strip() for line in lines if line.startswith("- ")]


if __name__ == "__main__":
    if len(sys.argv) < 2:
        sys.exit(
            'Как звать:\n'
            '  .venv/bin/python agent.py тестовые-чеки/01-чёткий.jpg\n'
            '  .venv/bin/python agent.py "такси 450 наличными"'
        )
    argument = sys.argv[1]
    path = Path(argument)
    if path.suffix.lower() in (".jpg", ".jpeg", ".png"):
        kind, payload = "фото", str(path.resolve())
    else:
        kind, payload = "текст", argument
    categories = seed_categories()
    print(f"вход: {kind}, статей в справочнике: {len(categories)} "
          f"(из categories-seed.md)")
    print(json.dumps(parse(kind, payload, categories, date.today()),
                     ensure_ascii=False, indent=2))
```

- [ ] **Шаг 4: Проверить, что справочник читается из заготовки**

```bash
.venv/bin/python -c "import agent; print(len(agent.seed_categories()), agent.seed_categories()[:3])"
```

Ожидание: `16 ['Продукты', 'Кафе и рестораны', 'Транспорт и такси']`.

- [ ] **Шаг 5: Проверить, что проверка формы ответа ловит отвалившуюся схему**

```bash
.venv/bin/python -c "
import agent
for bad in [{}, {'is_expense': True}, dict.fromkeys(agent.FIELDS, ''), 'строка']:
    try:
        agent.validate(bad)
        print('пропустил:', bad)
    except agent.AgentError as e:
        print('поймал:', e)
"
```

Ожидание: четыре строки «поймал: …» — про пустой structured_output, про
недостающие поля, про `is_expense не «да» и не «нет»` (в третьем случае все
поля на месте, но пустыми строками) и снова про пустой structured_output.
Ни одного «пропустил».

- [ ] **Шаг 6: Разобрать чёткий чек живьём**

```bash
.venv/bin/python agent.py тестовые-чеки/01-чёткий.jpg
```

Ожидание: 15–50 секунд ожидания, затем словарь с
`"amount": 1004.7`, `"date": "2026-08-20"`, `"merchant": "Пятёрочка"`,
`"category": "Продукты"`, `"payment": "карта"`, `"confidence": "высокая"`,
`"doubts": ""`. Это опорная точка: если здесь что-то не так, дело не в чеке,
а в настройке — сначала проверьте, что `structured_output` вообще непустой.

- [ ] **Шаг 7: Разобрать мятый чек два-три раза подряд**

```bash
for run in 1 2 3; do .venv/bin/python agent.py тестовые-чеки/02-мятый.jpg | grep -E '"date"|"amount"|"confidence"'; done
```

Ожидание: во всех прогонах `2026-08-20` и `1004.7` — файл разбирается на
sonnet. Расхождение между прогонами не считается провалом задачи: это свойство
модели, и ловить его будет задача 4. Провал — если пустой `structured_output`
или ошибка вызова.

- [ ] **Шаг 8: Разобрать обрезанный чек два-три раза подряд**

```bash
for run in 1 2 3; do .venv/bin/python agent.py тестовые-чеки/03-обрезанный.jpg | grep -E '"amount"|"confidence"|"doubts"'; done
```

Ожидание: `"amount": null`, `"confidence": "низкая"`, в `doubts` — что чек
обрезан и итога не видно. Продавец «Лукойл», статья «Топливо». Если в каком-то
прогоне подставилось `1819.2` — это ровно та ошибка, ради которой существуют
проверки задачи 4; записать её в отчёт о задаче, не «чинить» повтором.

- [ ] **Шаг 9: Прогнать пять текстовых примеров**

```bash
.venv/bin/python agent.py "такси 450 наличными"
.venv/bin/python agent.py "вчера бензин 3200"
.venv/bin/python agent.py "кофе 300, потом ещё такси 600"
.venv/bin/python agent.py "заплатил 12000"
.venv/bin/python agent.py "привет, а как этим пользоваться?"
```

Ожидание по порядку: 450 ₽, «Транспорт и такси», наличные, сегодняшняя дата —
**вчерашняя** дата и 3200 ₽, «Топливо», оплата «неизвестно» — `amount: null` и
просьба прислать двумя сообщениями — 12000 ₽ со статьёй «Прочее» и низкой
уверенностью — `is_expense: false` и подсказка, как оформить трату.

- [ ] **Шаг 10: Коммит**

```bash
git add engines agent.py
git commit -m "feat: шов агента и движок Claude Code, разбор чека из терминала"
```

---

### Задача 4: Проверки без модели

**Файлы:**
- Создать: `checks.py`
- Изменить: `tools/selfcheck.py` — добавить блок проверок перед итогом
- Проверка: `.venv/bin/python tools/selfcheck.py`

**Стыки:**
- Берёт: словарь, который вернул `agent.parse(...)`.
- Отдаёт: `checks.review(answer, categories, today)` → словарь с ключами
  `status` (`"готово"` или `"проверить"`), `category` (строка),
  `reasons` (список строк), `warnings` (список строк);
  `checks.is_number(value)` → да/нет; `checks.read_date(raw)` → `date` или `None`;
  `checks.BIG_AMOUNT`, `checks.FALLBACK_CATEGORY`.

- [ ] **Шаг 1: Написать `checks.py`**

```python
"""Проверки без модели — то, что спасает от уверенных ошибок агента.

Модель — не программа: один и тот же чек она читает по-разному и ошибается
уверенно. Поэтому ни одна проверка здесь не спрашивает у агента, уверен ли он.
«Высокая уверенность» на дате из будущего не отменяет ничего.
"""

from datetime import date, timedelta

BIG_AMOUNT = 100000
FALLBACK_CATEGORY = "Прочее"
YEAR = timedelta(days=365)


def review(answer, categories, today):
    """Смотрит на разобранную запись и возвращает словарь:

    status   — «готово» или «проверить»
    category — статья, уже приведённая к справочнику
    reasons  — почему «проверить»; пусто, если готово
    warnings — что сказать человеку, но статус это не меняет
    """
    reasons = []
    warnings = []

    day = read_date(answer.get("date"))
    if day is None:
        reasons.append("дата не прочиталась")
    elif day > today:
        # Так ловится ошибка находки 5: агент прочитал 20 сентября,
        # когда на дворе 23 августа, и был в себе уверен.
        reasons.append(f"дата {day.isoformat()} в будущем")
    elif day < today - YEAR:
        reasons.append(f"дата {day.isoformat()} старше года")

    amount = answer.get("amount")
    if not is_number(amount) or amount <= 0:
        reasons.append("сумма не прочиталась")
    elif amount > BIG_AMOUNT:
        # Крупная трата стоит взгляда, даже если чек читался идеально.
        reasons.append(f"крупная сумма — {amount:.2f}")

    if answer.get("confidence") == "низкая":
        reasons.append("агент не уверен в прочитанном")

    doubts = (answer.get("doubts") or "").strip()
    if doubts:
        warnings.append(doubts)

    # Статья не из справочника — рабочий сигнал, а не сомнение в строке:
    # значит, в справочнике не хватает статьи, и хозяин таблицы допишет её
    # строкой на листе «Статьи». Саму строку это не портит, поэтому статус
    # не меняется — человеку просто говорится, что было и что стало.
    category = (answer.get("category") or "").strip()
    if category not in categories:
        warnings.append(
            f"статьи «{category or 'пусто'}» нет в справочнике, "
            f"записал в «{FALLBACK_CATEGORY}»"
        )
        category = FALLBACK_CATEGORY

    return {
        "status": "проверить" if reasons else "готово",
        "category": category,
        "reasons": reasons,
        "warnings": warnings,
    }


def read_date(raw):
    """ГГГГ-ММ-ДД или None. Пустая строка — тоже None: агент честно сказал,
    что даты не видит."""
    try:
        return date.fromisoformat((raw or "").strip())
    except ValueError:
        return None


def is_number(value):
    """True у чисел, False у None и строк и — намеренно — у «да»: в питоне
    True это единица, и без этой оговорки «сумма: да» прошла бы как рубль."""
    return isinstance(value, (int, float)) and not isinstance(value, bool)
```

- [ ] **Шаг 2: Дописать блок в `tools/selfcheck.py`**

Импорт `checks` добавляется рядом с `import state`, а блок — сразу после блока
«служебная память», до `print()` с итогом.

```python
print()
print("проверки без модели")
today = date(2026, 8, 23)
categories = ["Продукты", "Топливо", "Прочее"]
clean = {
    "is_expense": True, "date": "2026-08-20", "amount": 1004.7,
    "currency": "RUB", "merchant": "Пятёрочка", "category": "Продукты",
    "payment": "карта", "confidence": "высокая", "doubts": "",
    "reply": "Пятёрочка, 1004,70 ₽, продукты",
}

verdict = checks.review(clean, categories, today)
check("чистый чек проходит как «готово»",
      verdict["status"] == "готово" and not verdict["reasons"])

verdict = checks.review(dict(clean, date="2026-09-20"), categories, today)
check("дата в будущем ловится при высокой уверенности",
      verdict["status"] == "проверить")

verdict = checks.review(dict(clean, date="2025-01-01"), categories, today)
check("дата старше года ловится", verdict["status"] == "проверить")

verdict = checks.review(dict(clean, date=""), categories, today)
check("пустая дата ловится", verdict["status"] == "проверить")

verdict = checks.review(dict(clean, amount=None), categories, today)
check("пустая сумма ловится", verdict["status"] == "проверить")

verdict = checks.review(dict(clean, amount=0), categories, today)
check("нулевая сумма ловится", verdict["status"] == "проверить")

verdict = checks.review(dict(clean, amount=True), categories, today)
check("«да» вместо суммы ловится", verdict["status"] == "проверить")

verdict = checks.review(dict(clean, amount=100000), categories, today)
check("ровно сто тысяч проходят", verdict["status"] == "готово")

verdict = checks.review(dict(clean, amount=100000.01), categories, today)
check("сумма больше ста тысяч ловится", verdict["status"] == "проверить")

verdict = checks.review(dict(clean, confidence="низкая"), categories, today)
check("низкая уверенность ловится", verdict["status"] == "проверить")

verdict = checks.review(dict(clean, category="Еда"), categories, today)
check("статья не из справочника заменяется на «Прочее»",
      verdict["category"] == "Прочее")
check("но статуса не меняет", verdict["status"] == "готово")
check("и человеку про это говорится",
      any("Еда" in warning for warning in verdict["warnings"]))

verdict = checks.review(dict(clean, doubts="время не видно"), categories, today)
check("сомнения агента доходят до человека",
      verdict["warnings"] == ["время не видно"])
check("но сами по себе статуса не меняют", verdict["status"] == "готово")
```

Наверх файла, к остальным импортам, добавляется:

```python
from datetime import date

import checks
```

- [ ] **Шаг 3: Убедиться, что новые проверки умеют падать**

```bash
sed -i '' 's/^BIG_AMOUNT = 100000/BIG_AMOUNT = 10000000/' checks.py
.venv/bin/python tools/selfcheck.py; echo "код выхода: $?"
```

Ожидание: `✗ сумма больше ста тысяч ловится`, `провалено 1`, `код выхода: 1`.

- [ ] **Шаг 4: Вернуть как было и прогнать**

```bash
sed -i '' 's/^BIG_AMOUNT = 10000000/BIG_AMOUNT = 100000/' checks.py
.venv/bin/python tools/selfcheck.py; echo "код выхода: $?"
```

Ожидание: `22 проверки, все прошли`, `код выхода: 0`.

- [ ] **Шаг 5: Проверить проверки на живом ответе агента**

```bash
.venv/bin/python -c "
from datetime import date
import agent, checks
cats = agent.seed_categories()
answer = agent.parse('фото', 'тестовые-чеки/03-обрезанный.jpg', cats, date.today())
print(checks.review(answer, cats, date.today()))
"
```

Ожидание: `status` = `проверить`, в `reasons` — «сумма не прочиталась» и
«агент не уверен в прочитанном». Путь передаём как есть: `agent.parse` отдаёт
его модели строкой, а Read читает относительно папки проекта.

- [ ] **Шаг 6: Коммит**

```bash
git add checks.py tools/selfcheck.py
git commit -m "feat: проверки без модели и самопроверка на них"
```

---

### Задача 5: Таблица, мост Apps Script и `sheet.py`

**Файлы:**
- Создать: `sheet.gs`, `sheet.py`
- Изменить: `.env` — настоящие `SHEET_URL` и `SHEET_SECRET` вместо заглушек
- Проверка: `.venv/bin/python sheet.py`

**Стыки:**
- Берёт: `config.load_env()`, `state.load()` (ключи `categories`,
  `categories_at`, `queue`).
- Отдаёт: `sheet.categories(state, url, secret)` → `(список, откуда)`, где
  «откуда» — `"таблица"`, `"кэш"`, `"запас"` или `"нет"`;
  `sheet.fetch_categories(url, secret)` → список;
  `sheet.append_row(url, secret, row)` → номер строки;
  `sheet.deliver(state, url, secret, row)` → да/нет;
  `sheet.flush(state, url, secret)` → сколько строк доехало; `sheet.SheetError`.
  `row` — словарь с ключами `date`, `amount`, `currency`, `merchant`,
  `category`, `payment`, `source`, `who`, `status`, `file`.

- [ ] **Шаг 1: Завести таблицу с двумя листами**

Новая Google-таблица. Лист «Расходы», в первой строке одиннадцать заголовков:

```
дата операции · сумма · валюта · продавец / на что · статья расхода ·
способ оплаты · источник · кто прислал · статус · файл · записано
```

Второй лист назвать «Статьи», в A1 написать «Статья», а с A2 вставить
шестнадцать строк из `categories-seed.md` — по строке на статью. «Прочее»
обязано быть в списке: в него уезжает всё, что агент не смог отнести никуда.

- [ ] **Шаг 2: Придумать секрет**

```bash
.venv/bin/python -c "import secrets; print(secrets.token_urlsafe(16))"
```

Полученную строку вписать в `.env` в `SHEET_SECRET` и в первую строку
`sheet.gs` на следующем шаге. Адрес веб-приложения — это и есть ключ от
таблицы: кто его знает, тот может дописывать строки. Поэтому и в POST, и в GET
идёт ещё и секрет, а сам адрес не показывается на экране во время урока.

- [x] **Шаг 3: `sheet.gs` уже написан — НЕ ПЕРЕЗАПИСЫВАТЬ**

> **Стоп.** Файл `sheet.gs` написан и проверен отдельной сессией
> (`course-halper-ba`): 123 строки, прогон на заглушках Apps Script проходит.
> Не создавать его заново, не приводить к образцу, не «восстанавливать по плану».
> При расхождении ожидаемого с фактическим — остановиться и спросить, а не править.
>
> Черновик, который стоял здесь раньше, **удалён намеренно**. 2 сентября подагент
> взял его отсюда и затёр им рабочий файл; тот был неотслеживаемым, и дословно
> вернуть его не удалось — выручила случайная копия у автора. Второй раз копии
> может не оказаться. Пока в шаге лежал код, шаг был миной.

Чего рабочая версия умеет сверх того черновика — это не оформление, и терять
это нельзя:

- **Отказы отвечают JSON, а не падают.** Нет листа «Расходы» или «Статьи», тело
  запроса не разбирается, на листе ноль непустых статей — на каждый случай
  внятный ответ с названием того, чего не хватает. Без этого Apps Script при
  ошибке отдаёт HTML-страницу, и `sheet.py` видит мусор вместо причины.
- **Строка секрета помечена комментарием и содержит заглушку `__СЕКРЕТ__`**,
  рассчитанную на замену текстом. На этом стоит шаг 2 мастера установки
  (срез 5): секрет придумывает программа и вписывает его в код за ученика,
  чтобы тот не изобретал «12345» и не забыл вставить значение в оба места.

`sheet-setup.md` документирует тексты отказа именно рабочей версии, поэтому
подмена файла рассогласует ещё и инструкцию.

Сверять `sheet.py` следует с фактическим `sheet.gs` на диске, а не с гитом:
в коммите `ecd6c80` лежит затёртый черновик, файл на диске новее.

- [ ] **Шаг 4: Опубликовать веб-приложение**

Расширения → Apps Script → вставить содержимое `sheet.gs`, заменив
`придумайте-свою-строку` на секрет из шага 2 → Развернуть → Веб-приложение,
«Запуск от имени: я», «Доступ: все». Полученный адрес (заканчивается на `/exec`)
вписать в `.env` в `SHEET_URL`.

Секрет в `sheet.gs` и в `.env` должен совпадать посимвольно. Сам `sheet.gs` в
репозитории остаётся с заглушкой `придумайте-свою-строку`: это файл для урока,
а не место для боевого секрета.

- [ ] **Шаг 5: Написать `sheet.py`**

```python
"""Мост к Google-таблице через Apps Script.

Google Cloud, сервисные аккаунты и файлы с ключами не нужны: таблица сама
отвечает на два запроса — «прими строку» и «отдай справочник».

Веб-приложение Apps Script отвечает редиректом на script.googleusercontent.com.
requests переходит по нему сам, но allow_redirects отключать нельзя — иначе
вместо ответа придёт пустой 302.
"""

import sys
import time

import requests

TIMEOUT = 20
CACHE_SECONDS = 300


class SheetError(Exception):
    """Таблица не ответила или ответила не «ok»."""


def fetch_categories(url, secret):
    """Справочник статей прямо из таблицы, без кэша."""
    answer = ask("GET", url, params={"secret": secret})
    found = [str(item).strip() for item in answer.get("categories", [])
             if str(item).strip()]
    if not found:
        raise SheetError("лист «Статьи» пуст или переименован")
    return found


def categories(state, url, secret):
    """Справочник с кэшем на пять минут и запасом из state.json.

    Возвращает (список, откуда). Кэш нужен, чтобы не дёргать таблицу на каждое
    сообщение; правка списка подхватывается сама, без перезапуска бота, —
    следующий чек после истечения кэша разберётся по-новому.

    Пустой список означает, что разбирать нельзя. Придумывать статьи агенту
    запрещено: в отчёте заведутся «Продукты», «Продукты питания» и «Еда»
    вместо одной строки."""
    now = time.time()
    if state["categories"] and now - state["categories_at"] < CACHE_SECONDS:
        return state["categories"], "кэш"
    try:
        found = fetch_categories(url, secret)
    except (SheetError, requests.RequestException) as error:
        if state["categories"]:
            print(f"справочник не прочитался ({error}) — работаю по последнему списку")
            return state["categories"], "запас"
        return [], "нет"
    state["categories"] = found
    state["categories_at"] = now
    return found, "таблица"


def append_row(url, secret, row):
    """Одна строка на лист «Расходы». Номер строки — в ответе."""
    answer = ask("POST", url, json=dict(row, secret=secret))
    return answer.get("row")


def deliver(state, url, secret, row):
    """Пишет строку, а не вышло — кладёт в очередь. True, если строка в таблице."""
    try:
        append_row(url, secret, row)
        return True
    except (SheetError, requests.RequestException) as error:
        print(f"таблица не приняла строку ({error}) — отложил в очередь")
        state["queue"].append(row)
        return False


def flush(state, url, secret):
    """Отдаёт отложенные строки по одной, в том же порядке. Сколько доехало —
    столько и вернёт; на первой неудаче останавливается."""
    delivered = 0
    while state["queue"]:
        try:
            append_row(url, secret, state["queue"][0])
        except (SheetError, requests.RequestException):
            break
        state["queue"].pop(0)
        delivered += 1
    return delivered


def ask(method, url, **kwargs):
    response = requests.request(method, url, timeout=TIMEOUT,
                                allow_redirects=True, **kwargs)
    response.raise_for_status()
    try:
        answer = response.json()
    except ValueError:
        raise SheetError("таблица ответила не JSON — проверьте, что доступ «все» "
                         "и что адрес заканчивается на /exec")
    if not answer.get("ok"):
        raise SheetError(answer.get("error", "таблица ответила не ok"))
    return answer


if __name__ == "__main__":
    import config

    env = config.load_env()
    if len(sys.argv) > 1 and sys.argv[1] == "строка":
        number = append_row(env["sheet_url"], env["sheet_secret"], {
            "date": "2026-09-01", "amount": 1, "currency": "RUB",
            "merchant": "проверка связи", "category": "Прочее",
            "payment": "неизвестно", "source": "проверка", "who": "терминал",
            "status": "проверить", "file": "",
        })
        print(f"записал строку {number} — удалите её из таблицы руками")
    else:
        found = fetch_categories(env["sheet_url"], env["sheet_secret"])
        print(f"статей в справочнике: {len(found)}")
        for item in found:
            print(" -", item)
```

- [ ] **Шаг 6: Проверить, что справочник читается**

```bash
.venv/bin/python sheet.py
```

Ожидание: `статей в справочнике: 16` и список, начинающийся с «Продукты».
Если пришло «таблица ответила не JSON» — доступ у веб-приложения не «все» или
в `.env` попал адрес `/dev` вместо `/exec`.

- [ ] **Шаг 7: Проверить, что строка записывается**

```bash
.venv/bin/python sheet.py строка
```

Ожидание: `записал строку 2` (или другой номер), в таблице на листе «Расходы»
появилась строка «проверка связи» с проставленным временем в колонке
«записано». Строку удалить руками.

- [ ] **Шаг 8: Проверить, что чужой секрет не пускают**

```bash
.venv/bin/python -c "
import sheet
try:
    sheet.fetch_categories('$(grep SHEET_URL .env | cut -d= -f2-)', 'не-тот-секрет')
except sheet.SheetError as e:
    print('поймал:', e)
"
```

Ожидание: `поймал: нет доступа`.

- [ ] **Шаг 9: Коммит**

Перед коммитом убедиться, что в `sheet.gs` осталась заглушка, а не боевой
секрет: `grep SECRET sheet.gs`.

```bash
git add sheet.gs sheet.py
git commit -m "feat: мост к таблице через Apps Script"
```

---

### Задача 6: Телеграм — приём, белый список, offset

После этой задачи бот отвечает своему хозяину и молчит для чужих. Чеки он ещё
не разбирает — это задача 7.

**Файлы:**
- Создать: `bot.py`
- Проверка: `.venv/bin/python bot.py` и переписка с ботом в телеграме

**Стыки:**
- Берёт: `config.load_env()`, `config.refuse_if_api_key()`,
  `config.load_settings()`, `config.allowed_ids()`, `state.load()`,
  `state.save()`.
- Отдаёт: `bot.call(token, method, **params)` → результат телеграма;
  `bot.get_updates(token, offset)` → список; `bot.say(token, chat_id, text)` → None;
  `bot.who(message)` → строка; `bot.handle(env, st, message)` → None
  (в задаче 7 переписывается целиком); `bot.knock(env, st, message)` → None;
  `bot.main()` → None.

- [ ] **Шаг 1: Написать `bot.py`**

```python
"""Телеграм: приём сообщений, белый список, ответы.

Мозг и руки разделены. Этот файл ничего не понимает про чеки — он их
принимает, проверяет и записывает. Понимает agent.py.
"""

import json
import time

import requests

import config
import state as memory

API = "https://api.telegram.org/bot{token}/{method}"
POLL_SECONDS = 30


def call(token, method, **params):
    """Один вызов Bot API. Ждём чуть дольше, чем длится долгий опрос."""
    response = requests.post(
        API.format(token=token, method=method), json=params,
        timeout=POLL_SECONDS + 35,
    )
    response.raise_for_status()
    answer = response.json()
    if not answer.get("ok"):
        raise RuntimeError(answer.get("description", "телеграм ответил не ok"))
    return answer["result"]


def get_updates(token, offset):
    return call(token, "getUpdates", offset=offset, timeout=POLL_SECONDS,
                allowed_updates=["message"])


def say(token, chat_id, text):
    """Ответ в чат. Не дошёл — это не повод ронять бота."""
    try:
        call(token, "sendMessage", chat_id=chat_id, text=text)
    except (requests.RequestException, RuntimeError) as error:
        print(f"не смог ответить в чат {chat_id}: {error}")


def who(message):
    """Как называть человека в таблице. Ник есть не у всех, имя есть всегда."""
    sender = message.get("from", {})
    if sender.get("username"):
        return "@" + sender["username"]
    return sender.get("first_name") or "неизвестно"


def knock(env, st, message):
    """Чужому отвечаем один раз и молчим дальше.

    В терминал печатается готовая строка для settings.json: в срезе 3 то же
    самое будет делать панель кнопкой «Впустить»."""
    sender = message.get("from", {})
    sender_id = sender.get("id")
    if sender_id in st["refused"]:
        return
    st["refused"].append(sender_id)
    line = {"id": sender_id, "username": sender.get("username", ""),
            "name": sender.get("first_name", "")}
    print(f"постучался чужой: {who(message)}")
    print("  впустить — добавьте эту строку в settings.json, в список allowed:")
    print("  " + json.dumps(line, ensure_ascii=False))
    say(env["bot_token"], message["chat"]["id"],
        "Этот бот записывает расходы своего хозяина. "
        "Если он вас ждёт — передайте ему, что вы написали.")


def handle(env, st, message):
    """Пока только белый список. Разбор чеков появится в следующей задаче."""
    settings = config.load_settings()
    if message.get("from", {}).get("id") not in config.allowed_ids(settings):
        knock(env, st, message)
        return
    say(env["bot_token"], message["chat"]["id"],
        "Вижу вас. Чеки научусь разбирать в следующей задаче.")


def main():
    env = config.load_env()
    config.refuse_if_api_key()
    st = memory.load()
    print("Бот запущен, жду сообщений. Ctrl+C — выход.")
    while True:
        try:
            updates = get_updates(env["bot_token"], st["offset"])
        except (requests.RequestException, RuntimeError) as error:
            # Телеграм не отвечает — ждём и повторяем, offset не двигаем.
            print(f"телеграм не отвечает ({error}), жду пять секунд")
            time.sleep(5)
            continue
        for update in updates:
            st["offset"] = update["update_id"] + 1
            message = update.get("message")
            if message:
                try:
                    handle(env, st, message)
                except Exception as error:
                    # Одно сломанное сообщение не должно останавливать бота:
                    # offset уже сдвинут, следующее разберётся.
                    print(f"сообщение не обработалось: {error}")
            memory.save(st)


if __name__ == "__main__":
    main()
```

- [ ] **Шаг 2: Запустить бота и написать ему**

```bash
.venv/bin/python bot.py
```

Написать боту в личку что угодно. В `settings.json` сейчас лежит пример с
`"id": 123456789` — то есть вы сами пока чужой.

Ожидание: в терминале «постучался чужой: @ваш-ник» и готовая строка JSON;
в телеграме — один ответ про хозяина.

- [ ] **Шаг 3: Проверить, что чужому отвечают один раз**

Написать боту ещё два сообщения.

Ожидание: в чате ответа больше нет, в терминале новых строк нет.

- [ ] **Шаг 4: Впустить себя, не останавливая бота**

Вставить строку из терминала в `settings.json`, в список `allowed`, вместо
примера с `123456789`. Бота **не перезапускать**. Написать ему ещё раз.

Ожидание: «Вижу вас. Чеки научусь разбирать в следующей задаче.» Это и есть
проверка того, что `settings.json` перечитывается перед каждым сообщением, —
в срезе 3 на этом же держится панель.

- [ ] **Шаг 5: Проверить, что offset переживает перезапуск**

Остановить бота (Ctrl+C), написать ему два сообщения, пока он лежит, запустить
снова.

Ожидание: при запуске бот разбирает оба накопившихся сообщения — и ровно по
одному разу. Остановить, запустить ещё раз: старые сообщения повторно не
разбираются.

- [ ] **Шаг 6: Проверить, что чужой аккаунт не пускают**

Написать боту со второго телеграм-аккаунта.

Ожидание: в терминале новая строка «постучался чужой», в чате один ответ,
в белый список никто сам не попал.

- [ ] **Шаг 7: Коммит**

```bash
git add bot.py
git commit -m "feat: приём сообщений, белый список из settings.json"
```

---

### Задача 7: Полный конвейер — фото и текст доезжают до таблицы

**Файлы:**
- Изменить: `bot.py` — новые импорты и константы, `handle()` переписывается
  целиком, добавляются `extract`, `download_photo`, `move`, `safe`, `reply_text`
- Проверка: переписка с ботом в телеграме и строки в таблице

**Стыки:**
- Берёт: `agent.parse`, `agent.AgentError`, `checks.review`, `checks.is_number`,
  `sheet.categories`, `sheet.deliver`, `sheet.flush`, `memory.seen`,
  `memory.remember` — всё уже написано в задачах 2–5.
- Отдаёт: рабочего бота. Наружу ничего нового не экспортирует.

- [ ] **Шаг 1: Добавить импорты и константы в начало `bot.py`**

Сразу под существующие импорты:

```python
from datetime import date, datetime
from pathlib import Path

import agent
import checks
import sheet
```

Под строку `POLL_SECONDS = 30`:

```python
INBOX = config.BASE_DIR / "чеки" / "входящие"
DONE = config.BASE_DIR / "чеки" / "готово"
DOUBTFUL = config.BASE_DIR / "чеки" / "спорные"
```

- [ ] **Шаг 2: Добавить работу с файлами и разбор входящего сообщения**

Рядом с `who()`:

```python
def extract(message):
    """Любой вход сводится к паре «что разбирать» и «откуда пришло».

    Веток две: фотография и текст. Голосовые в проекте не разбираются —
    расшифровки в Bot API нет, и решение это окончательное."""
    if message.get("photo"):
        # Подпись под фотографией в первой версии не читается: чек говорит
        # сам за себя, а спорить с ним подписью — отдельная история.
        largest = max(message["photo"],
                      key=lambda size: size.get("file_size")
                      or size.get("width", 0) * size.get("height", 0))
        return {"kind": "фото", "photo": largest}
    text = (message.get("text") or "").strip()
    if text:
        return {"kind": "текст", "text": text}
    return None


def safe(name):
    """Ник в имя файла: буквы и цифры оставляем, остальное — подчёркивание."""
    cleaned = "".join(ch if ch.isalnum() else "_" for ch in name).strip("_")
    return cleaned or "кто-то"


def download_photo(token, photo, sender):
    """Качаем самый крупный размер в чеки/входящие/ и возвращаем путь."""
    info = call(token, "getFile", file_id=photo["file_id"])
    url = f"https://api.telegram.org/file/bot{token}/{info['file_path']}"
    response = requests.get(url, timeout=60)
    response.raise_for_status()
    INBOX.mkdir(parents=True, exist_ok=True)
    stamp = datetime.now().strftime("%Y-%m-%d_%H%M%S")
    path = INBOX / f"{stamp}_{safe(sender)}.jpg"
    path.write_bytes(response.content)
    return path


def move(path, folder):
    """Файл едет в готово/ только при статусе «готово», иначе в спорные/."""
    folder.mkdir(parents=True, exist_ok=True)
    target = folder / path.name
    path.replace(target)
    return target


def reply_text(answer, verdict, written):
    """Одна фраза от агента плюс то, что заметили проверки."""
    if not written:
        head = "Разобрал, но в таблицу не попало — попробую позже"
    elif verdict["status"] == "готово":
        head = "Записал"
    else:
        head = "Записал с пометкой «проверить»"
    lines = [f"{head}: {answer['reply']}"]
    lines += ["— " + reason for reason in verdict["reasons"]]
    lines += ["— " + warning for warning in verdict["warnings"]]
    return "\n".join(lines)
```

- [ ] **Шаг 3: Переписать `handle()` целиком**

```python
def handle(env, st, message):
    """Путь сообщения от телеграма до строки в таблице."""
    settings = config.load_settings()
    chat_id = message["chat"]["id"]
    if message.get("from", {}).get("id") not in config.allowed_ids(settings):
        knock(env, st, message)
        return

    incoming = extract(message)
    if incoming is None:
        say(env["bot_token"], chat_id,
            "Пришлите фотографию чека или напишите тратой: что, сколько и чем "
            "платили. Например: обед 850 картой")
        return

    # Отложенные строки уезжают при первой возможности — раньше, чем новая.
    delivered = sheet.flush(st, env["sheet_url"], env["sheet_secret"])
    if delivered:
        say(env["bot_token"], chat_id,
            f"Отложенные строки доехали до таблицы: {delivered}.")

    categories, source = sheet.categories(st, env["sheet_url"], env["sheet_secret"])
    if not categories:
        say(env["bot_token"], chat_id,
            "Не вижу справочник статей в таблице — разбирать не буду. Иначе "
            "в отчёте заведутся выдуманные статьи. Проверьте лист «Статьи».")
        return
    if source == "запас":
        say(env["bot_token"], chat_id,
            "Таблица не отдала справочник, работаю по последнему известному "
            "списку статей.")

    path = None
    unique_id = None
    if incoming["kind"] == "фото":
        unique_id = incoming["photo"]["file_unique_id"]
        if memory.seen(st, unique_id):
            say(env["bot_token"], chat_id, "Этот чек уже записан.")
            return
        try:
            path = download_photo(env["bot_token"], incoming["photo"], who(message))
        except (requests.RequestException, RuntimeError) as error:
            print(f"не скачал фотографию: {error}")
            say(env["bot_token"], chat_id,
                "Не смог скачать фотографию, пришлите ещё раз.")
            return
        # Ответа ждать 15–50 секунд, человеку надо сказать, что мы живы.
        say(env["bot_token"], chat_id, "Смотрю чек…")
        payload = str(path)
    else:
        payload = incoming["text"]

    today = date.today()
    try:
        answer = agent.parse(incoming["kind"], payload, categories, today,
                             settings["engine"])
    except agent.AgentError as error:
        # Файл остаётся во входящих: разберём, когда починится.
        print(f"агент не справился: {error}")
        say(env["bot_token"], chat_id,
            "Не смог разобрать. Попробуйте ещё раз или напишите текстом.")
        return

    if not answer["is_expense"]:
        # В таблицу не пишем вообще. Файл не помечаем разобранным: если это
        # было меню, человек может прислать настоящий чек тем же файлом.
        if path:
            move(path, DOUBTFUL)
        say(env["bot_token"], chat_id, answer["reply"])
        return

    verdict = checks.review(answer, categories, today)
    if path:
        path = move(path, DONE if verdict["status"] == "готово" else DOUBTFUL)

    row = {
        "date": answer["date"],
        "amount": answer["amount"] if checks.is_number(answer["amount"]) else "",
        "currency": answer["currency"],
        "merchant": answer["merchant"],
        "category": verdict["category"],
        "payment": answer["payment"],
        "source": "фото чека" if incoming["kind"] == "фото" else "текст",
        "who": who(message),
        "status": verdict["status"],
        "file": path.name if path else "",
    }
    written = sheet.deliver(st, env["sheet_url"], env["sheet_secret"], row)
    if unique_id:
        memory.remember(st, unique_id)
    say(env["bot_token"], chat_id, reply_text(answer, verdict, written))
```

- [ ] **Шаг 4: Проверить, что ничего не сломалось в чистых функциях**

```bash
.venv/bin/python tools/selfcheck.py
```

Ожидание: `22 проверки, все прошли`.

- [ ] **Шаг 5: Прислать боту три тестовых чека**

Запустить бота и отправить в личку `01-чёткий.jpg`, `02-мятый.jpg`,
`03-обрезанный.jpg` — по одному, дождавшись ответа.

Ожидание:
- чёткий: «Записал: Пятёрочка, 1004,70 ₽…», в таблице строка со статусом
  «готово», файл в `чеки/готово/`;
- мятый: то же самое — либо, если модель прочитала дату как сентябрьскую,
  «Записал с пометкой «проверить»» и строка «— дата 2026-09-20 в будущем».
  Оба исхода правильные: проверка сработала;
- обрезанный: «Записал с пометкой «проверить»», в причинах «сумма не
  прочиталась», в таблице пустая ячейка суммы, файл в `чеки/спорные/`.

Отдельно посмотреть на колонку «источник» — во всех трёх строках «фото чека».

- [ ] **Шаг 6: Прислать текстовые примеры**

Отправить боту `такси 450 наличными`, `вчера бензин 3200`, `заплатил 12000`,
`привет, а как этим пользоваться?`.

Ожидание: три строки в таблице с источником «текст» (у «заплатил 12000» —
статус «проверить» и статья «Прочее»), на «привет» строки нет, в чат ушла
подсказка, как оформить трату.

- [ ] **Шаг 7: Прислать один и тот же чек дважды**

Ожидание: второй раз «Этот чек уже записан», новой строки в таблице нет,
нового файла в папках нет.

- [ ] **Шаг 8: Погасить интернет на середине**

Отправить чек и выключить Wi-Fi сразу после ответа «Смотрю чек…». Дождаться
сообщения про очередь в терминале, включить сеть обратно и отправить боту
любое сообщение.

Ожидание: бот не упал; в чате «Разобрал, но в таблицу не попало — попробую
позже», затем «Отложенные строки доехали до таблицы: 1»; в таблице строка на
месте и не задвоилась.

- [ ] **Шаг 9: Коммит**

```bash
git add bot.py
git commit -m "feat: фото и текст доезжают до таблицы, файлы расходятся по папкам"
```

---

### Задача 8: Приёмка по спеке и README

**Файлы:**
- Создать: `README.md`
- Проверка: восемь пунктов раздела «Как проверяем» базовой спеки

**Стыки:**
- Берёт: всё, что сделано в задачах 1–7.
- Отдаёт: срез, который можно показать группе.

- [ ] **Шаг 1: Прогнать справочник на живой правке**

Дописать на лист «Статьи» строку «Подарки», подождать пять минут (кэш) и
отправить боту `подарок коллеге 1500 картой`.

Ожидание: статья «Подарки», перезапуска бота не потребовалось.

- [ ] **Шаг 2: Прогнать отказ при пропавшем справочнике**

Переименовать лист «Статьи» в «Статьи2» и отправить боту любую трату.

Ожидание: пока не истёк кэш — бот работает как ни в чём не бывало; после
истечения — сообщение «Таблица не отдала справочник, работаю по последнему
известному списку статей», строки при этом с настоящими статьями, а не
выдуманными. Вернуть имя листа обратно.

- [ ] **Шаг 3: Проверить отказ, когда запаса нет**

```bash
.venv/bin/python -c "
import json, state
st = state.load(); st['categories'] = []; st['categories_at'] = 0; state.save(st)
print('кэш очищен')
"
```

Переименовать лист «Статьи», перезапустить бота, отправить трату.

Ожидание: «Не вижу справочник статей в таблице — разбирать не буду…», в таблице
ничего не появилось. Вернуть имя листа.

- [ ] **Шаг 4: Написать `README.md`**

````markdown
# Чеки в таблицу

Телеграм-бот принимает фотографию чека или сообщение «такси 450 наличными» и
дописывает строку расхода в Google-таблицу. Мозгом служит Claude Code по
подписке — ключ API не нужен.

## Запуск

```bash
cd чеки-бот
/Library/Frameworks/Python.framework/Versions/3.13/bin/python3 -m venv .venv
.venv/bin/pip install requests python-dotenv

cp .env.example .env            # токен бота, адрес Apps Script, секрет
chmod 600 .env
cp settings.example.json settings.json

.venv/bin/python bot.py
```

Бот работает, пока открыт терминал. Закрыли ноутбук — бот молчит, сообщения
копятся в телеграме и разберутся при следующем запуске.

## Как впустить себя

Напишите своему боту. В терминале появится готовая строка — вставьте её в
`settings.json`, в список `allowed`, и напишите ещё раз. Перезапускать не надо.

## Файлы

| Файл | Что |
|---|---|
| `prompt.md` | мозг агента: что искать в чеке и когда не угадывать |
| `schema.json` | поля ответа, ими же ответ и проверяется |
| `agent.py` | шов: зовёт движок и проверяет форму ответа |
| `engines/claude_code.py` | вызов Claude Code |
| `checks.py` | проверки без модели — главное место проекта |
| `bot.py` | телеграм: приём, скачивание, запись, ответ |
| `sheet.py` / `sheet.gs` | мост к Google-таблице |
| `config.py` | `.env` и `settings.json` |
| `state.py` | служебная память; этот файл можно удалять |

## Проверки

```bash
.venv/bin/python tools/selfcheck.py                    # чистые функции
.venv/bin/python agent.py тестовые-чеки/01-чёткий.jpg  # агент живьём
.venv/bin/python sheet.py                              # связь с таблицей
```

Живьём проверяется по разделу «Как проверяем» в
`docs/superpowers/specs/2026-08-23-чеки-в-таблицу-design.md`.

## Чего бот не делает

Не разбирает голосовые: расшифровки в Bot API нет, а локальная ломает
экономику проекта. Не работает на сервере круглосуточно, не редактирует уже
записанные строки, не считает суммы за месяц — для этого есть таблица.
````

- [ ] **Шаг 5: Пройти все восемь пунктов приёмки подряд**

Из раздела «Как проверяем» базовой спеки. Отметить каждый:

1. Три тестовых чека через `agent.py`, по два-три прогона каждый.
2. Пять текстовых примеров, включая «привет».
3. Те же файлы боту в личку: строки в таблице, файлы в `готово/` и `спорные/`.
4. Один и тот же чек дважды — второй раз строки нет.
5. Сообщение с чужого аккаунта — строки нет.
6. Интернет гаснет на середине — бот не падает, строка доезжает.
7. Новая статья на листе «Статьи» подхватывается без перезапуска.
8. Лист «Статьи» переименован — бот работает на кэше и говорит об этом.

Пункты 1–2 сделаны в задаче 3, 3–4 и 6 в задаче 7, 5 в задаче 6, 7–8 в шагах
1–3 этой задачи. Здесь они проходятся ещё раз, подряд, на собранном боте.

- [ ] **Шаг 6: Убедиться, что секретов нет в репозитории**

```bash
git status --short
git ls-files | grep -E "^(\.env|settings\.json|state\.json)$" && echo "СЕКРЕТ В ГИТЕ" || echo "секретов в гите нет"
grep -n "SECRET" sheet.gs
ls -l .env
```

Ожидание: `секретов в гите нет`, в `sheet.gs` заглушка
`придумайте-свою-строку`, права на `.env` — `-rw-------`.

- [ ] **Шаг 7: Коммит**

```bash
git add README.md
git commit -m "docs: README со запуском, файлами и проверками"
```

---

## Что этот срез намеренно не делает

Чтобы не приняли за недоделку:

- **Мастера установки и панели нет** — это срезы 2 и 3. Настройки правятся
  руками, и это записано в роадмапе.
- **Второго движка нет** — срез 4, и он ждёт установленного Codex, а не кода.
  Шов при этом готов: `agent.ENGINES` и папка `engines/`.
- **Голосовых нет и не будет.** Причина записана в спеке панели, раздел
  «Голосовые сообщения».
- **Склейки вызовов нет** — по одному чеку нагляднее на уроке. Записано
  в «куда расти».
- **`knocked` в `settings.json` не заполняется.** Постучавшиеся печатаются в
  терминал; собирать их в файл начнёт панель в срезе 3 — тогда же появится
  единственный, кто этот файл пишет.
