# Срез 3 «Правка последней записи» — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Цель:** «не 450, а 480» правит только что записанную строку — свою, не чужую,
и только те клетки, которые действительно изменились.

**Устройство:** мост уже умеет правку, питоновская сторона — нет. Бот запоминает
последнюю записанную строку **по каждому каналу отдельно**, при правке отдаёт
агенту прошлую запись целиком вместе с новым сообщением, получает исправленную
запись целиком, сам считает разницу и шлёт в таблицу только изменившиеся поля.
Фраза в ленте строится не по нашему намерению, а по списку `changed`, который
вернул мост.

**Спека:** [рабочее место](../specs/2026-09-02-рабочее-место-design.md), раздел
«Правка последней записи». Контракт моста —
[`sheet-setup.md`](../../../sheet-setup.md), раздел «Правка уже записанной строки»:
там точный вид запроса и все отказы дословно. В план они не переписаны намеренно —
у контракта должно быть одно место, иначе копии разъедутся молча.

**Образец формы и голоса:** [план среза 1](2026-09-01-срез-1-бот-который-работает.md).

## Что уже готово и переписывать не надо

Три вещи сделаны раньше, и срез 3 к ним не прикасается:

1. **`sheet.gs`** — операция `op: правка`, сверка продавца и суммы, ответ
   `{"ok": true, "row": 48, "changed": ["amount"]}`. Проверено на живом Google
   2 сентября. Ни одной строки в нём менять не потребуется.
2. **`schema.json`** — поле `intent` со значением `правка` вместо булева
   `is_expense`.
3. **`prompt.md`** — раздел «Правка только что записанного»: агент отдаёт не
   разницу, а полную новую версию траты.

Задача 3 начинается с того, что это проверяется живьём, а не принимается на веру.

## Границы среза

**Делаем:** память последней строки по каналам, разбор `intent: правка`, диff
старой записи с новой, отправку только изменившихся полей, отказ править чужое,
шесть отказов моста человеческими словами, новые проверки в `tools/selfcheck.py`,
команду `sheet.py правка` вместо ручного `curl` в `sheet-setup.md`.

**Не делаем:** правку произвольной строки (выбор записи в сайдбаре — «Куда расти»
спеки), правку из телеграма чужой строки, отмену правки, историю правок. Клиент
(`static/app.js`) не меняется ни строкой — почему, сказано в задаче 5.

## Global Constraints

Всё, что перечислено в [Global Constraints среза 1](2026-09-01-срез-1-бот-который-работает.md#global-constraints),
продолжает действовать. Ниже — то, что добавляет этот срез.

- **Дифф считает бот, и только бот.** Мост перечисляет в ответном поле `changed`
  всё, что получил, и с прежним значением не сравнивает — это не его дело.
  Пришлёт бот поле с тем же значением, что было, — мост честно запишет его в
  `changed`, и лента расскажет про правку, которой не было. Поэтому в запрос
  уходят только изменившиеся поля, и это главное требование среза.
- **Фраза в ленте — по ответу моста, а не по нашему намерению.** Строим её из
  списка `changed`, который вернула таблица.
- **Значение `op` — `правка`, кириллицей.** Так написано в `sheet.gs`, и это
  единственное место, где кириллица сравнивается по идентичности через сеть.
  Работает потому, что обе стороны — UTF-8 JSON, и потому что это **значение**,
  а не ключ. «Чинить» его на латиницу нельзя: мост тогда ответит «неизвестная
  операция».
- **Ключи в памяти и поля записи — латиницей** (`row`, `fields`, `channel`,
  `author`, `at`). Адрес канала в `state.json` начинается с латинского префикса
  (`web`, `tg:`). Причина в [Notes роадмапа](../specs/2026-09-01-чеки-бот-roadmap.md):
  кириллица безопасна там, где она данные, и опасна там, где её сравнивают по
  идентичности.
- **У правки нет очереди.** Не доехавшая строка кладётся в очередь и уезжает
  позже; не доехавшая правка — нет. Отложенная правка легла бы на строку,
  которая к тому времени могла сдвинуться, а сверка продавца и суммы делается
  в момент записи, на стороне таблицы. Не прошло — говорим человеку сразу.
- **Правка ничего не пишет в `seen`.** `seen` — про повторно присланные файлы,
  и запись правки туда сломала бы защиту от дубля.
- **Новые проверки дописываются в конец `tools/selfcheck.py`**, ничего не
  удаляя и не переставляя. Сейчас их 48, и все проходят.

## Раскладка файлов

```
чеки-бот/
  state.py            задача 1 — адрес канала, память последней строки
  sheet.py            задача 2 — edit_row() и команда «sheet.py правка»
  sheet-setup.md      задача 2 — раздел «Проверка» без curl
  intake.py           задачи 3–5 — сравнение записей, фраза, шов правки
  tools/selfcheck.py  задачи 1 и 3 — новые проверки в конец
  README.md           задача 6 — бот больше не «не правит записанное»

  sheet.gs            не меняется — мост готов с 2 сентября
  schema.json         не меняется — intent: правка уже там
  prompt.md           не меняется — раздел про правку уже там
  checks.py           не меняется — ей всё равно, откуда пришла запись
  feed.py             не меняется — правка это обычное событие ленты
  static/app.js       не меняется — said() рисует такое событие с задачи 6 среза 2
  bot.py, web.py      не меняются — оба зовут intake.accept(), как и раньше
```

Список «не меняется» здесь не для красоты. Срез 3 — первый, который целиком
умещается в уже существующие швы: если исполнителю захотелось тронуть `feed.py`
или `app.js`, значит, он свернул не туда.

---

### Задача 1: Память последней записанной строки

**Файлы:**
- Изменить: `state.py`, `tools/selfcheck.py`
- Проверка: `.venv/bin/python tools/selfcheck.py`

**Стыки:**
- Берёт: ничего, это первая задача среза.
- Отдаёт: `state.address(channel, author)` → строка-адрес;
  `state.last(state, address)` → запись или `None`;
  `state.remember_last(state, address, record)` → None;
  `state.newest(state)` → пара «адрес, запись» или `(None, None)`;
  ключ `last` в `state.EMPTY`.

**Зачем отдельная память.** В `state.py` уже есть `seen`/`remember`, и на первый
взгляд они про то же самое — «что мы уже видели». Это не так. `seen` помнит
**файлы**, чтобы не разобрать один и тот же чек дважды, и ключ там отпечаток
файла. Новая память помнит **строку в таблице**, чтобы её можно было поправить,
и ключ там канал. Свалить их в одну кучу нельзя: у них разный ключ, разный срок
жизни и разный смысл потери.

- [ ] **Шаг 1: Добавить `last` в заготовку памяти**

В `state.py`, в словарь `EMPTY`, между `seen` и `categories`:

```python
    "last": {},
```

И одной строкой дополнить докстринг модуля: там перечислено, что хранит файл,
и теперь вещей на одну больше — последняя записанная строка по каждому каналу.

Почему это важно сделать отдельным шагом: `load()` подмешивает содержимое файла
поверх `EMPTY`, поэтому старый `state.json` без ключа `last` поднимется и будет
работать. А вот если забыть про `EMPTY`, то у совсем свежей памяти ключа не
будет вовсе, и первая же правка упадёт на `KeyError` — молча для человека и
громко в терминале.

- [ ] **Шаг 2: Написать четыре функции памяти**

В конец `state.py`, после `remember()`:

```python
def address(channel, author):
    """Адрес памяти: чья это последняя строка.

    У браузера адрес один на всех: рабочее место открыто на одном компьютере,
    и человек за ним один, как бы он ни назвал себя в настройках. У телеграма
    свой адрес на каждого — там людей несколько, и последняя строка у каждого
    своя.

    Префикс латиницей и с двоеточием, а не просто имя: имя человека приходит
    из телеграма и может оказаться любым, вплоть до слова «web». Разбирать
    адрес обратно мы никогда не будем — его только сравнивают целиком."""
    if channel == "браузер":
        return "web"
    return "tg:" + author


def last(state, address):
    """Последняя строка, записанная по этому адресу, или None."""
    return state["last"].get(address)


def remember_last(state, address, record):
    """Запомнить последнюю записанную строку.

    Хранится строка целиком, все десять полей: правка сравнивает старую запись
    с новой, и сравнивать надо со всеми полями сразу, а не с суммой и
    продавцом. Время ставит сама функция, а не тот, кто её позвал: по этому
    времени потом решается, кто написал последним, и подделывать его не должно
    быть возможности даже по невнимательности.

    Растёт эта память ровно на одну запись в человека из белого списка —
    ограничивать её, как seen, незачем."""
    state["last"][address] = dict(record, at=time.time())


def newest(state):
    """Самая свежая запись из всех адресов — и адрес, по которому она лежит.

    Нужна, чтобы не поправить чужое. «Не 450, а 480» в браузере относится к
    тому, что человек видит внизу ленты, а внизу ленты стоит последняя запись
    любого канала — своя или Марии из телеграма."""
    spot, fresh = None, None
    for key, record in state["last"].items():
        if fresh is None or record["at"] > fresh["at"]:
            spot, fresh = key, record
    return spot, fresh
```

- [ ] **Шаг 3: Дописать проверки в `tools/selfcheck.py`**

В самый конец файла, **перед** пустым `print()` и блоком `if failed:`:

```python
print()
print("память последней записи")
with tempfile.TemporaryDirectory() as folder:
    state.STATE_PATH = Path(folder) / "state.json"
    memory = state.load()

    check("в заготовке есть место под последнюю запись", memory["last"] == {})

    check("у браузера адрес один на всех",
          state.address("браузер", "Лена") == state.address("браузер", "Пётр"))
    check("у каждого в телеграме свой адрес",
          state.address("телеграм", "@maria") != state.address("телеграм", "@ivan"))
    check("браузер и телеграм не сходятся в один адрес",
          state.address("браузер", "@maria") != state.address("телеграм", "@maria"))

    web = state.address("браузер", "Лена")
    tg = state.address("телеграм", "@maria")

    check("пустая память ничего не отдаёт", state.last(memory, web) is None)
    check("и самой свежей записи в ней тоже нет",
          state.newest(memory) == (None, None))

    state.remember_last(memory, web, {"row": 47, "fields": {"amount": 450},
                                      "channel": "браузер", "author": "Лена"})
    check("записанное помнится", state.last(memory, web)["row"] == 47)
    check("время проставляется само", state.last(memory, web)["at"] > 0)
    check("по чужому адресу своей строки нет", state.last(memory, tg) is None)

    state.remember_last(memory, tg, {"row": 48, "fields": {"amount": 900},
                                     "channel": "телеграм", "author": "@maria"})
    check("самая свежая — та, что записана последней",
          state.newest(memory) == (tg, state.last(memory, tg)))
    check("своя строка на месте и после чужой записи",
          state.last(memory, web)["row"] == 47)

    state.remember_last(memory, web, {"row": 49, "fields": {"amount": 120},
                                      "channel": "браузер", "author": "Лена"})
    check("помним последнюю, а не все", state.last(memory, web)["row"] == 49)

    state.save(memory)
    check("память переживает перезапуск", state.load()["last"][web]["row"] == 49)
```

Тринадцать проверок. Обратите внимание на предпоследнюю: она про то, что память
именно **последней** записи, а не журнала всех. Журнал строк живёт в таблице, и
второго такого журнала в проекте быть не должно.

- [ ] **Шаг 4: Прогнать самопроверку — и сначала сломать**

Проверка, которая не умеет падать, — не проверка.

```bash
sed -i '' 's/^    return "tg:" + author/    return "tg"/' state.py
.venv/bin/python tools/selfcheck.py; echo "код выхода: $?"
```

Ожидание: `✗ у каждого в телеграме свой адрес`, `провалено 1`, `код выхода: 1`.
Мария и Иван поправляли бы строки друг друга — ровно то, ради чего этот срез и
делается.

- [ ] **Шаг 5: Вернуть как было и прогнать снова**

```bash
sed -i '' 's/^    return "tg"$/    return "tg:" + author/' state.py
.venv/bin/python tools/selfcheck.py; echo "код выхода: $?"
```

Ожидание: `проверок пройдено: 61`, `код выхода: 0`.

- [ ] **Шаг 6: Коммит**

```bash
git add state.py tools/selfcheck.py
git commit -m "feat: бот помнит последнюю записанную строку по каждому каналу"
```

---

### Задача 2: Мост зовёт правку — `edit_row()` и `sheet.py правка`

**Файлы:**
- Изменить: `sheet.py`, `sheet-setup.md`
- Проверка: `.venv/bin/python sheet.py правка` (нужна настроенная таблица)

**Стыки:**
- Берёт: ничего из задачи 1.
- Отдаёт: `sheet.edit_row(url, secret, row, was_merchant, was_amount, changes)`
  → список полей, которые записал мост.

- [ ] **Шаг 1: Написать `edit_row()`**

В `sheet.py`, после `append_row()`:

```python
def edit_row(url, secret, row, was_merchant, was_amount, changes):
    """Правит уже записанную строку. Отдаёт список полей, которые мост записал.

    В changes кладутся только изменившиеся поля — считает их бот, до вызова.
    Мост перечисляет в ответе всё, что получил, и с прежним значением не
    сравнивает: пришлёшь неизменившееся поле — оно вернётся в changed, и лента
    расскажет про правку, которой не было.

    was_merchant и was_amount — то, что бот рассчитывает увидеть в этой строке.
    Человек мог вставить строки в таблицу руками, и номер начнёт указывать не
    туда; мост сверяет их перед записью и при несовпадении не пишет ничего.

    Очереди у правки нет, в отличие от append_row: отложенная правка легла бы
    на строку, которая к тому времени успела сдвинуться ещё раз. Не прошло —
    человеку говорится сразу."""
    answer = ask("POST", url, json=dict(changes, secret=secret, op="правка",
                                        row=row, was_merchant=was_merchant,
                                        was_amount=was_amount))
    return answer.get("changed", [])
```

Порядок в `dict(changes, ...)` важен: служебные поля идут после `changes` и
затрут одноимённые, если те как-то туда попадут. Совпасть они не могут — в
`changes` только имена колонок, — но полагаться тут на «не могут» дешевле, чем
однажды отправить в таблицу чужой `secret`.

- [ ] **Шаг 2: Добавить команду `sheet.py правка`**

В `sheet.py`, в блоке `if __name__ == "__main__":`, между веткой `== "строка"`
и `else:`:

```python
    elif len(sys.argv) > 1 and sys.argv[1] == "правка":
        # Проверка правки должна быть самодостаточной: сама заводит строку,
        # сама её правит и сама же ломает сверку. Раньше это делалось руками
        # через curl, и человек подставлял в запрос номер строки и секрет —
        # то есть проверка начиналась с двух мест, где можно ошибиться.
        number = append_row(env["sheet_url"], env["sheet_secret"], {
            "date": "2026-09-01", "amount": 1, "currency": "RUB",
            "merchant": "проверка правки", "category": "Прочее",
            "payment": "неизвестно", "source": "проверка", "who": "терминал",
            "status": "проверить", "file": "",
        })
        print(f"записал строку {number}: «проверка правки», сумма 1")

        changed = edit_row(env["sheet_url"], env["sheet_secret"], number,
                           "проверка правки", 1, {"amount": 2})
        print(f"поправил {changed} — в таблице теперь 2")

        # Тот же запрос второй раз: в строке уже 2, а мы говорим, что ждём 1.
        # Сверка не сойдётся, и это единственный правильный исход.
        try:
            edit_row(env["sheet_url"], env["sheet_secret"], number,
                     "проверка правки", 1, {"amount": 3})
            print("!! мост записал правку по несошедшейся сверке — так быть не должно")
            sys.exit(1)
        except SheetError as error:
            print(f"вторая правка отклонена, и это правильно: {error}")

        print(f"строку {number} удалите из таблицы руками")
```

Строка называется «проверка правки», а не «проверка связи», как у команды
`строка`: две проверки не должны оставлять в таблице одинаковый мусор, иначе
непонятно, какую из них не убрали.

- [ ] **Шаг 3: Прогнать живьём**

```bash
.venv/bin/python sheet.py правка
```

Ожидание — четыре строки: номер записанной строки, `поправил ['amount']`,
`вторая правка отклонена, и это правильно: строка N изменилась: ожидались
«проверка правки» и 1, а в таблице «проверка правки» и 2 — поправьте в таблице
руками`, и напоминание удалить строку. Откройте таблицу и убедитесь глазами:
в строке `2`, а в колонке «записано» — время правки, а не время записи.

Удалите строку из таблицы руками.

- [ ] **Шаг 4: Заменить curl в `sheet-setup.md`**

В разделе «Проверка» блок с `curl` и абзацы вокруг него заменить на третью
команду в общем списке:

```bash
.venv/bin/python sheet.py           # читает справочник
.venv/bin/python sheet.py строка    # дописывает тестовую строку
.venv/bin/python sheet.py правка    # дописывает строку, правит её и ломает сверку
```

Текст про третью команду сохраняет то, ради чего был написан curl: она
показывает **оба** исхода — удачную правку и отказ по сверке, — и второй тут
важнее первого. Оговорку про флаг `-L` из этого раздела убрать: она осталась от
curl, а на своём месте — в разделе «Редиректы отключать нельзя» — она никуда не
делась.

- [ ] **Шаг 5: Коммит**

```bash
git add sheet.py sheet-setup.md
git commit -m "feat: sheet.py умеет правку строки, проверка вместо curl"
```

---

### Задача 3: Схема, промпт и сравнение старой записи с новой

**Файлы:**
- Изменить: `intake.py`, `tools/selfcheck.py`
- Не менять: `schema.json`, `prompt.md` — шаг 1 объясняет, почему
- Проверка: `.venv/bin/python tools/selfcheck.py`

**Стыки:**
- Берёт: `checks.is_number()`.
- Отдаёт: `intake.EDITABLE`, `intake.LABELS`, `intake.rework(old, message)`,
  `intake.same(before, after)`, `intake.diff(old, new)`, `intake.look(value)`,
  `intake.retell(changed, old, new)`.

Задача целиком про чистые функции: ни сети, ни модели, ни таблицы. Поэтому её
можно и нужно закрыть самопроверкой, а не глазами.

- [ ] **Шаг 1: Убедиться, что схема и промпт менять не надо**

Не поверить на слово, а посмотреть:

```bash
.venv/bin/python -c "import json; print(json.load(open('schema.json'))['properties']['intent']['enum'])"
grep -n "Правка только что записанного" -A 8 prompt.md
```

Ожидание: `['расход', 'правка', 'не расход']` и раздел промпта, где сказано, что
при `intent: правка` остальные поля несут исправленную запись целиком, а не
разницу. Оба места готовы с 2 сентября.

Теперь то же самое живьём, одним вызовом агента:

```bash
.venv/bin/python agent.py "не 450, а 480"
```

Ожидание: в ответе `"intent": "правка"`. Остальные поля будут бедными —
продавца и даты в этом сообщении нет, и взять их неоткуда. **Это и есть причина
всей задачи 5:** одного сообщения агенту мало, ему нужно показать прошлую
запись. Здесь мы проверяем ровно одно — что намерение он распознаёт.

Если правите `prompt.md` — правьте и `.claude/agents/expense-reader.md`: это
дословная копия, и самопроверка сравнивает их посимвольно. Но в этом срезе
менять там нечего.

- [ ] **Шаг 2: Написать задание агенту на правку**

В конец `intake.py`. Сначала `import json` в шапку модуля, к остальным импортам,
потом:

```python
# Что из записи агент вправе поправить. Остальные четыре поля — source, who,
# file и status — говорят, откуда взялась строка и что с ней сделали проверки.
# Правкой они не меняются, и показать их агенту значило бы позвать его их менять.
EDITABLE = ("date", "amount", "currency", "merchant", "category", "payment")


def rework(old, message):
    """Задание агенту на правку: прошлая запись плюс слова человека.

    Отдельного входа для правки в agent.parse() нет намеренно. Правка — это
    сообщение текстом, к которому приложена прошлая запись; шов между ботом и
    движком от этого не меняется, и оба движка продолжают работать без единой
    правки в agent.py и engines/.

    Строчная буква в начале не опечатка: agent.build_task() ставит перед этим
    текстом «Разбери сообщение о расходе:», и фраза продолжает его."""
    known = {name: old.get(name) for name in EDITABLE}
    return ("человек поправляет уже записанную трату. Вот она целиком: "
            + json.dumps(known, ensure_ascii=False)
            + f". Человек пишет: «{message}». Верни эту же запись с "
              "исправлениями — поля, которых правка не касается, оставь как были.")
```

- [ ] **Шаг 3: Написать сравнение записей**

Дальше в `intake.py`:

```python
def same(before, after):
    """Одно ли это значение.

    Числа сравниваются как числа: 450 и 450.0 — одна и та же сумма. Пустая
    клетка и None — тоже одно: в таблицу они уезжают одинаково, и объявить их
    разными значило бы каждый раз стирать пустую клетку заново."""
    if checks.is_number(before) and checks.is_number(after):
        return float(before) == float(after)
    if before in ("", None) and after in ("", None):
        return True
    return before == after


def diff(old, new):
    """Что изменилось: словарь только из изменившихся полей.

    Это обязанность питоновской стороны, и ничьей другой. Мост перечисляет в
    ответном поле changed всё, что получил, и с прежним значением не сравнивает
    — это не его дело. Пошлём поле, которое не менялось, — мост честно вернёт
    его в changed, и лента расскажет про правку, которой не было.

    Никаких исключений по именам полей здесь нет: происхождение строки не
    попадает в дифф не потому, что мы его отсюда выкинули, а потому, что новая
    запись собирается из старой и эти поля в ней те же самые."""
    return {name: new[name] for name in new if not same(old.get(name), new[name])}
```

- [ ] **Шаг 4: Написать фразу для ленты**

Дальше в `intake.py`:

```python
# Имена колонок человеческими словами, в винительном падеже: «Поправил сумму».
# Ключи латиницей — это имена полей протокола, значения русские, как в схеме.
LABELS = {
    "date": "дату", "amount": "сумму", "currency": "валюту",
    "merchant": "продавца", "category": "статью", "payment": "способ оплаты",
    "source": "источник", "who": "автора", "status": "пометку", "file": "файл",
}


def look(value):
    """Значение словами для ленты.

    480.0 — это «480»: хвостовой ноль в сумме читается как копейки, которых
    не было. Пустая клетка — «пусто», иначе фраза «Поправил продавца:  → Магнит»
    выглядит как поломка, а не как правка."""
    if checks.is_number(value):
        return str(int(value)) if float(value) == int(value) else f"{value:.2f}"
    if value is None or not str(value).strip():
        return "пусто"
    return str(value)


def retell(changed, old, new):
    """«Поправил сумму: 450 → 480».

    Список changed берётся из ответа моста, а не из нашего диффа: в ленте должно
    стоять то, что таблица подтвердила, а не то, что мы собирались сделать.
    Незнакомое имя поля не роняет фразу — показываем его как есть: лучше
    непонятное слово в ленте, чем исключение вместо ответа."""
    parts = []
    for name in changed:
        parts.append(f"{LABELS.get(name, name)}: "
                     f"{look(old.get(name))} → {look(new.get(name))}")
    if not parts:
        return "Поправил строку"
    return "Поправил " + ", ".join(parts)
```

- [ ] **Шаг 5: Дописать проверки в `tools/selfcheck.py`**

В шапке файла, к импортам, добавить `import intake`. Он тянет за собой `agent`,
`sheet` и `config`, но ни один из них при импорте ничего не делает — ни в сеть
не ходит, ни `.env` не читает.

Дальше в самый конец файла, **перед** пустым `print()` и блоком `if failed:`:

```python
print()
print("сравнение записей")
was = {"date": "2026-09-01", "amount": 450.0, "currency": "RUB",
       "merchant": "Пятёрочка", "category": "Продукты", "payment": "карта",
       "source": "текст", "who": "Лена", "status": "готово", "file": ""}

check("одинаковые записи — пустой дифф", intake.diff(was, dict(was)) == {})
check("изменилась сумма — в диффе только сумма",
      intake.diff(was, dict(was, amount=480.0)) == {"amount": 480.0})
check("450 и 450.0 — одна и та же сумма",
      intake.diff(was, dict(was, amount=450)) == {})
check("пустая клетка и None — одно и то же",
      intake.diff(was, dict(was, file=None)) == {})
check("стереть продавца — тоже правка",
      intake.diff(was, dict(was, merchant="")) == {"merchant": ""})
check("дифф ничего не исключает: подмену источника он бы тоже заметил",
      intake.diff(was, dict(was, source="фото чека")) == {"source": "фото чека"})

check("в задании на правку есть прошлая запись",
      "Пятёрочка" in intake.rework(was, "не 450, а 480"))
check("и слова человека", "не 450, а 480" in intake.rework(was, "не 450, а 480"))
check("а происхождение строки агенту не показывается",
      "Лена" not in intake.rework(was, "не 450, а 480"))

now = dict(was, amount=480.0, category="Кафе и рестораны")
check("одно поле — «Поправил сумму: 450 → 480»",
      intake.retell(["amount"], was, now) == "Поправил сумму: 450 → 480")
check("два поля — оба в одной фразе",
      intake.retell(["amount", "category"], was, now) ==
      "Поправил сумму: 450 → 480, статью: Продукты → Кафе и рестораны")
check("копейки не теряются",
      intake.retell(["amount"], was, dict(was, amount=480.5)) ==
      "Поправил сумму: 450 → 480.50")
check("пустое значение читается как «пусто»",
      intake.retell(["merchant"], was, dict(was, merchant="")) ==
      "Поправил продавца: Пятёрочка → пусто")
check("незнакомое поле из ответа моста фразу не роняет",
      "выдумка" in intake.retell(["выдумка"], was, now))
```

Четырнадцать проверок. Две из них стоят особняком и объясняют весь срез:
«450 и 450.0 — одна и та же сумма» — потому что иначе бот отправил бы мосту
неизменившуюся сумму и лента отчиталась бы о правке, которой не было; «дифф
ничего не исключает» — потому что защита тут не в списке исключений, а в том,
что новая запись собирается из старой.

- [ ] **Шаг 6: Прогнать самопроверку — и сначала сломать**

```bash
sed -i '' 's/^        return float(before) == float(after)/        return before == after/' intake.py
.venv/bin/python tools/selfcheck.py; echo "код выхода: $?"
```

Ожидание: `✗ 450 и 450.0 — одна и та же сумма`, `провалено 1`, `код выхода: 1`.
`450 == 450.0` в питоне True, а вот `Decimal` и строки так уже не сравнились бы;
проверка сторожит именно это место, а не арифметику.

- [ ] **Шаг 7: Вернуть как было и прогнать снова**

```bash
sed -i '' 's/^        return before == after$/        return float(before) == float(after)/' intake.py
.venv/bin/python tools/selfcheck.py; echo "код выхода: $?"
```

Осторожно: в `same()` строка `return before == after` встречается дважды —
внутри `if` с числами и последней строкой функции. Первая правка меняет ту, что
с отступом в восемь пробелов, обратная — её же (`$` в конце шаблона не даёт
задеть вторую, у неё отступ в четыре). Убедитесь глазами, что функция вернулась
в прежний вид.

Ожидание: `проверок пройдено: 75`, `код выхода: 0`.

- [ ] **Шаг 8: Коммит**

```bash
git add intake.py tools/selfcheck.py
git commit -m "feat: сравнение старой записи с новой и фраза о правке"
```

---

### Задача 4: Шов запоминает, что записал

**Файлы:**
- Изменить: `intake.py`
- Проверка: `.venv/bin/python bot.py`, запись расхода в браузере, потом
  чтение `state.json`

**Стыки:**
- Берёт: `state.address()`, `state.remember_last()` из задачи 1.
- Отдаёт: заполненный ключ `last` в `state.json`.

Маленькая задача, и намеренно отдельная: после неё бот **помнит** последнюю
строку, но править по-прежнему отказывается. Это состояние можно проверить
целиком, не запуская ни одной правки, — а значит, если правка потом сломается,
будет видно, в какой из двух половин.

- [ ] **Шаг 1: Запоминать записанную строку**

В `intake.py`, в `accept()`, сразу после блока с `memory.remember(st, job["mark"], number)`
и перед `events.append({"kind": "запись", ...})` — добавить:

```python
    # Запомнили, что записали последним: из этого потом делается правка.
    # Помним и строку, не доехавшую до таблицы (number is None) — иначе
    # «не 450, а 480» уехало бы в предыдущую, давно записанную строку.
    memory.remember_last(st, memory.address(job["channel"], job["author"]), {
        "row": number,
        "fields": row,
        # Канал и автор лежат здесь не для адресации — адрес уже посчитан.
        # Они нужны, чтобы сказать человеку, чью строку он пытается поправить.
        "channel": job["channel"],
        "author": job["author"],
    })
```

Ничего больше в `accept()` на этой задаче не меняется: заглушка «Правки пока не
умею» остаётся на месте до задачи 5.

- [ ] **Шаг 2: Проверить живьём**

Запустите бота, откройте рабочее место и напишите «такси 450 наличными».
Дождитесь строки в ленте, потом в другом окне терминала:

```bash
.venv/bin/python -c "
import json
print(json.dumps(json.load(open('state.json'))['last'], ensure_ascii=False, indent=2))
"
```

Ожидание: один адрес `web`, в нём `row` с номером строки из ленты, `fields` со
всеми десятью полями записи, `channel`, `author` и `at`. Номер в `row` и номер в
ленте должны совпадать — это единственное место, где они сверяются глазами, и
дальше весь срез на этом совпадении держится.

- [ ] **Шаг 3: Проверить, что чужая запись ложится по своему адресу**

Если телеграм подключён — напишите боту с телефона трату и повторите команду
выше. Ожидание: два адреса, `web` и `tg:@ваш_ник`, у каждого свой номер строки.

Телеграм не подключён — этот шаг закрывается на приёмке, в задаче 6, где чужая
запись подделывается руками.

- [ ] **Шаг 4: Коммит**

```bash
git add intake.py
git commit -m "feat: бот запоминает последнюю записанную строку"
```

---

### Задача 5: Правка последней записи и все отказы

**Файлы:**
- Изменить: `intake.py`
- Проверка: `.venv/bin/python bot.py` и правка живьём в браузере

**Стыки:**
- Берёт: всё из задач 1–4 плюс `agent.parse()`, `checks.review()`,
  `sheet.edit_row()`.
- Отдаёт: `intake.fix()` — правка целиком, от памяти до события ленты.

**Почему агент зовётся дважды.** Первый вызов разбирает сообщение и говорит
`intent: правка` — и на этом его ответ исчерпан: в «не 450, а 480» нет ни
продавца, ни даты, ни статьи, и взять их неоткуда. Второй вызов получает
прошлую запись целиком и возвращает исправленную запись целиком.

Соблазн сделать это одним вызовом — всегда подкладывать прошлую запись в разбор
любого сообщения — отклонён: тогда поля прошлой траты подмешивались бы в каждый
новый чек, и «такси 450» унаследовало бы статью от «Пятёрочки». Правки редки,
чеки часты; платить лишним вызовом за редкое надёжнее, чем портить частое.

- [ ] **Шаг 1: Написать `fix()`**

В `intake.py`, сразу после `accept()` и перед `sign()`. Сначала `import requests`
в шапку модуля — понадобится, чтобы отличить молчание сети от отказа таблицы:

```python
def fix(env, st, job, categories, today, engine):
    """Правка последней записи. Возвращает события ленты.

    Ответ первого разбора сюда не передаётся: из него взято одно — намерение.
    Здесь агент зовётся второй раз, уже вместе с прошлой записью."""
    here = memory.address(job["channel"], job["author"])
    spot, fresh = memory.newest(st)

    # В браузере лента общая: внизу может стоять запись из телеграма, и «не
    # 450, а 480» тогда скорее всего про неё. Молча поправить вместо неё свою
    # прошлую строку — худшее, что тут можно сделать, поэтому отказываем и
    # показываем, чья это строка.
    #
    # В телеграме ровно наоборот: человек видит только свой чат, чужих записей
    # для него не существует, и отказ по строке, которой он не видел, выглядел
    # бы поломкой. Поэтому проверка на самую свежую запись — только для
    # браузера, а память по каналам работает в обоих.
    if job["channel"] == "браузер" and fresh and spot != here:
        line = (f"строка {fresh['row']}" if fresh["row"]
                else "запись, ещё не доехавшая до таблицы")
        return [{
            "kind": "слово",
            "text": f"Не трогаю: последняя запись не ваша — {line}, "
                    f"{fresh['author']} · {fresh['channel']}.",
            "note": "Правлю только то, что записано в этом же окне. "
                    "Чужую строку поправьте в таблице руками.",
        }]

    mine = memory.last(st, here)
    if mine is None:
        return [{"kind": "слово", "text": "Пока нечего править — я ещё ничего не записывал.",
                 "note": "Пришлите трату, а поправить её можно следующим сообщением."}]
    if mine["row"] is None:
        # Строка ушла в очередь и номера у неё пока нет. Править по номеру
        # соседней строки нельзя — там чужой расход.
        return [{"kind": "слово",
                 "text": "Последняя запись ещё не доехала до таблицы — править нечего.",
                 "note": "Она уедет сама, когда таблица ответит. Тогда и поправим."}]

    old = mine["fields"]
    try:
        answer = agent.parse("текст", rework(old, job["payload"]), categories,
                             today, engine)
    except agent.AgentError as error:
        print(f"агент не справился с правкой: {error}")
        return [{"kind": "слово", "text": "Не смог разобрать правку.",
                 "note": "Напишите, что поправить: «не 450, а 480»."}]

    if answer["intent"] != "правка":
        # Увидев прошлую запись, агент передумал: это не правка, а новая
        # трата. Дописать её отсюда нельзя — путь записи в проекте один, и он
        # в accept(); второго такого пути заводить не будем.
        return [{"kind": "слово", "text": "Это похоже не на правку, а на новую трату.",
                 "note": "Пришлите её отдельным сообщением."}]

    # Проверки без модели те же самые: правка — такая же запись, и дата из
    # будущего в ней ловится так же. Статус пересчитывается, и это важно в обе
    # стороны: продиктованная сумма снимает с строки пометку «проверить», а
    # правка, испортившая дату, эту пометку ставит.
    verdict = checks.review(answer, categories, today)
    amount = answer["amount"] if checks.is_number(answer["amount"]) else ""

    # Новая запись собирается из старой: source, who и file остаются как были.
    # Поэтому дифф их и не увидит — не потому, что мы их из него выкинули.
    new = dict(old,
               date=answer["date"],
               amount=amount,
               currency=answer["currency"],
               merchant=answer["merchant"],
               category=verdict["category"],
               payment=answer["payment"],
               status=verdict["status"])

    changes = diff(old, new)
    if not changes:
        # Мост на такой запрос ответил бы «в правке не пришло ни одного поля
        # строки: менять нечего» — правильный отказ, но человеку он не про то.
        # Запрос, отказ которого известен заранее, лучше не посылать.
        return [{"kind": "слово",
                 "text": f"Ничего не изменилось — строка {mine['row']} и так такая.",
                 "note": answer["reply"]}]

    try:
        changed = sheet.edit_row(env["sheet_url"], env["sheet_secret"],
                                 mine["row"], old["merchant"], old["amount"],
                                 changes)
    except sheet.SheetError as error:
        # Отказ моста написан для человека — передаём его дословно. «Строка 48
        # изменилась: ожидались „Пятёрочка“ и 450, а в таблице „Магнит“ и 980»
        # понятнее всего, что мы могли бы сочинить вместо него: по этой фразе
        # видно, куда уехала строка.
        print(f"таблица не приняла правку: {error}")
        return [{"kind": "слово", "text": f"Не поправил строку {mine['row']}.",
                 "note": str(error)}]
    except requests.RequestException as error:
        # А это написано для программиста: «HTTPSConnectionPool… Max retries
        # exceeded» человеку не говорит ничего. В терминал — как есть, в ленту
        # — своими словами.
        print(f"таблица не ответила на правку: {error}")
        return [{"kind": "слово",
                 "text": f"Таблица не ответила — строка {mine['row']} осталась прежней.",
                 "note": "Очереди у правки нет, попробуйте ещё раз."}]

    # Поправленная строка снова становится последней, и по времени тоже:
    # следующее «нет, 500» ляжет на неё же, а не на ту, что была до правки.
    memory.remember_last(st, here, dict(mine, fields=new))

    # Фраза строится по changed из ответа таблицы, а не по нашему changes:
    # в ленте должно стоять то, что таблица подтвердила.
    event = {"kind": "слово", "text": retell(changed, old, new), "row": mine["row"]}
    trouble = verdict["reasons"] + verdict["warnings"]
    if trouble:
        event["note"] = "; ".join(trouble)
    return [event]
```

- [ ] **Шаг 2: Заменить заглушку в `accept()`**

В `intake.py`, в `accept()`, найти блок:

```python
    if answer["intent"] == "правка":
        # Правка последней записи появится в срезе 3. Пока честно говорим,
        # что не умеем, и ничего не пишем: молча проглотить исправление
        # хуже, чем отказать.
        if path:
            move(path, DOUBTFUL)
        events.append({"kind": "слово", "text": "Правки пока не умею.",
                       "note": "Пришлите трату заново, целиком."})
        return [sign(event, job) for event in events]
```

и заменить его целиком на:

```python
    if answer["intent"] == "правка":
        if path:
            # Фотография правкой не бывает: так агент отвечает, когда на
            # снимке не чек. Ведём себя как с «не расход» — файл в спорные,
            # человеку честный ответ.
            move(path, DOUBTFUL)
            events.append({"kind": "слово", "text": "На фотографии не вижу расхода.",
                           "note": "Правку пришлите текстом: «не 450, а 480»."})
        else:
            events += fix(env, st, job, categories, today, settings["engine"])
        return [sign(event, job) for event in events]
```

Больше в `accept()` не меняется ничего. Остальные ветки — «не расход», проверки,
запись — стоят как стояли, и блок памяти из задачи 4 тоже.

- [ ] **Шаг 3: Проверить правку живьём**

Запустите бота, откройте рабочее место.

1. «такси 450 наличными» → запись, запомните номер строки.
2. «не 450, а 480» → в ленте `Поправил сумму: 450 → 480 — строка N`.
3. Откройте таблицу: в строке N сумма 480, продавец прежний, в колонке
   «записано» время правки.
4. «это было не такси, а метро» → `Поправил продавца: такси → метро`.
5. «не 480, а 480» → `Ничего не изменилось — строка N и так такая`.

Пункт 5 — главный в этом шаге. Он показывает, что дифф работает: без него мост
получил бы неизменившуюся сумму, вернул бы её в `changed`, и лента отчиталась бы
о правке, которой не было.

- [ ] **Шаг 4: Проверить отказы, до которых можно дотянуться**

```
«не 450, а 480» первым же сообщением после запуска   → «Пока нечего править»
правка после чужой записи (задача 6, пункт 3)        → «последняя запись не ваша»
строку в таблице сдвинули руками (задача 6, пункт 4) → отказ моста дословно
```

Из шести отказов правки, перечисленных в `sheet-setup.md`, до человека
по-настоящему доходят два: «строки больше нет» и «строка изменилась». Остальные
четыре — неизвестная операция, нет номера строки, нет ожидаемых продавца и
суммы, не пришло ни одного поля — до него дойти не должны никогда: каждый из них
означает ошибку на нашей стороне, и все четыре закрыты кодом до вызова моста.
Поэтому они и печатаются в терминал: увидели такой текст в ленте — чинить надо
бота, а не таблицу.

- [ ] **Шаг 5: Самопроверка не должна сломаться**

```bash
.venv/bin/python tools/selfcheck.py; echo "код выхода: $?"
```

Ожидание: те же `проверок пройдено: 75`. Новых проверок эта задача не приносит —
`fix()` ходит и в сеть, и к модели, а самопроверка живёт без обеих. Всё, что из
правки можно проверить без них, вынесено в чистые функции задачи 3, и это не
случайность, а способ разложить задачу.

- [ ] **Шаг 6: Коммит**

```bash
git add intake.py
git commit -m "feat: «не 450, а 480» правит последнюю запись, чужую не трогает"
```

---

### Задача 6: Приёмка живьём, README и роадмап

## Как проверяем
