{
  "type": "object",
  "additionalProperties": false,
  "required": ["intent", "date", "amount", "amount_source", "currency",
               "merchant", "category", "payment", "confidence", "doubts",
               "reply"],
  "properties": {
    "intent": {
      "type": "string",
      "enum": ["расход", "правка", "не расход"],
      "description": "Что это за сообщение. «Расход» — новая трата. «Правка» — человек поправляет только что записанную трату («не 450, а 480»). «Не расход» — не про деньги: «привет», меню, визитка и прочее."
    },
    "date": {
      "type": "string",
      "description": "Дата операции в виде ГГГГ-ММ-ДД. Если не удалось определить — пустая строка."
    },
    "amount": {
      "type": ["number", "null"],
      "description": "Итоговая сумма в рублях и копейках. Если не прочиталась — null. Не угадывать."
    },
    "amount_source": {
      "type": "string",
      "enum": ["строка ИТОГ", "строка К ОПЛАТЕ", "сложены позиции", "не нашёл",
               "сказано в сообщении"],
      "description": "Откуда взята сумма. Не оценка уверенности, а факт. Для сообщения текстом — всегда «сказано в сообщении»."
    },
    "currency": {
      "type": "string",
      "enum": ["RUB", "USD", "EUR", "другая"]
    },
    "merchant": {
      "type": "string",
      "description": "Кому заплатили или за что. Название с чека либо суть из текста."
    },
    "category": {
      "type": "string",
      "description": "Статья расхода — строго одна из списка в задании."
    },
    "payment": {
      "type": "string",
      "enum": ["карта", "наличные", "перевод", "неизвестно"]
    },
    "confidence": {
      "type": "string",
      "enum": ["высокая", "средняя", "низкая"],
      "description": "Низкая — если сумма или дата под вопросом."
    },
    "doubts": {
      "type": "string",
      "description": "Что именно не прочиталось. Пустая строка, если всё ясно."
    },
    "reply": {
      "type": "string",
      "description": "Одна короткая фраза человеку в чат."
    }
  }
}
